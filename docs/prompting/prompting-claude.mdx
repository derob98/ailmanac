---
sidebar_position: 5
title: "Prompting Claude Specifically"
description: "What's genuinely Claude-specific vs universal."
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/overview">
Anthropic's prompt-engineering guidance evolves with each model — check the official prompt-engineering docs for the current best practices.
</VerifyNote>

Most of [prompting](/docs/prompting/basics) is universal. But a few techniques are especially effective with Claude — and a couple of habits from older models are now counterproductive. This lesson separates the signal from the noise: what's actually Claude-specific.

<Callout type="objectives" items={["Recognize the Claude-specific techniques worth leaning into — XML tags, clarity, roles, thinking, examples, and prefilling", "Unlearn the old-model habits (ALL-CAPS, threats, over-engineered scaffolding) that now backfire", "Know which two habits — XML-tag affinity and prefilling — to carry as your Claude signature", "Understand what to re-check when you switch models"]} />

## Lean into these with Claude

These are the moves that pay off the most with Claude. The first one is the single highest-value habit you can build.

- **[XML tags](/docs/prompting/xml-tags).** Claude is well-tuned to structured prompts with named tags for inputs, instructions, and examples. This is the highest-value Claude habit.
- **Be clear and direct.** State the goal, constraints, and what "done" looks like. Claude follows explicit instructions closely.
- **Give it a role** via the [system prompt](/docs/foundations/roles).
- **Let it think on hard problems** — ask for step-by-step reasoning, or use [extended thinking/effort](/docs/api/thinking-and-effort) on reasoning models (where you often *don't* need to ask for CoT explicitly).
- **Use examples** ([few-shot](/docs/prompting/few-shot)) for format and edge cases.
- **Prefill** the start of Claude's response (on the API) to steer format.

### Try prefilling

Prefilling means starting the assistant turn yourself so Claude continues in the format you want. On the API, begin the assistant turn with an opening character or heading:

<PromptCard title="Prefill to push JSON output">{`{`}</PromptCard>

<PromptCard title="Prefill a heading to push structure">{`## Summary`}</PromptCard>

<Callout type="tip" items={["Begin the assistant turn with { to push JSON, or with a heading to push structured output. Prefilling is an API-only lever — it steers format without spending words on instructions."]} />

## Drop these old habits

Newer, literal-following Claude models change the rules. Tricks that helped on older models now get in the way.

<Callout type="warning" items={["Ease off ALL-CAPS and threat-style prompts (\"YOU MUST… OR ELSE\"). Newer Claude models follow instructions literally, and politely-phrased, specific instructions work better than shouting.", "Don't over-engineer with elaborate \"jailbreak-style\" scaffolding — clear, honest instructions outperform tricks.", "Less is often more — a tight prompt beats a sprawling one that buries the ask."]} />

## Claude vs other models

The *principles* transfer (see [Cross-AI Prompt Translation](/docs/prompting/cross-ai-translation)), but two things are most worth remembering as Claude-specific.

<Steps items={[{title: "Default to XML tags", body: "Wrap inputs, instructions, and examples in named tags. Claude's tag affinity makes this the most reliable structuring habit."}, {title: "Reach for prefilling on the API", body: "Seed the assistant turn to lock in format — an opening brace for JSON, a heading for structure."}, {title: "Re-check tone and formatting when you switch models", body: "When you move off Claude, re-check tone intensity and formatting cues; what's literal here may not transfer cleanly."}]} />

<Flashcards title="Claude-specific quick reference" cards={[{front: "Highest-value Claude habit", back: "Structured prompts with named XML tags for inputs, instructions, and examples."}, {front: "Prefilling", back: "Starting Claude's response on the API to steer format — e.g. begin with { for JSON or a heading for structure."}, {front: "ALL-CAPS / threat prompts", back: "Drop them. Newer Claude models follow literal, politely-phrased, specific instructions better than shouting."}, {front: "Over-engineered scaffolding", back: "Avoid it. Clear, honest instructions outperform jailbreak-style tricks."}, {front: "Extended thinking / effort", back: "On reasoning models, let Claude think on hard problems — you often don't need to ask for chain-of-thought explicitly."}]} />

<Callout type="takeaways" items={["XML tags are the highest-value Claude habit; prefilling is the most useful Claude-specific format lever.", "Newer Claude models follow instructions literally — be clear and direct, not loud or threatening.", "Less is more: a tight, honest prompt beats elaborate scaffolding.", "Principles transfer across models, but re-check tone intensity and formatting cues when you switch."]} />

<Quiz title="Check yourself" questions={[{q: "Which technique is described as the highest-value Claude-specific habit?", options: ["ALL-CAPS emphasis", "Named XML tags for inputs, instructions, and examples", "Threat-style prompting"], answer: 1, explain: "Claude is well-tuned to structured prompts with named XML tags — this is called out as the highest-value Claude habit."}, {q: "How does prefilling steer Claude's output format on the API?", options: ["By shouting the format in ALL-CAPS", "By starting the assistant response — e.g. with { for JSON or a heading for structure", "By adding jailbreak-style scaffolding"], answer: 1, explain: "Prefilling the start of Claude's response (begin with { to push JSON, or a heading to push structure) steers the format."}, {q: "What should you do with ALL-CAPS and \"YOU MUST… OR ELSE\" prompts on newer Claude models?", options: ["Use them more aggressively", "Ease off them — literal, politely-phrased, specific instructions work better", "Combine them with elaborate scaffolding"], answer: 1, explain: "Newer Claude models follow instructions literally; specific, polite instructions outperform shouting and threats."}]} />

## Next

- [Structuring Prompts with XML Tags](/docs/prompting/xml-tags)
- [Cross-AI Prompt Translation](/docs/prompting/cross-ai-translation)
- [Extended Thinking & Effort](/docs/api/thinking-and-effort)
