---
sidebar_position: 15
title: "AI Text Watermarks: What SynthID-Text Actually Detects (and What It Doesn't)"
description: "How the watermark that Claude, Gemini and other frontier models just started stamping into their text really works — SynthID-Text sampling bias, why detection needs a long passage, what it survives, what it doesn't, and the EU rule that forced it live on 2 August 2026."
keywords: [AI text watermark, SynthID-Text, Claude watermark, EU AI Act watermarking, Article 50, provenance, C2PA, AI content detection, synthid text detection]
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-08-18" source="https://www.anthropic.com/news/claude-text-watermark, https://en.wikipedia.org/wiki/General-Purpose_AI_Code_of_Practice">
Watermarking is being rolled out across providers this month. Feature scope, model coverage, and the detector API are all moving weekly — check each provider's docs before quoting a specific "yes we watermark X" or "no we don't."
</VerifyNote>

On **14 August 2026** Anthropic published a full FAQ on how Claude now stamps an invisible statistical signature into its text output. It's not the first — Google shipped SynthID-Text for Gemini in 2024, and the same week Anthropic's post appeared, other frontier providers were quietly turning the same feature on. The forcing function isn't marketing. It's [**Article 50 of the EU AI Act**](https://en.wikipedia.org/wiki/General-Purpose_AI_Code_of_Practice), whose transparency obligations for generative AI became **fully enforceable on 2 August 2026** — every provider serving the EU now has to mark AI-generated content in a machine-detectable way.

Coverage of the launch mostly said "AI text now has watermarks." That's the least interesting thing about it. This lesson is the mechanism: how the watermark is embedded, what a detector actually returns, the four ways it silently vanishes, and what "watermark detected" really tells you.

<Callout type="objectives" items={[
  "Explain the SynthID-Text mechanism — how a secret key biases token selection without changing what the model can say",
  "Predict when detection will and won't work: passage length, factual vs creative prose, code, translation, proofreading",
  "Read a watermark detector's output correctly — it returns a probability score, not a yes/no verdict",
  "List the four common operations that erase the watermark, and the one that leaves it fully intact",
  "Understand the EU AI Act Article 50 rule that forced text watermarking live across providers on 2 August 2026"
]} />

## The core idea: bias the coin flip, don't change the words

An LLM generates text one token at a time. At each step it produces a probability distribution over the vocabulary — often thousands of candidate tokens with non-trivial probability — and *samples* from it. When you write "the weather was", the model may weight `sunny`, `overcast`, `grey`, `mild`, `cool` as roughly equivalent choices; sampling picks one. That final coin flip normally uses an arbitrary random source.

A watermark **replaces the random source with a deterministic one derived from a secret key and a short window of the tokens that came just before.** The vocabulary hasn't changed, the probabilities haven't changed, and no specific word is forced. What changes is *which* of the equally-plausible options tends to get picked. As Anthropic phrases it: it isn't that the model becomes biased toward `overcast` or `grey` in general — `overcast` might be selected in one sentence, `grey` in the next. What accumulates over a long enough passage is a **statistical correlation between the local context and the token chosen** that random text simply cannot reproduce. That correlation is the watermark.

Anthropic's implementation is [a version of **SynthID-Text**](https://www.anthropic.com/news/claude-text-watermark), the scheme Google DeepMind published in *Nature* in 2024 (see the [Google DeepMind SynthID overview](https://deepmind.google/technologies/synthid/)). Because the bias sits inside sampling and only nudges between tokens the model already considered acceptable, quality on standard benchmarks is measurably unchanged — which is what let Google roll it out silently to Gemini traffic without users noticing.

<Steps items={[
  {
    title: "The model produces the usual probability distribution",
    body: "For token N+1 the LLM computes probabilities over the vocabulary as it always would. No re-training. No filter. This is why the model's capability is unaffected."
  },
  {
    title: "A pseudo-random function of (key + previous tokens) scores each candidate",
    body: "A secret key combined with a short window of tokens just before position N+1 seeds a function that assigns each candidate token a pseudo-random g-value. Same key + same context = same scores, always."
  },
  {
    title: "Sampling is nudged toward higher-scoring tokens",
    body: "The sampler is tilted to slightly prefer candidates with favourable g-values, subject to keeping the output distribution close to the original. On any single choice this is invisible. Over many choices, it plants a repeatable pattern."
  },
  {
    title: "A detector re-runs the same scoring against the finished text",
    body: "Given the text and the same key, the detector computes what the g-values would have been at every position and measures how often the observed tokens landed on high-scoring options. Random human text scores near chance; watermarked text scores significantly above."
  }
]} />

## Why the mechanism decides when detection breaks

The four common ways the watermark disappears aren't bugs — they follow directly from what the algorithm actually does.

**Short passages.** Detection is a statistical test. A few sentences produce too few token choices for the signal to rise above noise. Anthropic explicitly notes: "detecting a watermark also doesn't work well on small samples, where there are fewer word choices and thus less information to go on." Practically: expect reliable detection to need hundreds of tokens, not tweets.

**Factual text.** When there's essentially one correct next token — a date, a name, a definition, a code identifier — the sampler has no acceptable alternatives to choose from, so the bias has nowhere to attach. Anthropic: "watermarking is sparser on factual passages where there are fewer choices that can be made without decreasing the accuracy of the text." A page-long Wikipedia-style summary will carry less signal per word than the same length of creative writing.

**Code.** Same reason, harder. Programming syntax is largely fixed; token flexibility lives mostly in identifiers and comments. Anthropic acknowledges "code — which in very many cases has to be exact — has generally less watermarking than some other forms of text." Treat detector scores on code as low-confidence.

**Proofreading and rewriting.** When Claude *edits* human text instead of generating it, most tokens are the user's tokens; only the edits carry the watermark. A gentle proofread is essentially invisible. On the flip side, running watermarked text through a **different, non-watermarking model** to paraphrase it heavily removes the signal because a new sampler produced the tokens.

Two things that surprise people, but shouldn't:

- **Light human editing survives.** Swapping a few words leaves most of the signal intact — the detector is aggregating a small bias over thousands of positions, not looking for specific phrases.
- **Translation is still watermarked** — because a translation is a *fresh generation* where every token is chosen by the model. It's a different watermarked passage, not a diluted copy of the original.

## What the detector actually returns

The most misread thing about watermarks in press coverage: a detector does **not** output "AI: yes / no." It outputs a **score** — essentially "how far above chance does this text's token pattern fall for our key?" — and a threshold turns that score into a decision, with two error rates that trade off against each other.

<Callout type="warning" items={[
  "A high score is Bayesian evidence, not proof. On a long, creative passage it can be strong evidence. On a short or factual one, the same score means much less.",
  "A negative result never proves the text wasn't AI-generated. Paraphrasing, mixing with human writing, or using a non-watermarking model all defeat detection while producing AI-authored text.",
  "The detector needs the secret key. Anthropic is offering the detection API to trusted partners; there is no public 'is this Claude?' tool for anyone to run on arbitrary text."
]} />

Anthropic's own summary of the detector's guarantee is calibrated exactly this way: given a long enough passage and the right key, "one can assign a probability that the text was generated by Claude." Probability. Not verdict.

The gatekeeping matters. Google's [SynthID API documentation](https://ai.google.dev/responsible/docs/safeguards/synthid) is explicit that the detector is currently limited-access; Anthropic is on the same posture — access is being extended in stages to journalists, educators, and platforms with a legitimate verification need. This is deliberate: if the detector were public, adversaries could iteratively edit text until the score dropped below threshold. Restricted access buys some robustness.

## Watermark vs C2PA vs classifier — three completely different tools

Provenance discourse mashes these three together. They're not the same and don't compete.

| Tool | Signal lives in | Requires the generator to cooperate? | Survives paraphrase? | What it tells you |
|---|---|---|---|---|
| **SynthID-Text / statistical watermark** | Token choices inside the text | Yes (turned on at sampling time) | Partial — light edits yes, heavy rewrite no | "This text was generated by a model that shares this key, with probability X." |
| **C2PA / Content Credentials** | Cryptographically signed metadata attached to the file | Yes (signer includes credential) | No — trivially stripped by copying text out | "This file was published by this signer with this history." Aimed at images/audio/video where metadata rides along. |
| **Third-party AI-text classifier** (GPTZero, Pangram, etc.) | Statistical properties of the text itself, no key | No | Partial — improves as models leave detectable style, but fights an arms race | "This text *looks like* AI-generated text, based on my model." A guess, not a signature. |

The lesson: a **watermark** answers "did *this specific provider's model* generate this?" with the provider's own detector. A **classifier** answers "does this look AI-generated in general?" with no ground truth. **C2PA** answers "who signed this file and what's its edit history?" for media where a file wrapper exists.

<PromptCard title="A safer disclosure prompt for AI-written pieces">
{`When you finish, add a footer:
"AI assistance: Draft written by Claude (Anthropic). Edited by [name]. Substantive claims verified against cited sources."

Then list any factual claim you couldn't verify from the sources I provided,
so I can check or cut it before publishing.`}
</PromptCard>

Why this matters even with watermarks live: a watermark plus a human disclosure line covers *both* automated and human verification paths. A watermark alone becomes a legal fig leaf — technically compliant, useless to a reader glancing at the page.

## The rule that forced this live: EU AI Act Article 50

The August 14 announcement wasn't primarily a product decision. **Article 50** of the [EU AI Act](https://en.wikipedia.org/wiki/Artificial_Intelligence_Act) requires providers of generative AI systems to mark their outputs "in a machine-readable format and detectable as artificially generated or manipulated." The AI Act entered into force on 1 August 2024; the [transparency obligations under Article 50 became fully enforceable on **2 August 2026**](https://en.wikipedia.org/wiki/General-Purpose_AI_Code_of_Practice). That's the deadline every frontier provider has been quietly building against.

The [General-Purpose AI Code of Practice](https://en.wikipedia.org/wiki/General-Purpose_AI_Code_of_Practice) — the voluntary compliance framework the European Commission published on 10 July 2025 — takes an important position: **no single marking technique is sufficient today**. It recommends a **multi-layered approach** combining metadata (C2PA-style content credentials on files) *and* interwoven watermarking (SynthID-style bias inside the content), together with logging and fingerprinting to identify content even after marks are stripped. That's why you'll see providers turning on all three at once.

<Callout type="warning" items={[
  "Article 50 is provider-side. It obligates the AI *provider* to mark, and downstream deployers to disclose — it does not make watermarking evidence in court, and it does not stop you from stripping the mark from your own text. Compliance is on OpenAI/Anthropic/Google/Meta, not on you as a reader.",
  "The Code of Practice is voluntary — but signatories get a presumption of compliance with the AI Act. Non-signatories can still comply through their own means, but face heavier documentation demands."
]} />

## What this changes in practice

For four different readers, four different takeaways:

<Steps items={[
  {
    title: "If you write with AI",
    body: "Assume anything Claude, Gemini, or the next model you use writes carries a watermark by default. Long creative passages are the most detectable; heavily edited or reference-heavy passages, the least. Disclose AI assistance directly rather than relying on the absence of a watermark to hide it — a watermark is easy to defeat, but the ethical/policy expectations don't relax because the detection did."
  },
  {
    title: "If you publish or run a platform",
    body: "Preserve provenance you receive (C2PA content credentials on uploaded media). Consider a policy that requires disclosure of AI-generated text, and remember the current watermark detectors give probabilities — not verdicts — and won't be reliable on short posts."
  },
  {
    title: "If you're building on the API",
    body: "Nothing in your code changes. Watermarking happens inside sampling, downstream of any prompt or tool-use decision. Do not add your own 'AI-generated' banners as a substitute for good UX disclosure. Do think about your users' rights: if you offer text that will be republished, an opt-in disclosure line is friendlier than relying on invisible marks."
  },
  {
    title: "If you're a teacher, editor, or fact-checker",
    body: "When detector access opens up, use it as one signal in a triangulation — alongside citations, source consistency, and the writer's own history. Never treat a positive score on a short passage as proof, and never treat a negative score anywhere as proof of human authorship. The right frame is Bayesian evidence, not verdict."
  }
]} />

## What could still change

Two big open questions to watch:

1. **Cross-provider detection.** Today each provider has its own key and its own detector. A watermark from Claude will not be flagged by Gemini's detector, and vice versa. The Code of Practice hints at eventual interoperability — for example, a public metadata field that says "this content was watermarked, ask *this* endpoint to verify" — but nothing production-grade exists yet.
2. **Adversarial robustness.** Academic literature on statistical watermarks (start with the [Kirchenbauer et al. 2023 paper](https://arxiv.org/abs/2301.10226) that popularised the g-value approach) already shows several attacks: cross-model paraphrasing, token-level substitution, and back-translation each degrade the signal. Providers know this. Expect the schemes to iterate — watermark schemes will get more robust, attacks will keep pace, and the informational value of a "detected" verdict on adversarially edited text will remain limited.

<Quiz questions={[
  {
    q: "What does the SynthID-Text watermark actually change inside the model?",
    options: [
      "The model's training data is fine-tuned to prefer certain words",
      "The sampling step's random source is replaced by a key-derived pseudo-random function that biases which of the already-acceptable tokens gets picked",
      "A hidden Unicode character is inserted between every few words",
      "The tokenizer is swapped for a version whose IDs form a checksum"
    ],
    answer: 1,
    explain: "The model, probabilities, and vocabulary are unchanged. What changes is the sampler's random source — a secret key plus recent tokens deterministically biases the choice among near-equivalent options, planting a statistical pattern detectable only with the same key."
  },
  {
    q: "You paste a two-sentence AI-generated tweet into a watermark detector. It returns a low score. What can you conclude?",
    options: [
      "The text was definitely human-written",
      "The text was definitely not generated by that provider's model",
      "The detector doesn't have enough tokens to make a confident call — the result is essentially uninformative",
      "The watermark was intentionally removed"
    ],
    answer: 2,
    explain: "Watermark detection is a statistical test that needs many token choices to rise above noise. On very short passages the score is near chance regardless of origin — a low score means 'we can't tell,' not 'it's human.'"
  },
  {
    q: "Which of these operations most reliably removes the watermark from a piece of Claude-generated prose?",
    options: [
      "Fixing three typos and rewording one sentence",
      "Copy-pasting the text into a new document",
      "Translating it into French with Claude",
      "Feeding it to a different, non-watermarking model and asking for a complete paraphrase"
    ],
    answer: 3,
    explain: "The watermark lives in the sampler's token choices. Copying doesn't touch the text; light edits leave most of the signal. Translating with Claude produces a *new* watermarked passage. Only a full paraphrase by a different, non-watermarking sampler replaces the tokens and destroys the signal."
  },
  {
    q: "Which EU regulation forced text-generation providers to have watermarking live by 2 August 2026?",
    options: [
      "GDPR Article 22",
      "The Digital Services Act",
      "The EU AI Act — specifically Article 50 transparency obligations for generative AI",
      "The ePrivacy Directive"
    ],
    answer: 2,
    explain: "The EU AI Act entered into force on 1 August 2024, and the Article 50 transparency obligations for generative AI — which require AI-generated content to be marked in a machine-detectable way — became fully enforceable on 2 August 2026."
  },
  {
    q: "What is the key difference between a SynthID-Text watermark and a C2PA content credential?",
    options: [
      "They are the same thing, just named differently by different companies",
      "SynthID-Text is embedded in the content itself via biased token choice; C2PA is signed metadata attached to a file wrapper that gets stripped when the text is copied out",
      "C2PA is only used for video; SynthID-Text is for images",
      "SynthID-Text requires a paid subscription; C2PA is free"
    ],
    answer: 1,
    explain: "SynthID-Text lives inside the token sequence — it travels with the words, at the cost of being partially removable by rewriting. C2PA is a cryptographically signed metadata wrapper — very strong when the file is preserved, useless once the text is copied out of it. That's why the EU Code of Practice recommends both layered, not either alone."
  }
]} />

<Flashcards cards={[
  { front: "SynthID-Text (one-sentence definition)", back: "A watermarking scheme that biases an LLM's sampler at each step using a secret key plus prior tokens, planting a statistical pattern detectable in long enough passages without changing what the model can say." },
  { front: "Why detection fails on short passages", back: "Watermark detection is a statistical test; a handful of token choices doesn't give enough evidence to rise above the noise floor. Reliable detection needs hundreds of tokens." },
  { front: "Why detection fails on code and factual text", back: "The bias only nudges *among acceptable alternatives*. When the correct next token is essentially forced (a date, a syntax keyword, a variable name), there's no choice to bias, so nothing to detect." },
  { front: "Why translation is still watermarked", back: "Translation is a fresh generation — every output token is chosen by the model, so the sampler bias plants a new watermark. The translated text is a different watermarked passage, not a stripped copy." },
  { front: "One-line rule for using detector output", back: "A watermark detector returns a probability score, not a verdict. Treat 'detected' as Bayesian evidence proportional to passage length and creativity, and 'not detected' as never proof of human authorship." },
  { front: "EU AI Act Article 50 enforcement date", back: "2 August 2026 — the day generative-AI transparency obligations (machine-detectable marking of AI-generated content) became fully enforceable for providers serving the EU market." },
  { front: "Watermark vs C2PA vs classifier", back: "Watermark = signal in the tokens (needs key). C2PA = signed metadata on a file (needs wrapper). Classifier = statistical guess without ground truth. The Code of Practice recommends layering all three." }
]} />

## Related on AILmanac

- **[Choosing a Model Provider](/docs/foundations/choosing-a-model-provider)** — where provider policies (like watermarking, data retention, safety tiers) fit into a picking decision.
- **[Privacy](/docs/foundations/privacy)** — how outputs are handled beyond just watermarks: retention, training use, and enterprise controls.
- **[Hallucinations](/docs/foundations/hallucinations)** — watermarks don't tell you whether AI text is *true*, only whether it was generated. Verification is a separate job.
- **[Generative Media: Image, Audio, Video](/docs/models/generative-media-image-audio-video)** — the same provenance question, for the modalities where C2PA is doing most of the work.

## Sources & further reading

- [How Claude's text watermarking works — Anthropic (14 Aug 2026)](https://www.anthropic.com/news/claude-text-watermark) — the primary FAQ this page synthesises.
- [SynthID overview — Google DeepMind](https://deepmind.google/technologies/synthid/) — the family of watermarking tools SynthID-Text is part of.
- [SynthID Text safeguards docs — Google AI](https://ai.google.dev/responsible/docs/safeguards/synthid) — how SynthID-Text is exposed as an API, with detector access notes.
- [Kirchenbauer et al., "A Watermark for Large Language Models" (2023)](https://arxiv.org/abs/2301.10226) — the g-value / red-list mechanism the current schemes are built on.
- [EU AI Act — Wikipedia summary of Article 50 and the 2 August 2026 enforcement date](https://en.wikipedia.org/wiki/Artificial_Intelligence_Act) — the regulation that forced watermarking live across providers.
- [General-Purpose AI Code of Practice — Wikipedia](https://en.wikipedia.org/wiki/General-Purpose_AI_Code_of_Practice) — the voluntary framework recommending layered watermark + metadata + logging.
- [Coalition for Content Provenance and Authenticity (C2PA)](https://c2pa.org/) — the media-provenance standard that complements text watermarks.
