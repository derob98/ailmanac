---
sidebar_position: 10.8
title: "AI Explainer Videos: The Code-Driven Pipeline"
description: "Opus 5.5 explainer videos hit the Hacker News front page — and almost nobody explains what is actually happening: the model writes a deterministic animation program, not a video. The mechanism, the three render engines and their licences, the two traps that break agent-written animation code, the pacing formula that makes the difference between watchable and unwatchable, and what the bill really looks like."
keywords: [AI explainer video, Remotion, Motion Canvas, Manim, programmatic video, Opus 5.5 video, code-driven animation, deterministic rendering, AI video pipeline, Remotion agent skills, whisper captions]
---

<LevelBadge level="intermediate" />

A submission titled *"Opus 5.5 is good at explainer videos"* sat near the top of
Hacker News with **319 points and 175 comments** — and reading the thread, the
most interesting thing is how many people got the mechanism wrong. They assumed
a video model. It isn't one.

What is happening is stranger and far more useful: a coding model writes a
**deterministic animation program**, and a separate renderer turns that program
into frames. The model never sees a pixel of the output. Which means the whole
thing behaves like software — diffable, re-renderable, parameterisable, and
debuggable — instead of like a slot machine you re-prompt until it looks right.

This page is the practical guide that thread was missing: how the pipeline
works, which of the three render engines to pick (and which one the models are
measurably bad at), the two failure modes that silently break agent-written
animation code, the pacing arithmetic that separates a watchable explainer from
an unreadable one, and where the money actually goes.

<Callout type="objectives" items={[
  "Explain why a coding model beats a video model at explainers — and what it gives up",
  "Name the reason Math.random() is banned in a frame-rendered animation, and what replaces it",
  "Pick between Remotion, Motion Canvas and Manim on licence and on how well models write them",
  "Spot the two traps in agent-written animation code: the Manim fork split and the stale Remotion API",
  "Compute a minimum dwell time per slide instead of guessing, so viewers can actually read",
  "Predict which line item dominates the bill (it is usually not the LLM)"
]} />

<VerifyNote lastVerified="2026-09-25" source="https://news.ycombinator.com/item?id=49836374">
Volatile here: the Hacker News score, the community-reported per-video costs, the
claim that one model is better than another at a specific library, the Remotion
version and its package layout (checked at `remotion@4.0.529`), and the star
counts. Re-check any of those before you build a budget or a template on them.
Durable: the mechanism (a program rendered frame by frame), the determinism
constraint, the licence split between the three engines, and the pacing
arithmetic — none of that changes with a model release.
</VerifyNote>

## The mechanism: the model writes a program, not a video

Two completely different things get called "AI video".

**Diffusion video models** (the family covered in
[generative media](/docs/models/generative-media-image-audio-video)) take a
prompt and emit pixels directly. Excellent for b-roll, a drone shot, a texture,
a mood. Bad at anything that must be *exactly right*: legible text, a correct
architecture diagram, your actual product UI, a number that has to say 40%.

**Code-driven animation** goes the other way. A coding model writes source code
for an animation — React components, a TypeScript scene graph, or Python — and a
renderer executes it. Text is text, so it is sharp and spelled correctly. A
diagram is a layout, so it is accurate. Your logo is your logo file.

That split explains the HN thread's most common workflow report: the model
"creates an HTML page and uses raw JS timed animations", then the frames get
captured and assembled with ffmpeg. It also explains what the approach gives up
— it cannot invent photographic imagery, so most real pipelines call an image
model or a video model for b-roll and composite it in.

The consequence worth internalising: **your explainer becomes a repository.**
Change the price on slide 4, re-render, done. Localise the voiceover, re-render
twelve languages from the same scenes. That is not a property any prompt-to-video
model gives you.

## Why determinism is the whole trick

Here is the detail that quietly decides whether your pipeline produces clean
video or a flickering mess, and almost nobody writing about this mentions it.

A renderer like Remotion does **not** screen-record your animation. It opens the
composition in headless Chrome, sets the clock to frame 0, screenshots, sets the
clock to frame 1, screenshots, and so on — and it does this **across multiple
threads, opening the page multiple times**. Frames are produced out of order, in
parallel, in separate browser contexts.

Everything follows from that:

- **Animation must be a pure function of the frame number.** In Remotion that is
  literally the API: `useCurrentFrame()` gives you the frame, `interpolate()`
  maps it to a value. No timers, no `requestAnimationFrame`, no CSS transitions
  driven by wall-clock time. A CSS transition renders whatever it happened to
  be mid-flight when the screenshot fired.
- **`Math.random()` is broken by construction.** It returns a different value in
  each thread and each page load, so a "random" particle jitters to a new
  position every frame. Remotion ships `random(seed)` — a seeded mulberry32
  PRNG — precisely for this: `random(1)` always returns
  `0.07301638228818774`. Same for `spring()`, which is a cached pure calculation
  keyed on `(frame, fps, config)`.
- **Dropped frames stop being possible.** A screen recording of a heavy
  animation drops frames when the CPU can't keep up. Frame-by-frame rendering
  just takes longer. This is why a 5-minute render on a laptop can produce a
  video that plays perfectly smoothly.

<Callout type="warning" items={[
  "If you only remember one rule for prompting an agent to write animation code: forbid wall-clock time. No setTimeout, no CSS transition, no Date.now(), no Math.random(). Every visual value must be derived from the current frame number. Models will reach for CSS transitions because that is what the web corpus is full of."
]} />

## The three engines

All three are real, actively maintained, and produce broadcast-quality output.
They differ on language, on licence, and — importantly — on how reliably a model
writes them.

| | Remotion | Motion Canvas | Manim |
|---|---|---|---|
| Language | React / TypeScript | TypeScript (generators) | Python |
| Repo | [remotion-dev/remotion](https://github.com/remotion-dev/remotion) | [motion-canvas/motion-canvas](https://github.com/motion-canvas/motion-canvas) (~19.2k stars) | [ManimCommunity/manim](https://github.com/ManimCommunity/manim) (~41k) · [3b1b/manim](https://github.com/3b1b/manim) (~94k) |
| Licence | Source-available + **Company Licence** | MIT | MIT |
| Sweet spot | Product/launch video, UI, text-heavy explainer | Timeline-precise motion graphics | Mathematical and algorithmic visualisation |
| Model-written quality | Strong — and it ships official agent skills | Decent | **Weakest of the three** (see below) |

Two things on that table deserve more than a row.

### Remotion's licence is the gotcha, not the code

Remotion is not MIT. It is source-available with a two-tier commercial licence,
and the threshold is small enough to catch most teams: **free for individuals,
non-profits, evaluation, and for-profit companies with up to 3 employees; a paid
Company Licence for for-profit organisations with more than three.**

The second split matters more if you are building a product rather than a video:

- **Remotion for Creators** — seat-based, intended for low-volume rendering
  *within and for your own company*, explicitly **not** for serving
  personalised videos to your end users.
- **Remotion for Automators** — render-based, which is the tier you land in the
  moment an agent renders videos for customers. From version 5.0 it carries
  **mandatory telemetry reporting with a license key.**

So "my agent generates a video per signup" is not a Creators use case. Price it
as Automators before you ship it. If that is a dealbreaker, Motion Canvas is MIT
and covers a lot of the same ground.

### The Manim trap: two forks, one import name

This one burns people who have never heard of it, and it also explains an
observation from the HN thread — that strong coding models write *worse* Manim
than you would expect, with community reports that Gemini currently handles it
better than Opus 5.5.

There are two Manims:

- **3b1b/manim** — Grant Sanderson's original, the one from the famous videos,
  ~94k stars, installed as `manimgl`.
- **ManimCommunity/manim** — the community fork, ~41k stars, the one that is
  actually documented and maintained for general use, installed as `manim`.

They are **API-incompatible** and both are imported as `from manim import *`.
Every tutorial, StackOverflow answer and blog post in the training corpus is one
or the other, unlabelled. So the model writes plausible code that mixes both
dialects, and Python raises on a method that exists in the other fork. It is not
a reasoning failure; it is a poisoned-namespace failure.

The fix is boring and it works: pin the fork in your prompt and in your
environment, and give the agent the version's real API surface instead of
letting it recall one.

<PromptCard title="Pin the Manim dialect before the agent writes a line">
{`Use ManimCommunity Manim (the "manim" package on PyPI), NOT 3b1b/manimgl.
Before writing any scene, run:
  python -c "import manim; print(manim.__version__)"
and confirm the version. If a class or method you want is not in that
version's API, do not use it — check with:
  python -c "import manim; print([n for n in dir(manim) if 'Text' in n])"
Never mix manimgl APIs (e.g. ShowCreation) with community APIs (Create).`}
</PromptCard>

## The stale-API trap (Remotion edition)

The same poisoned-corpus problem hits Remotion for a different reason: the
library moved faster than the training data.

In current Remotion, media components live in **`@remotion/media`**, and the
trimming props are `trimBefore` / `trimAfter`. Preview and render can even use
different components deliberately — `Html5Audio` from `remotion` while
previewing, `Audio` from `@remotion/media` when rendering, switched on
`useRemotionEnvironment()`. A model trained mostly on older material writes
`import {Audio} from 'remotion'` with `startFrom`, which is the *old* shape.

That code may still run. It may also silently do nothing useful, which is worse.

This is exactly why Remotion publishes **official Agent Skills** — a set of
twelve skills that hand an agent the current best practices instead of hoping it
remembers them. They are designed for Claude Code, Codex, Cursor and friends,
and they follow the same open format covered in
[SKILL.md as an open standard](/docs/models/skill-md-open-standard).

<PromptCard title="Install Remotion's official agent skills">
{`npx skills add remotion-dev/skills`}
</PromptCard>

The skills are granular, which is the point — you invoke the one that matches
the phase of work: `/remotion-create` to scaffold, `/remotion-markup` for
animation and typography, `/remotion-studio` to preview, `/remotion-render` to
render, `/remotion-captions` for subtitles, plus `remotion-best-practices` as
the umbrella when you are not sure. Vendor-maintained skills are the cheapest
available fix for a model's stale knowledge of a fast-moving library: one
install, and the agent stops guessing.

## The pipeline that actually works

The single biggest quality lever is **not** a better prompt for the whole video.
It is splitting the job so the model does judgement work and the renderer does
determinism work — with a human-readable artefact in between.

<Steps items={[
  {
    title: "Generate a storyboard as data, not as code",
    body: "Ask for a JSON scene list first: per scene, a title, the on-screen text, the visual intent, and a duration in seconds. This is the cheap, reviewable step. You can read twelve scenes in thirty seconds and fix the narrative before a single component exists — and fixing the story is the edit that most improves the video. Code generated from a reviewed storyboard is also far more consistent than code generated from a paragraph of vibes."
  },
  {
    title: "Fix the pacing in the storyboard, before any rendering",
    body: "Do the reading-speed arithmetic below on each scene's text and clamp the durations. Doing this at the data layer costs nothing; doing it after the components exist means re-timing every animation that depends on the scene length."
  },
  {
    title: "Write the voiceover script per scene, then synthesise it",
    body: "Script per scene, not per video, so audio boundaries line up with visual boundaries. Synthesise each scene to its own audio file with a TTS API. Then measure the real duration of each clip — the synthesised audio is the ground truth for scene length, not your estimate."
  },
  {
    title: "Re-derive scene durations from the real audio",
    body: "This is the step everyone skips and it is why so many AI explainers have visuals racing ahead of the narration. Read each audio file's actual duration, convert to frames, and feed those durations back into the composition as props. Never hardcode a frame count you guessed."
  },
  {
    title: "Build the components from the storyboard, frame-derived only",
    body: "Now let the agent write the animation code, scene by scene, against the frozen storyboard. Enforce the determinism rule: every value from useCurrentFrame() and interpolate(), randomness from a seeded helper, no CSS transitions. Preview in the studio — a real preview catches layout errors that no amount of code reading does."
  },
  {
    title: "Add captions from the audio you already have",
    body: "Do not ask the model to guess word timings. Transcribe your own voiceover locally and use the token-level timestamps. Remotion ships install-whisper-cpp for exactly this: installWhisperCpp(), downloadWhisperModel(), then transcribe() with token-level timestamps on, and toCaptions() to post-process. Captions are also the cheapest accessibility win available and they are what makes the video watchable muted."
  },
  {
    title: "Render, then measure the render",
    body: "Render from the CLI so it is reproducible in CI. Then tune concurrency with a real measurement instead of a guess — Remotion has a benchmark command that runs the same render repeatedly across several concurrency values and compares them."
  }
]} />

<PromptCard title="Render from the CLI, then find your real concurrency">
{`# deterministic render, reproducible in CI
npx remotion render src/index.ts MyExplainer out/video.mp4

# stop guessing --concurrency: measure it
npx remotion benchmark src/index.ts MyExplainer --concurrencies=2,4,8 --runs=3`}
</PromptCard>

## Pacing: the reason most AI explainers are unwatchable

The most consistent complaint in the HN thread was not that the videos looked
bad. It was that they **moved too fast to read**. Slides appear, text animates
in, and it is gone before a human finishes the sentence.

This is a bug with a formula, not a matter of taste. Adult reading speed for
unfamiliar screen text sits around 200–250 words per minute, and a viewer also
needs time to *find* the new text before starting to read it. So:

```text
# minimum dwell time for a text block, in frames
words        = number of words in the block
wpm          = 200          # conservative for unfamiliar on-screen text
settle       = 0.4          # seconds to notice and locate the new text
exit         = 0.3          # seconds of breathing room before it leaves

dwell_seconds = settle + (words / wpm) * 60 + exit
dwell_frames  = ceil(dwell_seconds * fps)

# worked example: a 14-word bullet at 30 fps
# 0.4 + (14 / 200) * 60 + 0.3 = 4.9 s  ->  147 frames
```

Two notes that make this actually useful:

- **Animate in fast, hold long.** The entrance transition should be 200–400 ms;
  the *hold* is where the dwell budget goes. Models tend to do the opposite —
  a long elegant fade, then a short hold.
- **If narration exists, the dwell floor is the narration length.** Text must
  stay up at least as long as the sentence describing it. Which is why deriving
  scene duration from the synthesised audio (step 4 above) fixes pacing almost
  for free.

Clamp every scene to `max(dwell_frames, narration_frames)` and a large fraction
of the "AI slop video" feeling disappears, with no change to the visuals at all.

## What it costs, and which line item dominates

Community reports from the HN thread put a complete generated explainer at
roughly **$3–$4 in API spend**, with the notable detail that the cost was
dominated by **image-model calls, not by the language model**. One commenter
reported generating a 5-minute video on an M4 MacBook Air in about 5 minutes of
wall-clock time using an orchestrated pipeline.

Treat the absolute numbers as perishable, but the *shape* is structural and
worth planning around:

- **The LLM is cheap here.** A storyboard plus a few hundred lines of animation
  code is a small number of tokens. Even at flagship pricing it is cents. See
  [what AI costs across providers](/docs/models/what-ai-costs-across-providers).
- **Per-asset generative calls dominate.** Every generated image or b-roll clip
  is a separate paid call, and they multiply by scene count. This is the line
  item to cap.
- **Compute is yours.** Rendering runs on your machine or your CI, so it is time
  and electricity rather than API spend — until you move it to a serverless
  renderer, at which point it becomes both.
- **Re-renders are nearly free**, which is the whole structural advantage. Fixing
  a typo on slide 4 costs one render, not one regeneration. Localising into
  twelve languages costs twelve TTS runs and twelve renders, not twelve videos.

The practical budgeting move: generate images once, commit them as assets, and
keep re-renders asset-free. A pipeline that re-generates imagery on every render
is the one that produces a surprising bill.

## The sameness problem

The sharpest criticism in the thread is the one worth taking seriously: within
weeks of these tools spreading, the outputs became recognisable. Same gradient
backgrounds, same spring-in cards, same three-word bullets, same brisk
voice. Several commenters also flagged repetitive copy and, in one case, a video
confidently about the wrong subject entirely — a Twilight Imperium reference
site described as an AI data startup.

If you use this seriously, the levers that measurably de-generify output:

- **Supply the design system, don't let it be invented.** Pass your real
  palette, type scale, spacing scale and logo as constants the components import.
  Underspecified prompts are exactly where a model's house style leaks in.
- **Write the copy yourself, or edit it hard.** The visuals are the part models
  are good at. The narrative is the part that reads as generated.
- **Use your own screen recordings as the hero asset.** Real product footage
  composited into a generated scene is the single strongest anti-sameness move,
  and it also fixes the hallucination risk — the video cannot misdescribe your
  product if the product is on screen.
- **Verify the claims like documentation.** Anything factual in an explainer is a
  factual claim being published in your name, with none of a text page's easy
  correction path. Check it before you render, not after it is on YouTube.
- **Break one convention on purpose.** One deliberate choice that the default
  template would not make — an odd aspect ratio, silence where music is
  expected, a hand-drawn asset — is usually enough to stop the output reading as
  machine default.

<Flashcards title="Code-driven video: the vocabulary" cards={[
  { front: "Why can't you use Math.random() in a frame-rendered animation?", back: "Frames are rendered in parallel across multiple threads, each loading the page separately. Math.random() returns a different value per thread, so the 'random' value changes every frame and the element flickers. Use a seeded PRNG — Remotion's random(seed) is mulberry32-based, so random(1) always returns 0.07301638228818774." },
  { front: "Why are CSS transitions banned in this pipeline?", back: "A CSS transition is driven by wall-clock time. The renderer screenshots a fixed frame, so it captures whatever mid-flight value happened to exist at screenshot time. Every animated value must be a pure function of useCurrentFrame()." },
  { front: "Remotion's licence threshold", back: "Free for individuals, non-profits, evaluation, and for-profit companies with up to 3 employees. A paid Company Licence is required above three. Serving personalised videos to end users is the render-based 'Automators' tier, not the seat-based 'Creators' tier — and from v5.0 Automators carries mandatory telemetry with a license key." },
  { front: "The Manim fork trap", back: "3b1b/manim (installed as manimgl, ~94k stars) and ManimCommunity/manim (installed as manim, ~41k) are API-incompatible but both import as 'from manim import *'. Training data mixes them unlabelled, so models emit code blending both dialects. Pin the fork in the prompt and verify the installed version's API." },
  { front: "Where the money goes in a code-driven explainer", back: "Community reports land around $3–$4 per video, dominated by image-model calls rather than the LLM. The LLM writes a storyboard plus a few hundred lines of code — cents. Render compute is yours. Re-renders are nearly free, which is the structural advantage over prompt-to-video." },
  { front: "Minimum dwell time for a text block", back: "settle (~0.4 s) + words / 200 wpm * 60 + exit (~0.3 s), converted to frames. A 14-word bullet at 30 fps needs ~4.9 s, i.e. ~147 frames. If there is narration, the floor is the narration length — so derive scene duration from the synthesised audio." },
  { front: "Why generate a storyboard as JSON before any code?", back: "It makes the narrative reviewable and cheap to fix, it is where pacing should be clamped, and code generated from a frozen storyboard is far more consistent than code generated from a prose brief. Fixing the story is the highest-value edit." },
  { front: "Why do vendor-maintained agent skills matter for a library like Remotion?", back: "The library moved faster than the training corpus — media components now live in @remotion/media with trimBefore/trimAfter, while models recall the older remotion imports and startFrom. Remotion publishes twelve official Agent Skills (npx skills add remotion-dev/skills) that hand the agent current APIs instead of remembered ones." }
]} />

## What this is good at, and what it isn't

Worth being honest, because the sceptics in that thread had a point: much of
what gets produced is an animated slide deck with a voiceover. That is a
perfectly good artefact for a changelog, a launch, a release note or an
onboarding walkthrough — and a poor substitute for actual teaching, which needs
a worked example, a wrong turn, and a reason to care.

Reach for the code-driven pipeline when:

- The content must be **exactly right** — real UI, real numbers, real diagrams.
- You will need to **change it later**, or ship it in several languages.
- The same template will produce **many videos** from different data.

Reach for something else when the goal is photographic footage (use a video
model), or when the value is in a human explaining a hard idea badly and then
well — which is still the thing no pipeline produces.

<Quiz questions={[
  {
    q: "A model writes an explainer animation using CSS transitions and setTimeout. The preview looks perfect. What happens on render?",
    options: [
      "Identical output — the renderer records the preview",
      "Frames capture arbitrary mid-transition values, so the animation looks broken or frozen",
      "The render fails with an error about unsupported CSS",
      "It works but the file is larger"
    ],
    answer: 1,
    explain: "The renderer sets a frame number and screenshots, in parallel across threads — it does not play the animation in real time. Anything driven by wall-clock time captures whatever value happened to exist at screenshot time. Every animated value must derive from useCurrentFrame()."
  },
  {
    q: "Your agent's Manim code keeps failing on methods that 'should exist'. Most likely cause?",
    options: [
      "The model is hallucinating APIs at random",
      "Manim requires a GPU that isn't present",
      "It is mixing the 3b1b/manimgl and ManimCommunity dialects, which share the import name but are API-incompatible",
      "The Python version is too new"
    ],
    answer: 2,
    explain: "Two maintained forks, both imported as 'from manim import *', both heavily represented in training data without labels. The model blends dialects. Pin the fork explicitly and make the agent verify the installed version's API surface."
  },
  {
    q: "A 3-person startup builds a product that renders a personalised Remotion video for every new signup. Licensing position?",
    options: [
      "Free — under the 3-employee threshold, no further questions",
      "MIT, so nothing to consider",
      "Needs the render-based 'Automators' tier: serving personalised videos to end users is excluded from the seat-based Creators use case",
      "Only needs a licence once revenue passes a threshold"
    ],
    answer: 2,
    explain: "The employee count governs the free tier, but the Creators tier is explicitly for low-volume rendering within and for your own company — not for serving personalised videos to end users. That is the render-based Automators tier, which from v5.0 also carries mandatory telemetry with a license key."
  },
  {
    q: "Reviewers say your generated explainer is 'too fast'. Cheapest structural fix?",
    options: [
      "Slow every entrance animation down",
      "Clamp each scene to max(reading-time dwell, narration length) and derive durations from the synthesised audio",
      "Re-prompt the model to 'make it slower'",
      "Lower the frame rate"
    ],
    answer: 1,
    explain: "Pacing is arithmetic: settle + words/200*60 + exit, and if narration exists the floor is the narration length. Clamping in the storyboard costs nothing; re-prompting is non-deterministic, and slowing entrances eats hold time without adding reading time."
  },
  {
    q: "You expect the LLM to dominate the cost of a generated explainer. What do reports actually show?",
    options: [
      "The LLM is the largest line item by far",
      "Render compute on a serverless platform dominates",
      "Image-model calls dominate; the LLM writes a storyboard plus a few hundred lines of code and costs cents",
      "Text-to-speech is always the largest cost"
    ],
    answer: 2,
    explain: "Community reports land around $3–$4 per video with per-asset generative calls — images especially — as the dominant item, multiplying by scene count. Generate assets once, commit them, and keep re-renders asset-free."
  }
]} />

## Sources & further reading

- [*Opus 5.5 is good at explainer videos* — Hacker News discussion](https://news.ycombinator.com/item?id=49836374) — the thread this page is built around: 319 points, 175 comments, the reported workflows (HTML + JS timed animations captured with ffmpeg), the ~$3–$4 costs, the Manim weakness, the pacing complaints and the sameness criticism. Community reports, not vendor claims — treated as such throughout.
- [Remotion](https://github.com/remotion-dev/remotion) — the React-based renderer. See [`useCurrentFrame()` and `interpolate()`](https://www.remotion.dev/docs/troubleshooting/css-animations) for the determinism model, [`random()`](https://www.remotion.dev/docs/random) for why `Math.random()` is banned and what the seeded PRNG returns, and the [licence pages](https://www.remotion.dev/docs/licensing) for the Company Licence, the Creators/Automators split and the v5.0 telemetry requirement.
- [remotion-dev/skills](https://github.com/remotion-dev/skills) — Remotion's twelve official Agent Skills for Claude Code, Codex, Cursor and others, installable with `npx skills add remotion-dev/skills`. The clearest current example of a vendor shipping agent-facing docs to fix stale model knowledge.
- [`@remotion/install-whisper-cpp`](https://www.remotion.dev/docs/install-whisper-cpp) — local transcription for captions: `installWhisperCpp()`, `downloadWhisperModel()`, `transcribe()` with token-level timestamps, and `toCaptions()` for post-processing.
- [`npx remotion benchmark`](https://www.remotion.dev/docs/cli/benchmark) — measures render time across concurrency values instead of guessing a flag.
- [Motion Canvas](https://github.com/motion-canvas/motion-canvas) — MIT, TypeScript, generator-based timelines. The licence-free alternative when Remotion's Company Licence is a blocker.
- [ManimCommunity/manim](https://github.com/ManimCommunity/manim) and [3b1b/manim](https://github.com/3b1b/manim) — both MIT, both imported as `manim`, mutually incompatible. The root cause of the Manim failure mode above.
- AILmanac companions: [Generative media: image, audio, video](/docs/models/generative-media-image-audio-video) · [SKILL.md as an open standard](/docs/models/skill-md-open-standard) · [What AI costs across providers](/docs/models/what-ai-costs-across-providers) · [Claude Opus 5.5 field guide](/docs/models/opus-5-5-field-guide)

## Next

- [Generative media: image, audio and video](/docs/models/generative-media-image-audio-video) — the other branch: diffusion models for the b-roll this pipeline cannot draw
- [SKILL.md as an open standard](/docs/models/skill-md-open-standard) — why a vendor shipping agent skills is the general fix for a model's stale library knowledge
- [Claude Opus 5.5: the field guide](/docs/models/opus-5-5-field-guide) — the model at the centre of this wave, priced and benchmarked
