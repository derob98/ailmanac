---
sidebar_position: 14
title: "Safety, Refusals & Fallbacks"
description: "Handle refusals gracefully and distinguish them from classifier blocks."
---

<LevelBadge level="intermediate" />

In production, your code must handle the case where Claude **won't** (or can't) answer as expected. Done well, this is invisible to users; done badly, it's a crash or a confusing reply.

<Callout type="objectives" items={[
  "Tell a model refusal apart from a classifier/safety block — and why it matters",
  "Handle a refusal as a normal outcome instead of crashing or showing an empty reply",
  "Cut the refusals you didn't want, without trying to jailbreak the ones you did",
  "Pick a fallback: clarifying question, safe alternative, or human handoff",
]} />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/api">
Stop reasons and safety response shapes can change — confirm in the official API reference.
</VerifyNote>

## Two different things

- **A model refusal** — Claude declines a request (e.g. it judges it harmful). The response signals this (commonly via a refusal `stop_reason`/content). Treat it as a normal outcome, not an error.
- **A classifier/safety block** — a separate safety layer may block content. This can look different from a model refusal.

Knowing which you got lets you respond appropriately rather than retrying blindly.

## Handle it gracefully

```python
resp = client.messages.create(...)
if getattr(resp, "stop_reason", None) == "refusal":
    # Don't show a raw/empty result. Offer a safe fallback or a clarifying ask.
    show_user("I can't help with that as asked. Here's what I can do instead…")
else:
    render(resp)
```

## Reduce unwanted refusals

- **Add legitimate context.** A request can pattern-match to something sensitive when intent is benign; stating the real, legitimate purpose helps.
- **Be specific.** Vague or edgy phrasing invites caution.
- **Don't fight it.** If a request is genuinely disallowed, refusal is correct — design a graceful path, don't try to jailbreak.

<PromptCard title="Add the legitimate context up front">{`I am a security engineer at [company] reviewing our own authentication code
before a release. Below is a function we wrote and own.

Identify weaknesses in it and explain how an attacker would exploit each one,
so we can fix them before we ship.

[code]`}</PromptCard>

## Fallback patterns

- A **clarifying question** instead of a dead end.
- A **safe alternative** ("I can summarize the public info instead").
- For pipelines, **route to a human** when confidence/eligibility is low.

<Steps items={[
  {title: "Detect, don't assume", body: "Inspect the response for a refusal signal before you render it. Never pipe an unchecked response straight into your UI — that is how users get a blank box or a raw object."},
  {title: "Classify it", body: "Model refusal or classifier/safety block? A refusal is a judgment about the request; a block is a separate safety layer. Retrying identical input helps with neither, but they point at different fixes."},
  {title: "Degrade gracefully", body: "Swap the dead end for a clarifying question or a safe alternative, in your own product's voice. The user should get a next step, not an error."},
  {title: "Escalate when the stakes justify it", body: "In a pipeline, route low-confidence or ineligible cases to a human queue instead of guessing or silently dropping them."},
]} />

<Quiz title="Check yourself" questions={[
  {q: "Your code gets a refusal back from Claude. What's the right way to treat it?", options: ["As an exception — catch it and retry the same request", "As a normal outcome — detect it and show a fallback", "As a bug in your prompt to be jailbroken around"], answer: 1, explain: "A refusal is a normal response, not an error. Detect it, then offer a clarifying question or a safe alternative. Retrying identical input just spends tokens."},
  {q: "A benign request keeps getting refused. What's the first thing to try?", options: ["Rephrase it to hide what you're asking for", "State the real, legitimate purpose and be more specific", "Switch to a smaller model"], answer: 1, explain: "Requests get refused when they pattern-match something sensitive. Adding legitimate context and being specific is the fix. Hiding your intent is jailbreaking — the wrong move, and it makes the output less useful too."},
  {q: "Why does it matter whether you got a model refusal or a classifier/safety block?", options: ["It doesn't — handle them identically", "They can look different in the response and point to different fixes", "Only one of them is billable"], answer: 1, explain: "They're different mechanisms: a model refusal is Claude declining, while a separate safety layer can block content. Knowing which you got lets you respond appropriately instead of retrying blindly."},
]} />

<Callout type="takeaways" items={[
  "A refusal is an outcome, not an error — detect it before rendering, always.",
  "Model refusal and classifier block are different things; identify which you got.",
  "Unwanted refusals usually shrink when you add legitimate context and get specific.",
  "If a request is genuinely disallowed, refusal is correct — build a graceful path, don't jailbreak.",
  "Fallbacks in order: clarifying question, safe alternative, human handoff.",
]} />

## Next

- [Errors, Rate Limits & Reliability](/docs/api/errors-and-rate-limits)
- [Responsible Use, Ethics & Verification](/docs/security/responsible-use)
- [Troubleshooting: Why Did Claude Do That?](/docs/contribute/troubleshooting)
