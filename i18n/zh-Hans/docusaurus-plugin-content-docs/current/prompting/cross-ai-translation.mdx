---
sidebar_position: 6
title: "跨 AI 提示词迁移"
description: "同一个概念在 Claude、GPT 和 Gemini 上的表现——以及哪些能够通用。"
---

<LevelBadge level="intermediate" />

<Callout type="objectives" items={["了解哪些提示词基本功可以原封不动地迁移到每一个模型", "识别切换模型或厂商时会发生变化的少数表层细节", "运行一套可移植的工作流，把提示词迁移到新模型而无需重写", "避免对推理模型重复施加'一步一步思考'的陷阱"]} />

好消息：**提示词的基本功可以跨越每一个模型通用。** 如果你能把 Claude 提示得很好，你也能把 ChatGPT、Gemini 或 Llama 提示得很好。只有少数表层细节有差异——这就是那块罗塞塔石碑。

## 处处通用的部分

以下这些在所有模型上都适用——它们关乎的是*沟通*，而不是某个厂商：

- **给出上下文**：目标、受众、格式、语气（[提示词基础](/docs/prompting/basics)）。
- **示例**（[少样本](/docs/prompting/few-shot)）。
- 在困难任务上**要求推理**。
- **约束输出**（长度、格式、"只要 JSON"）。
- 用平实的语言**迭代**。
- **角色 / 系统提示词**——每个主流模型都有这个概念（[角色](/docs/foundations/roles)）。

学会这些一次，你就具备了可移植能力。

## 有差异的部分（以及该如何调整）

| 方面 | 切换时需要注意的事项 |
|---|---|
| **结构提示** | Claude 喜欢 [XML 标签](/docs/prompting/xml-tags)；其他模型也接受，但可能更偏好 Markdown 标题或 JSON。 |
| **语气强度** | 较老的模型能容忍强硬/全大写的提示词；较新的字面遵循型模型更偏好平和、具体的指令。要针对每个模型重新调校。 |
| **推理** | "一步一步思考"对某些模型有帮助；而专门的推理模型会在内部完成这一步——不要重复叠加。 |
| **预填充** | 把话塞进助手嘴里是一种 Claude 风格的杠杆；其他地方的支持程度各不相同。 |
| **系统提示词的分量** | 所有模型都有系统提示词，但被遵循的力度各不相同——请做测试。 |
| **工具/函数调用** | 概念相同，但每家厂商的请求结构不同。 |

<Flashcards title="六个表层差异" cards={[{front: "结构提示", back: "Claude 偏爱 XML 标签；其他模型可能更偏好 Markdown 标题或 JSON。结构本身可以迁移——分隔符风格则未必。"}, {front: "语气强度", back: "较老的模型能容忍强硬、全大写的提示词。较新的字面遵循型模型更偏好平和、具体的指令。切换时要重新调校。"}, {front: "推理", back: "'一步一步思考'对某些模型有帮助；专门的推理模型会在内部完成这一步——不要重复叠加，否则会浪费 token 并可能损害质量。"}, {front: "预填充", back: "先起个助手回复的头来引导格式，是一种 Claude 风格的杠杆。其他地方的支持程度各不相同——依赖它之前先做测试。"}, {front: "系统提示词的分量", back: "每个主流模型都有系统提示词，但被遵循的力度各不相同。不要假设同一条指令在各处都同样有约束力。"}, {front: "工具 / 函数调用", back: "概念是通用的；请求结构则因厂商而异。你的工具逻辑可以迁移；包裹它的 JSON 外壳则不能。"}]} />

## 一套可移植的工作流

<Steps items={[
  {title: "用通用结构撰写", body: "用上下文 + 格式 + 示例来起草提示词——这些是能迁移到任何模型的部分。此时先不要写死厂商专属的语法。"},
  {title: "在目标模型上测试", body: "用几个真实输入跑一遍你实际要上线的那个模型（/docs/foundations/evals）。评判输出，而不是你对模型的直觉。"},
  {title: "只调整表层细节", body: "如果哪里不对，一次只改一个表层杠杆——分隔符风格、语气强度、是否要求推理——而不是整个提示词。"},
  {title: "把提示词纳入版本控制", body: "把提示词以文件形式存储，这样切换模型就是一个小的、可评审的 diff，而不是一次重写。"}
]} />

因为只有*表层*发生变化，一个结构良好的提示词通常只需一行微调就能移植。这里有一个用通用方式撰写的例子——上下文、明确的格式、清晰的任务，没有需要剥离的厂商专属语法：

<PromptCard title="一个可移植、厂商中立的提示词">{`You are a technical editor. Rewrite the text below to be clearer and more concise.

Constraints:
- Keep every fact unchanged.
- Target a general professional audience.
- Return only the rewritten text, no preamble.

Text:
"""
[paste the draft here]
"""`}</PromptCard>

那个提示词可以原样运行在 Claude、GPT、Gemini 或某个开源模型上。如果某个模型忽略了"只返回重写后的文本"，那就是你唯一要做的表层微调——收紧约束，或者在 Claude 上[预填充](/docs/prompting/prompting-claude)回复。

:::tip 不要从零重写
迁移到新模型很少需要一个新提示词——通常只是一个语气/格式上的微调。你的结构和示例可以延续下来。
:::

<Quiz title="自我检测" questions={[{q: "以下哪一项无法跨模型迁移，切换时需要重新调校？", options: ["给出上下文：目标、受众、格式、语气", "提供少样本示例", "精确的语气强度——强硬措辞还是平和措辞", "约束输出的长度和格式"], answer: 2, explain: "上下文、示例和输出约束都可以迁移。语气强度是一个表层细节：较老的模型能容忍强硬的提示词，较新的字面遵循型模型更偏好平和、具体的提示词。"}, {q: "你把一个提示词迁移到专门的推理模型。你应该如何处理你那句'一步一步思考'的指令？", options: ["重复两遍以求保险", "删掉它——模型会在内部推理，所以不要重复叠加", "把它改成全大写以示强调", "把它包在 XML 标签里"], answer: 1, explain: "专门的推理模型会在内部完成逐步推理。在此之上再加一句明确的'一步一步思考'是多余的——不要重复叠加。"}, {q: "把提示词切换到新模型的推荐做法是什么？", options: ["为那个厂商从零重写", "保留通用结构，在目标模型上测试，只调整表层细节", "总是先把 XML 标签转换成 JSON", "假设它的表现完全相同并跳过测试"], answer: 1, explain: "可移植的工作流：用通用结构撰写，用真实输入在目标模型上测试，然后只调整那些有差异的表层细节。"}]} />

<Callout type="takeaways" items={["基本功——上下文、示例、推理、输出约束、角色——可以原封不动地迁移到每一个模型。", "只有六个表层细节有差异：结构提示、语气强度、推理提示、预填充、系统提示词的分量，以及工具结构。", "切换模型是微调，而不是重写：保留通用结构，在目标模型上测试，一次只调整一个表层杠杆。", "把你的提示词纳入版本控制，这样在模型之间迁移就是一个小的、可评审的 diff。"]} />

## 下一步

- [专门针对 Claude 的提示](/docs/prompting/prompting-claude)
- [Claude 对比 ChatGPT、Gemini 与 Copilot](/docs/foundations/claude-vs-others)
- [选择模型与厂商](/docs/foundations/choosing-a-model-provider)
