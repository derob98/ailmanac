---
sidebar_position: 5
title: "给 Claude 用户的 Gemini 指南"
description: "熟悉 Claude，却需要用 Google 的 Gemini？重要的差异——Gems、Workspace 集成、超大上下文、多模态——以及哪些经验可以直接迁移。"
keywords: [Gemini for Claude users, Google Gemini vs Claude, Gemini API, AI Studio, switching AI assistants]
---

<LevelBadge level="beginner" />

你已经习惯用 Claude 的思维方式。现在一个项目落在了 Google Workspace 里，某位同事分享了一个自定义的 **Gem**，或者你需要把一段两小时的视频和一叠 PDF 一次性丢进同一个提示词里——而最自然的工具就是 Google 的 **Gemini**。好消息是：你所知道的几乎一切都能迁移过去。本页把你的 Claude 心智模型映射到 Gemini 上，标出少数几个确实运作不同的地方，并告诉你什么时候 Gemini 往往是更好的选择。

<Callout type="objectives" items={[
  "把 Claude 的概念对应到它们在 Gemini 里的等价物（Gems、Gemini 应用、Workspace 集成、AI Studio + Gemini API）",
  "知道你的哪些 Claude 提示词习惯可以原封不动地延续（大多数都可以）",
  "识别出少数几个你确实需要调整的地方",
  "知道 Gemini 什么时候往往会大放异彩——Google 生态系统任务、超大上下文，以及原生多模态",
]} />

<VerifyNote lastVerified="2026-06-28" source="https://ai.google.dev/gemini-api/docs/models">
Google 那边的精确模型名称与版本号、套餐层级、文件/上传限制、上下文窗口大小以及价格**在不断变化**。本页教的是持久的心智模型和概念映射；对于任何具体的名称、限制或价格，请查阅 Google 自己的最新文档（在「参考资料」中有链接），而不要相信这里的某个数字。
</VerifyNote>

## 60 秒概念对照

如果你只读一节，就读这一节。下面是你在 Claude 里熟悉的东西如何与 Gemini 对应：

| 在 Claude 里你叫它… | 在 Gemini 的世界里它是… | 是同一个概念吗？ |
| --- | --- | --- |
| Claude.ai（聊天应用） | 位于 gemini.google.com 的 **Gemini 应用** | 是——面向消费者/助手的界面 |
| Claude Projects（工作区 + 文件 + 指令）+ 可复用的 Custom Styles | **Gems**（一个已保存、可复用的助手，带有指令 + 知识文件） | 接近——参见下文的区别 |
| 账户偏好设置 /「我希望你如何回应」 | Gem 指令 + 你的账户设置 | 是——同一个概念 |
| Artifacts / 文件上下文 | 附加到 Gem 上的**知识文件**（含来自 Google Drive 的文件） | 接近 |
| 模型选择器（Opus / Sonnet / Haiku） | Gemini 的模型选择器（**Pro** 求深度，**Flash** 求速度） | 是——同一个概念，名字不同 |
| Console / API + 提示词工作台 | **Google AI Studio** + **Gemini API** | 是——用来做原型和拿密钥的开发者界面 |
| 工具使用 / 函数调用（API） | **函数调用（Function calling）** | 是——同一个循环，JSON 细节不同 |

结论：几乎没有*新概念*要学。它主要是把你已经理解的东西**改名和挪位置**——外加两个 Gemini 确实比 Claude 走得更远的领域：**Google 生态系统集成**，以及**原生多模态 + 超大上下文**。

## Gems 就是你早就盼着的那个「可复用的 Claude」

一个 **Gem** 是你构建一次、反复使用的可定制助手：你给它起个名字，写下指令，并可选地附加一些供它参考的知识文件。如果你曾经保存过一个 Claude 系统提示词，或者围绕参考文档搭建过一个 Claude Project，那么 Gem 就是把同样的本能收进了一个对象里。<VerifyNote lastVerified="2026-06-28" source="https://support.google.com/gemini/answer/15235603">Google 把 Gem 描述为一个由指令（通常按人设、任务、上下文和格式来组织）加上可选的上传知识文件（PDF、Word/Google Docs、Drive 文件）定义的可定制助手；具体的创建流程、文件限制和支持的类型会变化——请在 Google 当前的帮助页面上核实。</VerifyNote>

Google 推荐的指令结构对 Claude 用户来说会感觉很熟悉，因为它不过就是良好的提示词写法：

<Steps items={[
  {title: "人设（Persona）", body: "这个 Gem 是谁——它的角色和语气。就是你在 Claude 系统提示词里用的那句 '你是一个……' 的框架。"},
  {title: "任务（Task）", body: "它该做什么。用一句清晰的话说明目标。"},
  {title: "上下文（Context）", body: "它应当假定的背景——以及它可以参考的知识文件，包括来自 Google Drive 的实时文档。"},
  {title: "格式（Format）", body: "输出的样式：表格、列表、JSON、结构化文档。就像你向 Claude 索要某种特定格式一样。"},
]} />

一个值得弄准的微妙之处：一个 Gem 把**指令 + 参考材料打包进一个可复用的助手里**，很像一个 Claude Project 的指令和文件，但它被框定为一个团队可以运行的*可共享专家*，而不仅仅是你的私人工作区。当一个 Gem 能引用 Drive 文件时，它往往读取**最新版本**——这是与把静态快照粘贴进聊天相比一个虽小但真实的区别。<VerifyNote lastVerified="2026-06-28" source="https://workspace.google.com/blog/product-announcements/5-ways-to-boost-your-teams-productivity-with-the-gemini-app-featuring-new-sharable-gems">Google 已经描述过面向 Workspace 订阅者的可共享 Gems 以及引用 Drive 文件（使用其最新版本）的能力；可用性、共享范围和文件限制会变化——请查看 Google 当前的公告。</VerifyNote>

## Workspace 集成才是真正的重头戏

这是 Gemini 与你的 Claude 习惯分歧最大的地方，而且对于合适的任务通常是往*好*的方向分歧。Gemini 应用与 Google Workspace 打通了——Gmail、Docs、Drive、Calendar 等等——因此它可以拉取上下文并在你的 Google 数据上执行操作，而无需你复制粘贴。<VerifyNote lastVerified="2026-06-28" source="https://knowledge.workspace.google.com/admin/gemini/google-workspace-with-gemini">Google 描述 Gemini 与 Workspace 应用（Gmail、Docs、Drive 等）集成，以呈现和总结你的内容；哪些应用被连接、以及对这种访问的管理员/账户控制，都会变化——请在 Google 当前的 Workspace 文档上核实。</VerifyNote>

对于 Claude 用户来说，心态转变是这样的：与其*把材料带到模型面前*，你常常可以*让模型指向已经存在于你 Google 账户里的材料*。这改变工作流的程度大于它改变提示词的程度。提示词仍然是「总结这个、提取那个、起草这个」——但那个「这个」可以是一份实时的 Drive 文档或你收件箱里的一个会话，而不是一段粘贴内容。

<Callout type="tip" items={[
  "如果你的工作已经存在于 Gmail/Docs/Drive 里，Gemini 的集成就省掉了 Claude 会要求的那个复制粘贴步骤。",
  "把 Gemini 能访问的东西当作受账户和管理员控制的——核实当前的权限，而不要想当然。",
  "仍然要限定范围：指向你所指的那份具体文档或会话，而不是你的整个 Drive，理由和你不会把所有东西都倒进一个 Claude 提示词里是一样的。",
]} />

## 原生多模态 + 超大上下文——尽情利用它

有两项能力是 Gemini 常常真正比习惯性的 Claude 工作流更值得选择的地方：

- **原生多模态输入。** Gemini 模型天生就能在单次请求里同时接受文本、图像、音频和视频——而不是把图像硬拼上去。如果你的任务涉及*观看*视频、*聆听*音频，或者一次性对混合媒介进行推理，那就是 Gemini 的甜蜜点。<VerifyNote lastVerified="2026-06-28" source="https://ai.google.dev/gemini-api/docs/vision">Google 记录了 Gemini 在单次请求中接受多种输入模态（文本、图像，以及对许多模型而言的音频和视频）；每个具体模型支持哪些模态因模型而异——请在 Google 当前的模型和视觉文档上核实。</VerifyNote>

- **超大上下文窗口。** 许多 Gemini 模型接受极大的输入——Google 把这框定为一次性处理整个代码库、多份长文档或数小时的媒体，从而减少对分块或检索变通方案的需要。*精确的* token 上限是易变的、且因模型而异，所以不要把它写死；持久的要点是「把整个东西丢进去」在这里是一种比你可能习惯的更可行的策略。<VerifyNote lastVerified="2026-06-28" source="https://ai.google.dev/gemini-api/docs/long-context">Google 把许多 Gemini 模型描述为拥有大上下文窗口（它援引了能处理 100 万或更多 token 的模型），从而在单次请求中支持长文档、大代码库以及长视频/音频；每个模型的精确限制会变化——请在 Google 当前的长上下文文档上核实。</VerifyNote>

要调整的习惯：用 Claude 时你可能还没开始尝试就本能地去裁剪、分块或搭建检索。用一个大上下文的 Gemini 模型时，通常值得先**把整份文档（或视频）一次性放进一个提示词里试试**，只有在撞上真实的限制时才去做优化。

## AI Studio + Gemini API——开发者界面

当你从聊天转向构建时，Claude 用户有一个干净的类比：

- **Google AI Studio** 是原型工作台——测试提示词、试验多模态输入、调参数并**获取 API 密钥**的最快途径。把它想成你在动手接线之前去做实验的地方。<VerifyNote lastVerified="2026-06-28" source="https://ai.google.dev/aistudio">Google 把 AI Studio 定位为用 Gemini 做原型、测试多模态输入、调整参数并获取 API 密钥的开发者环境；它的具体功能和免费层条款会变化——请在 Google 当前的 AI Studio 页面上核实。</VerifyNote>
- **Gemini API** 是你做完原型后从自己的代码里调用的东西——相当于从控制台演练场转向生产环境调用。

有一个框定上的区别要弄清楚：**Gemini 应用**（gemini.google.com）是那个带 Workspace 集成的精致*助手*；**AI Studio** 是用来测试模型行为和获取密钥的*开发者*界面。它们是不同的门，Claude 用户偶尔会把它们混淆，就像有人可能把 Claude.ai 和 API 控制台混淆一样。按活儿选门：做助手的活儿 → 用应用；做构建 → 用 AI Studio 然后用 API。

### 函数调用——形状相同，JSON 细节不同

如果你在 Claude 上做过[工具使用](/docs/api/tool-use)，Gemini 的**函数调用**会让你立刻感到熟悉，因为*循环是一样的*：

<Steps items={[
  {title: "声明你的函数", body: "你用名称、描述以及参数的 JSON schema（类型、哪些是必需的）来描述每个函数。和 Claude 的工具定义是同一个概念。"},
  {title: "模型请求一次调用", body: "给定提示词和已声明的函数，模型返回一个结构化的函数调用——名称加参数——来代替（或伴随）文本。它自己不执行任何东西。"},
  {title: "你来执行它", body: "你的应用提取名称和参数，并运行真正的函数。模型提议，你的代码定夺。"},
  {title: "返回结果", body: "你把函数的输出喂回给模型。"},
  {title: "模型作答", body: "它用结果来产生最终回应——并且在收尾前可能请求更多调用。"},
]} />

相同的是：描述 → 模型请求 → 你执行 → 返回结果 → 作答这个循环，以及*模型永远不运行你的代码*这条原则。Gemini 还支持**并行**调用（一个回合里若干个相互独立的函数）和**组合式**调用（把函数串起来，后面的调用依赖于前面的结果）——这些概念你已经从 Claude 那里知道了。<VerifyNote lastVerified="2026-06-28" source="https://ai.google.dev/gemini-api/docs/function-calling">Google 记录了通过名称/描述/JSON-schema 参数来声明函数，以及一个「定义 → 调用模型 → 本地执行 → 返回结果」的循环，外加并行和组合式（顺序）调用；具体的字段名、响应形状和选项会变化——请在 Google 当前的函数调用指南上核实。</VerifyNote>

**有区别**、并且如果你复制粘贴会把你绊倒的是：具体的 JSON 字段名、函数调用与结果在消息列表里如何表示、参数/schema 约定，以及具体的端点形状。作为一条跨提供商的规则，**提示词和架构可以迁移；线路格式细节不能**——一定要重新核对每个提供商各自的 schema。（如果你是从 [ChatGPT for Claude Users](/docs/models/chatgpt-for-claude-users) 过来的，同样的警告也适用：循环是通用的，JSON 不是。）

## 哪些经验可以迁移（剧透：几乎全部）

你辛苦学来的 Claude 习惯不会白费。以下这些基本上原封不动地延续：

- **提示词结构**——清晰的角色、明确的任务、示例、要求的输出格式 → [提示词基础](/docs/prompting/basics)
- **有意识地给上下文**——限定范围；指向正确的材料（现在可能是一份实时的 Drive 文档）
- **要求特定的输出格式**——表格、JSON、步骤；用法相同
- **就地迭代**——通过追问来打磨
- **工具使用循环**——描述工具，让模型调用它们，返回结果 → [工具使用](/docs/api/tool-use)
- **按任务难度选择模型**——Flash 求快/省，Pro 求深度，和 Opus/Sonnet/Haiku 是同样的判断

<PromptCard title="与提供商无关的系统提示词——放进一个 Gem 的指令里，或放进一个 Gemini API 系统提示词里">{`You are a {role}.
Goal: {one clear sentence}.
Rules:
- Use ONLY the context I provide or the files attached; if the answer isn't there, say "I don't know".
- Be concise. No preamble.
- Output format: {exact format — e.g. a markdown table with columns X, Y, Z}.
When you need information you don't have, ask one focused question instead of guessing.`}</PromptCard>

## 什么才是真正要调整的

一份简短、诚实的、确实有区别的清单：

- **集成改变的是工作流，不是提示词。** 用 Gemini 你可以指向实时的 Google 数据，而不是把它粘贴进来。核实它被允许访问什么——那是受账户和管理员控制的。
- **对大输入默认「整个丢进去」。** 大上下文和原生多模态意味着你常常可以跳过分块/检索的条件反射，先试整份文档或视频。
- **构建者有两扇门。** Gemini 应用（助手）和 AI Studio（开发者演练场 + 密钥）是分开的；别跑到聊天应用里去找 API 密钥。
- **工具的线路格式。** 函数调用的*概念*是相同的；JSON 细节不是——永远不要一字不差地复用 Claude 的工具 JSON。
- **易变的具体信息。** 模型名称、版本号、层级、上下文限制和价格变得很快。养成到源头确认的习惯，就像你对 Claude 也该这么做一样。

## Gemini 什么时候往往会大放异彩

两者都是出色的通用助手，而在任何给定任务上的差距往往很小且在变动——所以把这当作*倾向*，而非定律，并且让你自己在[自己的数据上做评测](/docs/models/choosing-a-model)来决定：

- **Google 生态系统任务。** 如果工作已经存在于 Gmail、Docs、Drive 或 Calendar 里，Gemini 的原生 Workspace 集成消除了 Claude 不靠手动粘贴就无法匹敌的摩擦。
- **超大的单次上下文。** 巨型文档、整个代码库，或你想*一次性*推理的长媒体——倾向于选一个大上下文的 Gemini 模型，跳过检索脚手架。
- **原生多模态。** 视频和音频理解，或混合媒介提示词，是 Gemini 结构性的强项。
- **一个已经跑在 Google 上的团队。** 如果你的协作者共享 Gems 并生活在 Workspace 里，去那里跟他们会合胜过摩擦。

大多数时候诚实的答案是：**你（或你的团队）已经搭建在哪个上面，就用哪个。** 技能是可移植的；搭建成本才是真正的切换成本。关于与提供商无关的选择方式，参见[选择一个模型](/docs/models/choosing-a-model)和[我该用哪个 Claude？](/docs/start-here/which-claude)。

<Flashcards title="Gemini 到 Claude 词汇对照" cards={[
  {front: "Gemini 应用（gemini.google.com）", back: "Google 面向消费者/助手的界面，带深度 Workspace 集成。≈ Claude.ai。"},
  {front: "Gem", back: "一个可复用、通常可共享的助手，由指令（人设/任务/上下文/格式）加上可选的知识文件（含实时 Google Drive 文档）定义。≈ 一个已保存的 Claude 系统提示词 + 一个 Project 的文件。"},
  {front: "Workspace 集成", back: "Gemini 可以在 Gmail/Docs/Drive/Calendar 上读取并执行操作，无需复制粘贴。这是与 Claude 最真实的最大区别——指向实时数据，而不是粘贴它。"},
  {front: "大上下文 + 原生多模态", back: "许多 Gemini 模型接受极大的输入，并同时接受文本/图像/音频/视频。在分块之前默认「整个丢进去」。"},
  {front: "Google AI Studio", back: "开发者原型工作台——测试提示词、调参数、获取 API 密钥。≈ 一个控制台/演练场。与聊天应用分开。"},
  {front: "函数调用", back: "Gemini 的工具使用：通过 JSON schema 声明函数，模型请求一次调用，你执行并返回结果。支持并行和组合式调用。和 Claude 工具使用是同一个循环；JSON 细节不同。"},
]} />

<Quiz title="自我检测" questions={[
  {q: "你的项目材料已经存在于 Google Docs 和 Gmail 里。在这里选用 Gemini 的持久优势是什么？", options: ["它写的文笔总是比 Claude 好", "它的原生 Workspace 集成让它能在你实时的 Google 数据上工作，无需复制粘贴", "它是唯一带模型选择器的助手"], answer: 1, explain: "结构性的区别在于 Google 生态系统集成——Gemini 可以指向实时的 Drive/Gmail 内容，而不需要你粘贴它。提示词本身没有变化。"},
  {q: "你用过 Claude 的工具使用。关于 Gemini 函数调用，什么是真的？", options: ["一个你必须重新学习的完全不同的概念", "同样的「声明→调用→执行→返回」循环，带并行/组合式调用——但具体的 JSON 字段名不同", "连 JSON 都一模一样，所以你可以原封不动地粘贴 Claude 的工具定义"], answer: 1, explain: "循环和架构直接迁移（而且 Gemini 还支持并行和组合式调用）；只有线路格式细节不同——所以不要在提供商之间照搬原始 JSON。"},
  {q: "你有一段 90 分钟的视频和好几份长 PDF 要一起推理。Gemini 原生的做法是什么？", options: ["立刻搭一条检索流水线并把所有东西分块", "试着把整个东西放进一个提示词里，倚靠大上下文和原生多模态，只有在撞上限制时才去优化", "先把所有东西转成纯文本，因为 Gemini 读不了视频"], answer: 1, explain: "大上下文窗口和原生多模态输入正是 Gemini 往往大放异彩的地方——从 Claude 要调整的习惯是先试「整个丢进去」，而不是条件反射地去分块或搭 RAG。"},
]} />

<Callout type="takeaways" items={[
  "几乎没有新概念——Gemini 应用 ≈ Claude.ai，一个 Gem ≈ 一个可复用的系统提示词 + Project 文件，模型选择器（Pro/Flash）≈ Opus/Sonnet/Haiku，函数调用 ≈ 工具使用。",
  "真正的区别在于杠杆，而非重新学习：深度的 Google Workspace 集成、超大的上下文，以及原生多模态（图像/音频/视频）。",
  "调整你的条件反射：指向实时的 Google 数据而不是粘贴它；在分块之前先试「整个丢进去」；记住应用和 AI Studio 是两扇分开的门。",
  "你的提示词、上下文纪律、输出格式和工具循环习惯原封不动地迁移——只有工具调用的 JSON 细节不同。",
  "名称、版本、层级、上下文限制和价格很快就会过时——到 Google 的文档核实具体信息，就像你对 Claude 也该做的那样。",
]} />

## 参考资料与延伸阅读

- [Gemini API — Models | Google AI for Developers](https://ai.google.dev/gemini-api/docs/models) — 权威的、当前的 Gemini 模型及其能力清单。
- [Long context | Gemini API](https://ai.google.dev/gemini-api/docs/long-context) — 大上下文窗口如何运作以及它们能带来什么。
- [Image / vision understanding | Gemini API](https://ai.google.dev/gemini-api/docs/vision) — 跨模型的原生多模态输入。
- [Function calling | Gemini API](https://ai.google.dev/gemini-api/docs/function-calling) — 工具调用的形状，外加并行和组合式调用。
- [Google AI Studio | Gemini API](https://ai.google.dev/aistudio) — 开发者原型工作台以及在哪里获取 API 密钥。
- [Create a Gem in the Gemini app — Gemini Apps Help](https://support.google.com/gemini/answer/15235603) — Gems 是什么以及如何构建它们。
- [Google Workspace with Gemini — Workspace Help](https://knowledge.workspace.google.com/admin/gemini/google-workspace-with-gemini) — Gemini 如何与 Gmail、Docs、Drive 等集成。
- [Sharable Gems & team productivity — Google Workspace Blog](https://workspace.google.com/blog/product-announcements/5-ways-to-boost-your-teams-productivity-with-the-gemini-app-featuring-new-sharable-gems) — 可共享的 Gems 以及引用 Drive 文件。

## 下一步

- 与提供商无关的选择方式 → [选择一个模型](/docs/models/choosing-a-model)
- 也从其他大助手过来的？ → [ChatGPT for Claude Users](/docs/models/chatgpt-for-claude-users) · [Grok for Claude Users](/docs/models/grok-for-claude-users)
- 到处都通用的习惯 → [提示词基础](/docs/prompting/basics) · [工具使用](/docs/api/tool-use)
- 深入核心 → [我该用哪个 Claude？](/docs/start-here/which-claude)
