---
sidebar_position: 6
title: "开源 AI Agent 框架"
description: "一份中立于厂商的地图，梳理构建 LLM agent 的主要开源框架——LangGraph、LlamaIndex、AutoGen、CrewAI 以及极简循环方法——以及如何选择。"
keywords: [AI agent frameworks, LangChain, LangGraph, AutoGen, CrewAI, LlamaIndex, open source agents]
---

<LevelBadge level="advanced" />

当你亲手搭过几个 agent 之后，同样的管道逻辑会反复出现：一个循环调用模型、运行模型要求的工具、把结果喂回去，并在任务完成时停止。**Agent 框架**把这套管道封装起来——外加状态、记忆和多 agent 协调——让你少写胶水代码。本页是一份持久、中立于厂商的地图，梳理主要的**开源**选项、它们实际提供什么，以及如何选择。它们几乎全都是**模型无关的**：既能配合 Claude、GPT、Gemini，也能配合本地开放权重模型。

<Callout type="objectives" items={[
  "了解 agent 框架相比手写循环给了你什么——以及没给什么",
  "认识三种原型：有状态图、基于角色的团队，以及极简循环",
  "使用一套可复用的流程来选择其一——用复杂度换取控制力",
  "记住这些大多是模型无关的——框架很少把你锁定在某一个厂商上",
]} />

<VerifyNote lastVerified="2026-06-28" source="https://github.com/langchain-ai/langgraph">
框架版图变化很快：star 数、版本号、默认 API，乃至**哪个项目仍在积极维护**都经常变动。举个例子，AutoGen 已进入维护模式，并把新用户导向其继任者。请把下面每一处具体断言都视为易过期的，在拿一个项目下注之前，先到项目自己的仓库/文档（见「来源」中的链接）确认。
</VerifyNote>

## agent 框架实际给了你什么

抛开品牌包装，一个框架无非是在向你提供四样东西中的某个子集：

- **编排（Orchestration）**——控制循环。顺序步骤、分支、重试、循环到完成，以及（越来越常见的）能在崩溃后存活并从中断处恢复的**持久执行**。
- **工具（Tools）**——一套标准方式来声明模型可调用的函数、校验其参数、运行它并返回结果。就是你在 [工具使用](/docs/api/tool-use) 中已经熟悉的那套「描述→调用→执行→返回」循环。
- **记忆与状态（Memory & state）**——一个跨轮次保存对话历史、草稿事实和检索到的文档的地方，让 agent 在各步骤之间不会失忆。
- **多 agent（Multi-agent）**——让多个专门化 agent 相互交接工作的模式：一个主管把任务委派给工人，或者对等 agent 协作完成一项任务。（在概念上和 Claude Code 的 [子 agent](/docs/claude-code/subagents) 是同一个思路。）

当你原本得亲手编写并维护这全部四样东西时，框架才值得用。而当你的任务是「调用模型、跑一两个工具、返回一个答案」时，它*不*值得——在那里，框架只是你要花时间与之搏斗的额外开销。

## 三种原型

框架之间的差别比它们的营销话术所暗示的要小。真正只有三种形态，大多数项目都是其中之一的变体：

- **有状态图 / 工作流（Stateful graph / workflow）。**你把 agent 建模为一张由节点和边（或步骤和转移）构成的显式图。控制力最大、状态可检查，适合长时运行和人在回路的流程。前期要学的东西更多。→ **LangGraph**、**LlamaIndex Workflows**。
- **基于角色的团队（Role-based crew）。**你按*角色*（「研究员」「写手」「审稿人」）描述 agent，让它们协作或跑一段流程。表达一个多 agent 团队很快；你用一些细粒度控制换取了高层抽象。→ **CrewAI**，以及 **AutoGen** 那种对话式多 agent 风格。
- **极简循环 / 少抽象（Minimal loop / few abstractions）。**在模型原生工具调用之上薄薄一层，包含几个 agent 之间的交接，别的不多。从头到尾都好读、好丢弃。→ **OpenAI Agents SDK**（实验性项目 **Swarm** 的生产级继任者），以及**你自己写的朴素循环**。

<Callout type="tip" items={[
  "从能用的最简单方案开始——朴素的工具调用循环往往胜过一个笨重的框架。",
]} />

## 快速巡览（下注前请核实定位）

以下是值得了解的开源项目。之所以点名，是因为每个项目真实的仓库都经过核实；一切易变的信息都藏在上面的 VerifyNote 背后。

- **LangGraph**——一个面向有状态、长时运行 agent 的低层编排框架，把 agent 建模为图；持久执行和人在回路是一等公民。既可独立使用，也可与更广的 **LangChain** 生态一起用。模型无关。<VerifyNote lastVerified="2026-06-28" source="https://github.com/langchain-ai/langgraph" />
- **LlamaIndex**——起初是一个数据/RAG 框架（连接器、索引、检索），如今也提供了一个事件驱动的 **Workflows** 层给 agent 用。当你的 agent 本质上是*在你的文档之上做检索*时，它很强。<VerifyNote lastVerified="2026-06-28" source="https://github.com/run-llama/llama_index" />
- **Microsoft AutoGen**——一个面向对话式多 agent 系统的框架。截至 2026 年年中，它处于**维护模式**；微软把新项目导向一个统一的继任者（**Microsoft Agent Framework**，合并了 AutoGen + Semantic Kernel）。开工前请查看当前状态。<VerifyNote lastVerified="2026-06-28" source="https://github.com/microsoft/autogen" />
- **CrewAI**——一个基于角色的框架：按角色和目标定义 agent，把它们组织成 **Crews**（自主协作）或 **Flows**（事件驱动的控制）。是搭出一个多 agent 团队的快捷路径。<VerifyNote lastVerified="2026-06-28" source="https://github.com/crewAIInc/crewAI" />
- **OpenAI Agents SDK**——一个刻意保持轻量、少抽象的框架，面向带交接的多 agent 工作流。尽管名字如此，它其实是**厂商无关的**（其文档指出支持 100+ 种 LLM），并且是实验性项目 **Swarm** 的生产级继任者。<VerifyNote lastVerified="2026-06-28" source="https://github.com/openai/openai-agents-python" />
- **朴素的 agent 循环**——完全没有框架：你自己围绕模型原生工具调用写的 `while` 循环。对简单 agent 而言这是正确的默认选择，也正是上面每个框架归根结底所包装的东西。

## 如何选一个框架

<Steps items={[
  {title: "写下任务及其硬约束", body: "用一句话说明 agent 做什么，外加那些不可谈判的条件：延迟、成本上限、数据隐私、运行是否必须在崩溃后存活，以及是否必须有人来批准某些步骤。"},
  {title: "先问你到底需不需要框架", body: "如果只是一个 agent 调用一两个工具，写个朴素循环。许多生产级 agent 从不需要更多。只有当你原本得自己重新实现编排、记忆「和」多 agent 时，才采用框架。"},
  {title: "让形态匹配工作", body: "长时运行 / 人在回路 / 状态可检查 → 有状态图（LangGraph、LlamaIndex Workflows）。一支专家团队 → 基于角色的团队（CrewAI）。几个 agent 互相交接、保持简单 → 极简循环（OpenAI Agents SDK，或你自己写的）。"},
  {title: "确认它还活着且模型无关", body: "确认仓库仍在积极维护（不是处于维护模式），并且它能干净地支持你实际使用的模型——Claude、GPT、Gemini，或本地模型。大多数是厂商中立的；去核实，别假设。"},
  {title: "把最冒险的 20% 做出原型", body: "把那唯一最难的步骤——棘手的工具、那次交接、失败后恢复——在两个候选里各做一遍。让那一步变得可读的抽象，就是你的赢家。"},
  {title: "留一条撤离通道", body: "把你的工具和提示词保持为朴素的函数和字符串，让框架包裹你的逻辑，而不是拥有它。切换框架应该意味着重接编排，而不是重写一切。"},
]} />

## 每个框架都在包装的那个东西

在伸手去拿任何库之前，先看看它们全都构建于其上的那个循环会很有帮助。这就是全部思路——模型决策，你执行，重复直到完成：

```python
# Provider-neutral agent loop — the core every framework wraps.
# `model_call` and `run_tool` are yours; swap in Claude, GPT, Gemini, or a local model.

def agent_loop(task, tools, max_steps=10):
    messages = [{"role": "user", "content": task}]

    for _ in range(max_steps):
        # 1. Ask the model what to do next (it sees the tool schemas).
        response = model_call(messages, tools=tools)

        # 2. No tool requested → the model is done. Return its answer.
        if not response.tool_calls:
            return response.text

        # 3. Run each requested tool and feed results back in.
        messages.append(response.as_message())
        for call in response.tool_calls:
            result = run_tool(call.name, call.arguments)
            messages.append({
                "role": "tool",
                "tool_call_id": call.id,
                "content": result,
            })

    return "Stopped: hit max_steps without finishing."
```

如果你能读懂这段，你就理解了本页每个框架在底层做的事。图为其加上显式状态和分支；团队为其加上角色和委派；极简 SDK 为其加上整洁的交接——但那个心跳始终是这个循环。

<PromptCard title="极简 agent 系统提示词（与上面的循环搭配）">{`You are a task-completing agent with access to tools.

Loop:
1. Think briefly about the next single step toward the goal.
2. If a tool would help, call exactly ONE tool with valid arguments.
3. When you have enough to answer, stop calling tools and give the final answer.

Rules:
- Prefer the fewest tool calls that get the job done.
- If a tool fails, read the error and adjust — do not repeat the same call.
- Never invent tool results; use only what tools actually returned.
- If the goal is impossible with the available tools, say so and stop.

Goal: {one-sentence task}`}</PromptCard>

## 关于炒作的一点说明

这里没有哪个框架是「最好的」——这个问题本身就是错的。图框架赢在控制力和持久性；团队框架赢在快速表达一支团队；极简框架赢在可读性和低锁定；而朴素循环获胜的频率，比框架的 README 所承认的要高。正确的选择，是那个能让*你自己*最难的一步变清晰的最小工具。就像挑模型一样，让你自己的任务——而不是 star 数——来决定。你在 [评估](/docs/power-user/evals) 上会用的那套纪律，在这里同样适用：做原型、在真实用例上测量、留一条撤离通道。

<Quiz title="自我检查" questions={[
  {q: "你的 agent 做一次模型调用、运行单个搜索工具，然后返回一个答案。正确的起点是什么？", options: ["采用最大的多 agent 框架，好让你随时准备扩展", "写一个朴素的工具调用循环", "用一个带研究员和写手 agent 的基于角色的团队"], answer: 1, explain: "对于一个 agent 和一两个工具，手写循环最简单且没有锁定。只有当你原本得自己重新实现编排、记忆和多 agent 协调时，才去伸手拿框架。"},
  {q: "你需要一个长时运行、必须在崩溃后存活并恢复、并让人来批准某些步骤的 agent。哪种原型最合适？", options: ["一个极简的少抽象循环", "一个有状态图 / 工作流框架", "GitHub star 最多的那个"], answer: 1, explain: "持久执行、可检查的状态和人在回路，正是有状态图原型（如 LangGraph、LlamaIndex Workflows）为之而生的东西。star 数不是选择标准。"},
  {q: "一位同事说「OpenAI Agents SDK 意味着我们被锁死在 GPT 上」。这对吗？", options: ["对——这个名字暗示它只能配合 OpenAI 的模型", "不对——它是厂商无关的，支持许多模型，包括 Claude、Gemini 和本地模型", "只有当你还装了 LangChain 时才如此"], answer: 1, explain: "尽管名字如此，该 SDK 自己的文档把它描述为厂商无关，支持 100+ 种 LLM。大多数开源 agent 框架都是模型无关的——请按项目逐个核实，而不是从名字去假设。"},
]} />

<Flashcards title="Agent 框架——核心思想" cards={[
  {front: "agent 框架给了你什么？", back: "以下之一子集：编排（控制循环）、工具（声明/校验/运行）、跨轮次的记忆与状态，以及多 agent 协调。只有当你原本得自己构建并维护这全部四样时才值得用。"},
  {front: "三种原型", back: "1）有状态图/工作流——显式节点、持久、可检查（LangGraph、LlamaIndex Workflows）。2）基于角色的团队——按角色划分 agent，表达快（CrewAI；AutoGen 风格）。3）极简循环——少抽象、低锁定（OpenAI Agents SDK；你自己的循环）。"},
  {front: "这些是否绑定到某一个模型厂商？", back: "大多不是——它们模型无关，能配合 Claude、GPT、Gemini 和本地开放权重模型。OpenAI Agents SDK 尽管名字如此却是厂商无关的。永远按项目逐个核实。"},
  {front: "什么时候你「不」该用框架？", back: "当任务是一个 agent 调用一两个工具时。朴素的工具调用循环更简单、完全可读、没有锁定——而这正是每个框架所包装的东西。"},
  {front: "AutoGen 的状态（截至 2026 年年中）", back: "处于维护模式——API 稳定、只做关键修复、无重大新功能。微软把新项目导向一个统一的继任者（Microsoft Agent Framework）。采用前永远查看当前仓库状态。"},
  {front: "如何不被炒作左右地选择", back: "让原型匹配工作，确认项目还活着且模型无关，在两个候选里把最冒险的 20% 做原型，并把工具/提示词保持为朴素函数，好让你留一条撤离通道。"},
]} />

<Callout type="takeaways" items={[
  "agent 框架把编排、工具、记忆和多 agent 协调打包起来——只有当你原本得自己构建这全部四样时才采用它。",
  "三种原型覆盖了整个领域：有状态图（控制/持久性）、基于角色的团队（快速团队）、极简循环（可读性、低锁定）。",
  "这些几乎全都是模型无关的——它们跑在 Claude、GPT、Gemini 和本地模型上；请按项目逐个核实，而不是假设。",
  "没有哪个框架是普遍「最好」的；选那个能让「你」最难的一步变清晰的最小工具，并留一条撤离通道。",
  "朴素的工具调用循环是诚实的默认选择——而它正是每个框架所包装的东西。",
  "维护状态会变（例如 AutoGen → 一个继任者）；在下注前，先到项目自己的仓库确认它仍在积极维护。",
]} />

## 来源与延伸阅读

- [LangGraph — GitHub](https://github.com/langchain-ai/langgraph) — 面向有状态、长时运行 agent 的低层编排框架。
- [LangGraph 概述 — LangChain 文档](https://docs.langchain.com/oss/python/langgraph/overview) — 概念、持久执行和人在回路。
- [LangChain — GitHub](https://github.com/langchain-ai/langchain) — LangGraph 所集成的更广生态。
- [LlamaIndex — GitHub](https://github.com/run-llama/llama_index) — 带事件驱动 Workflows 层的数据/RAG 框架，供 agent 使用。
- [LlamaIndex 文档](https://developers.llamaindex.ai/) — RAG、查询引擎、agent 和 Workflows。
- [Microsoft AutoGen — GitHub](https://github.com/microsoft/autogen) — 对话式多 agent 框架（查看维护状态）。
- [Microsoft Agent Framework — GitHub](https://github.com/microsoft/agent-framework) — 用于构建和编排 agent 的统一继任者（AutoGen + Semantic Kernel）。
- [CrewAI — GitHub](https://github.com/crewAIInc/crewAI) — 带 Crews 和 Flows 的基于角色的多 agent 框架。
- [CrewAI 文档](https://docs.crewai.com/) — agent、任务、团队、流程和工具。
- [OpenAI Agents SDK — GitHub](https://github.com/openai/openai-agents-python) — 轻量、厂商无关、带交接的多 agent 框架。
- [OpenAI Agents SDK 文档](https://openai.github.io/openai-agents-python/) — 那个极少抽象的 agent 模型。
- [OpenAI Swarm — GitHub](https://github.com/openai/swarm) — Agents SDK 所取代的实验性前身（教学性质，现已被取代）。

## 下一步

- 中立于厂商地挑选模型的方式 → [选择模型](/docs/models/choosing-a-model)
- 这些框架所包装的循环 → [工具使用](/docs/api/tool-use)
- Claude Code 式的多 agent → [子 agent](/docs/claude-code/subagents)
- 如何真正地比较候选 → [评估](/docs/power-user/evals)
