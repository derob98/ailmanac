---
sidebar_position: 8
title: "확장 사고와 노력(Effort)"
description: "적응형 추론과 노력(effort) 제어 — 작업량에 따라 사고 깊이 선택하기."
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-07-01" source="https://platform.claude.com/docs/en/docs/build-with-claude/extended-thinking">
추론/노력 파라미터는 모델마다 다르고 계속 발전합니다(예전의 `budget_tokens` 방식 조절값은 최신 모델에서 대체되었습니다). 현재 API는 공식 문서에서 확인하세요.
</VerifyNote>

어려운 문제에서 Claude는 **답하기 전에 생각하는** 데 추가 연산을 쓸 수 있어 — 다단계 추론, 까다로운 코드, 수학에서 정확도를 높입니다. 얼마나 *많은* 노력을 쓸지는 대략 여러분이 제어합니다. 이 레슨에서는 작업에 맞게 깊이를 조절하고, API로 사고를 켜고, 과하게 지불하지 않도록 예산을 잡는 법을 배웁니다.

<Callout type="objectives" items={["트레이드오프 설명하기: 사고가 적으면 더 빠르고 저렴하며, 사고가 많으면 어려운 문제에서 더 낫다", "주어진 작업량에 맞는 노력 등급(낮음 / 보통 / 높음) 고르기", "Messages API에서 확장 사고를 켜고 사고 블록과 텍스트 블록을 읽어내기", "budget_tokens가 max_tokens보다 낮게 유지되도록 사고 토큰 예산을 올바르게 잡기", "에이전트와 사고 사슬(chain-of-thought) 프롬프팅에서 노력을 현명하게 적용하기"]} />

## 노력 뒤에 있는 하나의 아이디어

사고는 스위치가 아니라 다이얼입니다. 두 끝을 떠올려 보세요:

- **적은 사고** = 더 빠르고 저렴 — 간단하고 잘 명세된 작업에 적합.
- **많은 사고** = 진짜로 어려운 문제에서 더 낫지만, 지연 시간/비용이 높아짐.

여기에 사람들이 걸려 넘어지는 함정이 있습니다: 이미 쉬웠던 작업에는 추가 사고가 **아무것도** 해주지 않습니다. 지연 시간과 비용만 지불하게 됩니다. 요령은 *답을 바꾸는 곳*에 깊이를 쓰는 것입니다.

최신 모델은 이를 **노력(effort)** 제어로 노출하며(사고 깊이를 자동으로 조정합니다), 그런 모델에서는 원시 토큰 예산이 아니라 등급을 고릅니다. 등급을 작업에 맞추세요.

## 1단계 — 깊이 고르기

코드를 건드리기 전에 물어보세요: *이 작업이 정말 얼마나 어려운가?* 그것을 등급에 대응시키세요.

| 작업 | 권장 노력 |
|---|---|
| 형식 지정, 추출, 간단한 Q&A | 낮음 |
| 일상적인 코딩, 초안 작성, 분석 | 보통 |
| 어려운 디버깅, 까다로운 알고리즘, 신중한 증명 | 높음 |

<Callout type="tip" items={["모든 것을 기본값으로 최대에 두지 마세요 — 작업에 필요 없는 사고에 지연 시간과 비용을 지불하게 됩니다.", "보통에서 시작하세요; 품질이 요구하는 곳에서만 올리세요."]} />

> **직접 해보기:** 계속 읽기 전에 다음 셋을 분류해 보세요 — (a) "이 텍스트에서 이메일을 추출하라", (b) "이 함수를 리팩터링하라", (c) "이 부등식을 증명하라". 각각에 어떤 등급이 맞나요? 위 표와 답을 맞춰보세요.

## 2단계 — 켜기 (API)

Messages API에서는 `thinking` 블록과 토큰 예산으로 사고를 켭니다. 다음 단계를 따르세요.

<Steps items={[{title: "thinking 블록 추가하기", body: "messages.create에 thinking={\"type\": \"enabled\", \"budget_tokens\": N}을 전달해 확장 추론을 켭니다."}, {title: "예산을 상한 아래로 유지하기", body: "예산은 같은 출력 풀에서 나오므로 budget_tokens는 max_tokens보다 작아야 합니다."}, {title: "두 블록 타입 모두 읽어내기", body: "응답은 사고 블록에 이어 답변 텍스트로 돌아옵니다 — message.content를 순회하며 각 block.type을 처리하세요."}]} />

이제 연결해 봅시다. 이것을 복사해 실행하고, 응답이 두 블록으로 도착하는 것을 지켜보세요 — 먼저 추론, 그다음 답변입니다.

<PromptCard title="Enable extended thinking (Python)">{`import anthropic

client = anthropic.Anthropic()

message = client.messages.create(
    model="claude-sonnet-5",
    max_tokens=16000,
    thinking={"type": "enabled", "budget_tokens": 10000},
    messages=[{"role": "user", "content": "Prove that 2^n > n^2 for all n >= 5."}],
)

for block in message.content:
    if block.type == "thinking":
        print("REASONING:", block.thinking)
    elif block.type == "text":
        print("ANSWER:", block.text)`}</PromptCard>

더 큰 예산은 추론할 여지를 더 사줄 뿐, Claude가 그것을 다 쓴다는 보장은 아닙니다 — 깊이는 문제에 맞춰 적응합니다. 예산은 천장처럼 설정하고, 지연 시간이 아프면 낮춰 조정하세요. 모델 ID는 [모델 표](/docs/whats-new/models-and-pricing)에서 확인하며, 정확한 파라미터 형태는 최신 모델에서 다를 수 있으니 위에 링크된 원본과 대조해 확인하세요.

## 3단계 — 실전에서 잘 쓰기

사고를 *켜는* 사람과 그것을 *숙달한* 사람을 가르는 세 가지 습관:

- 확장 사고는 **[사고 사슬 프롬프팅](/docs/prompting/pattern-library)**과 잘 어울립니다 — 하지만 추론 모델에서는 단계별로 *요청할* 필요가 흔히 없습니다; 사고가 내부적으로 일어납니다.
- 사고는 [토큰](/docs/api/tokens-and-pricing)을 소비하며 이는 비용에 영향을 줍니다 — 그에 맞게 예산을 잡으세요.
- 에이전트에서는 *계획* 단계에 더 많은 노력을, 일상적인 도구 호출에는 더 적은 노력을 두는 것이 좋은 분배입니다.

<Callout type="takeaways" items={["노력/사고는 어려운 문제에서 정확도를 위해 지연 시간과 비용을 맞바꿉니다 — 이미 간단한 작업에는 아무것도 해주지 않습니다.", "등급: 형식 지정/추출/간단한 Q&A에는 낮음, 일상적인 코딩/초안 작성/분석에는 보통, 어려운 디버깅/알고리즘/증명에는 높음.", "Messages API에서 budget_tokens는 같은 출력 풀에서 나오므로 max_tokens보다 작아야 합니다; 응답은 사고 블록 다음 텍스트 블록입니다.", "예산을 목표가 아니라 천장으로 다루세요 — Claude는 문제에 필요한 만큼만 씁니다.", "에이전트에서는 계획에 노력을 쓰고 일상적인 도구 호출에서는 아끼세요."]} />

## 확실히 익히기

떠나기 전 빠른 회상 — 각 카드를 뒤집고 소리 내어 답해 보세요.

<Flashcards cards={[{front: "낮은 노력은 어떤 작업에 가장 좋은가?", back: "형식 지정, 추출, 간단한 Q&A."}, {front: "보통 노력은 어떤 작업에 가장 좋은가?", back: "일상적인 코딩, 초안 작성, 분석."}, {front: "높은 노력은 어떤 작업에 가장 좋은가?", back: "어려운 디버깅, 까다로운 알고리즘, 신중한 증명."}, {front: "왜 budget_tokens는 max_tokens보다 작아야 하는가?", back: "사고 예산은 답변과 같은 출력 풀에서 나오기 때문."}, {front: "더 큰 예산이 더 많은 추론을 보장하는가?", back: "아니요 — 추론할 여지를 더할 뿐이며 깊이는 문제에 적응합니다; 예산은 천장으로 다루세요."}]} />

<Quiz title="스스로 점검하기" questions={[{q: "더 많은 사고 노력을 쓰는 것의 핵심 트레이드오프는 무엇인가?", options: ["어려운 문제에서 더 높은 정확도, 대신 더 높은 지연 시간과 비용", "단점 없이 더 낮은 비용", "모든 작업에서 더 빠른 응답", "더 많은 토큰이지만 동일한 품질"], answer: 0, explain: "사고가 적으면 더 빠르고 저렴하며; 사고가 많으면 진짜 어려운 문제에서 더 낫지만 지연 시간/비용이 높아집니다."}, {q: "일상적인 코딩, 초안 작성, 분석에 맞는 노력 등급은?", options: ["낮음", "보통", "높음", "최대"], answer: 1, explain: "표는 일상적인 코딩, 초안 작성, 분석을 보통에 대응시킵니다. 보통에서 시작하고 품질이 요구하는 곳에서만 올리세요."}, {q: "Messages API에서 budget_tokens에 대해 참이어야 하는 것은?", options: ["max_tokens와 같아야 한다", "max_tokens보다 커야 한다", "max_tokens보다 작아야 한다", "max_tokens와 무관하다"], answer: 2, explain: "예산은 같은 출력 풀에서 나오므로 budget_tokens는 max_tokens보다 작아야 합니다."}, {q: "확장 사고가 켜져 있을 때 응답은 어떻게 돌아오는가?", options: ["텍스트 블록만", "사고 블록에 이어 답변 텍스트", "두 개의 별도 API 응답", "오류 블록 다음 텍스트"], answer: 1, explain: "응답은 사고 블록에 이어 답변 텍스트로 돌아옵니다 — message.content를 순회하며 block.type을 확인하세요."}, {q: "에이전트에서는 노력을 어떻게 분배해야 하는가?", options: ["모든 곳에 최대 노력", "일상적인 도구 호출에 더 많이, 계획에 더 적게", "계획 단계에 더 많이, 일상적인 도구 호출에 더 적게", "단계별로 노력을 바꿀 수 없다"], answer: 2, explain: "에이전트에서는 계획 단계에 더 많은 노력을, 일상적인 도구 호출에 더 적은 노력을 두는 것이 좋은 분배입니다."}]} />

## 다음

- [Claude 모델 선택하기](/docs/api/choosing-a-model)
- [프롬프트 캐싱 및 비용 최적화](/docs/api/prompt-caching)
- [프롬프트 패턴 라이브러리](/docs/prompting/pattern-library)
