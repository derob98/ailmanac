---
sidebar_position: 3
title: 첫 API 호출
description: SDK를 설치하고 인증한 뒤, Python, TypeScript 또는 cURL로 Claude에 실제 호출을 해보세요.
keywords: [Anthropic API, Claude API, messages.create, SDK, first call]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<LevelBadge level="beginner" />

<VerifyNote lastVerified="2026-07-01" source="https://platform.claude.com/docs/en/api/getting-started">
아래 API 형태는 안정적이지만 모델 ID와 SDK 버전은 바뀝니다. [현재 모델 표](/docs/whats-new/models-and-pricing)를 참고하세요.
</VerifyNote>

API는 *여러분의* 프로그램이 Claude와 대화하게 해줍니다. 이 레슨이 끝나면 실제 요청을 실행해봤을 것이고, 각 구성 요소가 정확히 무엇을 하는지 이해하게 될 것입니다.

<Callout type="objectives" items={["API 키를 만들고 환경 변수로 안전하게 저장하기", "Python 또는 TypeScript용 공식 SDK를 설치하기(또는 순수 cURL 사용)", "첫 messages 요청을 보내고 Claude의 응답 읽기", "네 가지 핵심 필드 이해하기: model, max_tokens, messages, system"]} />

## 전체 여정을 세 단계로

아래 모든 것은 하나의 단순한 순서에 들어맞습니다. 진행하면서 이 지도를 염두에 두세요.

<Steps items={[{title: "API 키 발급", body: "Anthropic Console에서 키를 만들고 환경 변수로 export하여 코드 안에 절대 남지 않게 하세요."}, {title: "SDK 설치", body: "Python용 anthropic 패키지 또는 TypeScript용 @anthropic-ai/sdk를 추가하세요. cURL은 설치할 것이 없습니다."}, {title: "호출하기", body: "메시지 목록을 모델에 보내고 반환된 content를 출력하세요."}]} />

## 1. API 키 발급

[Anthropic Console](https://console.anthropic.com)에서 하나 만드세요. 그런 다음 코드 안에 절대 남지 않도록 환경 변수로 설정하세요:

<PromptCard title="API 키 export하기">{`export ANTHROPIC_API_KEY="sk-ant-..."`}</PromptCard>

<Callout type="warning" items={["키를 절대 커밋하지 마세요. 키는 환경 변수나 시크릿 매니저에 보관하고, 소스 관리에는 절대 넣지 마세요. 보안 페이지(/docs/security)를 참고하세요."]} />

## 2. SDK 설치

언어를 고르세요. SDK는 방금 설정한 환경 변수에서 키를 자동으로 읽습니다.

<Tabs groupId="lang">
<TabItem value="python" label="Python">

<PromptCard title="설치 (Python)">{`pip install anthropic`}</PromptCard>

</TabItem>
<TabItem value="ts" label="TypeScript">

<PromptCard title="설치 (TypeScript)">{`npm install @anthropic-ai/sdk`}</PromptCard>

</TabItem>
<TabItem value="curl" label="cURL">

설치할 것이 없습니다 — `curl`만 있으면 됩니다.

</TabItem>
</Tabs>

## 3. 호출하기

모든 요청은 `messages`의 목록입니다. 모델은 `content`로 응답합니다. 여러분의 언어에 맞는 스니펫을 실행하면 Claude가 질문에 답하는 것을 볼 수 있습니다.

<Tabs groupId="lang">
<TabItem value="python" label="Python">

```python
import anthropic

client = anthropic.Anthropic()  # reads ANTHROPIC_API_KEY from the environment

message = client.messages.create(
    model="claude-sonnet-5",
    max_tokens=1024,
    messages=[
        {"role": "user", "content": "In one sentence, what is an API?"}
    ],
)

print(message.content[0].text)
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```ts
import Anthropic from "@anthropic-ai/sdk";

const client = new Anthropic(); // reads ANTHROPIC_API_KEY from the environment

const message = await client.messages.create({
  model: "claude-sonnet-5",
  max_tokens: 1024,
  messages: [
    { role: "user", content: "In one sentence, what is an API?" },
  ],
});

console.log(message.content[0].text);
```

</TabItem>
<TabItem value="curl" label="cURL">

```bash
curl https://api.anthropic.com/v1/messages \
  --header "x-api-key: $ANTHROPIC_API_KEY" \
  --header "anthropic-version: 2023-06-01" \
  --header "content-type: application/json" \
  --data '{
    "model": "claude-sonnet-5",
    "max_tokens": 1024,
    "messages": [
      {"role": "user", "content": "In one sentence, what is an API?"}
    ]
  }'
```

</TabItem>
</Tabs>

## 방금 무슨 일이 일어났나

여러분은 네 가지 정보를 보냈습니다. 각각이 무엇을 제어하는지 살펴봅시다:

- **`model`** — 어떤 Claude를 사용할지. 무작정 하드코딩하지 마세요; [모델 선택](/docs/api/choosing-a-model)을 참고하세요.
- **`max_tokens`** — 응답 길이의 상한([토큰](/docs/foundations) 단위). 컨텍스트 창을 설정하지는 *않습니다*.
- **`messages`** — 지금까지의 대화. API는 **무상태(stateless)**입니다: 대화를 이어가려면 매번 전체 히스토리를 다시 보내야 합니다.
- **`system`** (선택) — 이 호출에서 Claude의 역할을 설정하는 최상위 지시.

<Flashcards title="네 가지 핵심 필드" cards={[{front: "model", back: "어떤 Claude를 사용할지. 무작정 하드코딩하기보다 신중하게 고르세요."}, {front: "max_tokens", back: "응답 길이의 상한(토큰 단위). 컨텍스트 창을 설정하지 않습니다."}, {front: "messages", back: "지금까지의 대화. API는 무상태이므로 매 턴마다 전체 히스토리를 다시 보냅니다."}, {front: "system", back: "이 호출에서 Claude의 역할을 설정하는 선택적 최상위 지시."}]} />

<Callout type="tip" items={["API가 무상태이므로 멀티 턴 채팅은 그저 커지는 messages 배열일 뿐입니다: 각 응답을 추가하고 다음 호출에서 전체 목록을 다시 보내세요."]} />

<Quiz title="스스로 점검하기" questions={[{q: "max_tokens는 무엇을 제어하나요?", options: ["컨텍스트 창의 크기", "Claude 응답 길이의 상한", "보낼 수 있는 메시지 수"], answer: 1, explain: "max_tokens는 응답 길이를 토큰 단위로 제한합니다. 컨텍스트 창을 설정하지 않습니다."}, {q: "이 API로 멀티 턴 대화를 어떻게 이어가나요?", options: ["서버가 이전 메시지를 자동으로 기억한다", "API가 무상태이므로 매번 전체 메시지 히스토리를 다시 보낸다", "세션 ID 헤더를 설정한다"], answer: 1, explain: "Messages API는 무상태입니다: 대화를 이어가려면 매 요청마다 전체 대화 히스토리를 다시 보내야 합니다."}, {q: "API 키는 어디에 있어야 하나요?", options: ["환경 변수 또는 시크릿 매니저에", "소스 파일에 하드코딩", "편의를 위해 저장소에 커밋"], answer: 0, explain: "키는 환경 변수나 시크릿 매니저에 보관하고, 소스 관리에는 절대 넣지 마세요."}]} />

<Callout type="takeaways" items={["세 단계: 키 발급, SDK 설치, messages 요청 보내기.", "키를 ANTHROPIC_API_KEY에 저장하면 SDK가 자동으로 읽고 코드에 들어가지 않습니다.", "요청은 메시지 목록이고, 응답은 content로 돌아옵니다.", "API는 무상태입니다: 대화를 이어가려면 전체 대화 히스토리를 다시 보내세요.", "model, max_tokens, messages, system이 가장 먼저 다루게 될 필드입니다."]} />

## 다음

- 알맞은 모델 고르기 & 비용 추정 → [모델 선택](/docs/api/choosing-a-model) · [토큰 & 가격](/docs/api)
- 응답 스트리밍 & 대화 유지 → [스트리밍 & 멀티 턴](/docs/api)
- Claude가 함수를 호출하게 하기 → [도구 사용](/docs/api)
- 프로덕션 준비된 스니펫 → [API 스니펫](/docs/templates)
