---
sidebar_position: 15
title: "오류, 속도 제한 & 안정성"
description: "오류 지도, 백오프를 사용한 재시도, 속도 제한 등급, 모델 마이그레이션."
---

<LevelBadge level="intermediate" />

<Callout type="objectives" items={["HTTP 오류 지도를 읽고 재시도할 상태와 수정할 상태를 구분하기", "지수 백오프와 지터로 일시적 오류를 재시도하되 상한 두기", "retry-after, 스무딩, 배치 처리, 저렴한 모델로 속도 제한 다루기", "모델 지원 중단과 마이그레이션으로부터 코드 보호하기"]} />

<VerifyNote lastVerified="2026-06-28" source="https://platform.claude.com/docs/en/api/errors">
정확한 상태 코드, 헤더, 속도 제한 등급은 변합니다 — 공식 오류/속도 제한 문서에서 확인하세요.
</VerifyNote>

프로덕션 코드는 네트워크 서비스와 통신하므로 실패를 예상해야 합니다. 여기서 약간의 구조가 불안정한 통합과 신뢰할 수 있는 통합의 차이를 만듭니다.

## 오류 지도

여러분이 처리하게 될 일반적인 HTTP 상태:

| 상태 | 의미 | 할 일 |
|---|---|---|
| 400 | 잘못된 요청 | 페이로드를 수정하세요; 그대로 재시도하지 마세요 |
| 401 | 잘못되거나 누락된 API 키 | 자격 증명을 확인하세요 |
| 403 | 허용되지 않음 | 접근/권한을 확인하세요 |
| 429 | 속도 제한 | **백오프 후 재시도** (`retry-after` 존중) |
| 500/529 | 서버 오류 / 과부하 | **백오프 후 재시도** |

<Callout type="tip" items={["SDK는 이것들을 타입이 지정된 예외로 노출하므로, 문자열을 파싱하는 대신 깔끔하게 분기할 수 있습니다."]} />

## 백오프를 사용한 재시도

일시적 오류(429, 5xx)에 대해서는 **지수 백오프 + 지터**로 상한을 두고 재시도하세요:

```python
import time, random
for attempt in range(5):
    try:
        return client.messages.create(...)
    except (RateLimitError, APIStatusError) as e:
        if attempt == 4 or not should_retry(e):
            raise
        time.sleep(min(2 ** attempt + random.random(), 30))
```

<Callout type="warning" items={["많은 SDK가 일시적 오류를 자동으로 재시도합니다 — 여러분만의 재시도를 추가하기 전에 클라이언트의 기본 동작을 파악하세요. 그렇지 않으면 재시도가 중복될 수 있습니다."]} />

## 속도 제한

제한은 계정/등급별로 적용됩니다(분당 요청 수와 토큰 수). 제한에 도달하면 타이밍 힌트와 함께 **429**를 받습니다. 상한 아래에 머무는 전략:

<Steps items={[{title: "retry-after 존중", body: "429를 받으면 응답의 타이밍 힌트를 읽고 그만큼 기다린 뒤 재시도하세요."}, {title: "버스트 스무딩", body: "요청을 한꺼번에 발사하는 대신 시간에 걸쳐 분산하세요."}, {title: "오프라인 작업 배치화", body: "대용량 비대화형 작업을 배치 처리로 옮기세요."}, {title: "저렴한 모델 사용", body: "대용량 단계를 저렴한 모델로 라우팅하세요 — 모델 선택 참고."}]} />

대용량 단계에 알맞은 모델을 고르려면 [모델 선택](/docs/api/choosing-a-model)을 참고하세요.

## 모델 마이그레이션

모델 ID는 날짜/버전이 붙어 있고 지원이 중단됩니다. 스스로를 보호하세요:

- **모델 ID를 설정에서 읽기**, 여기저기 흩어진 리터럴이 아니라.
- **지원 중단 주시하기** — [지원 중단 & 마이그레이션 워치](/docs/whats-new/deprecations)와 [모델 표](/docs/whats-new/models-and-pricing) 참고.
- 모델을 전환할 때 **[평가(evals)](/docs/foundations/evals)를 다시 실행하기**.

<Callout type="takeaways" items={["400/401/403은 여러분 잘못입니다 — 요청이나 자격 증명을 수정하세요, 무작정 재시도하지 마세요. 429와 500/529는 재시도 가능합니다.", "일시적 오류는 지수 백오프 + 지터로 상한을 두고 재시도하세요(예: min(2 ** attempt + random(), 30)).", "429에서는: retry-after를 존중하고, 버스트를 스무딩하고, 오프라인 작업을 배치화하고, 대용량 단계를 저렴한 모델로 라우팅하세요.", "모델 ID를 설정에서 읽고, 지원 중단을 주시하고, 모델을 마이그레이션할 때 평가를 다시 실행하세요."]} />

<Quiz title="스스로 점검하기" questions={[{q: "400 잘못된 요청을 받았습니다. 무엇을 해야 하나요?", options: ["백오프 후 같은 페이로드로 재시도한다", "페이로드를 수정한다; 그대로 재시도하지 않는다", "API 키를 교체한다", "retry-after를 기다린다"], answer: 1, explain: "400은 요청 자체가 유효하지 않다는 뜻입니다 — 페이로드를 수정하세요. 같은 페이로드로 재시도하면 다시 실패할 뿐입니다."}, {q: "백오프로 재시도해야 하는 상태는 어느 것인가요?", options: ["400과 401", "403과 401", "429와 500/529", "400과 429"], answer: 2, explain: "429(속도 제한)와 500/529(서버 오류 / 과부하)는 일시적입니다 — 백오프 후 재시도하세요. 400/401/403은 대신 수정해야 할 호출자 측 문제입니다."}, {q: "지수 백오프에 지터를 더하는 이유는?", options: ["재시도를 정확하고 예측 가능하게 만들기 위해", "대기 시간을 무작위화해 재시도가 모두 충돌하지 않도록", "retry-after 헤더를 건너뛰기 위해", "토큰 사용량을 줄이기 위해"], answer: 1, explain: "예제는 min(2 ** attempt + random.random(), 30)을 사용합니다 — 무작위 요소(지터)가 재시도를 분산시켜 많은 클라이언트가 동시에 재시도하지 않도록 하고, 상한이 대기를 제한합니다."}, {q: "제안된 속도 제한 전략이 아닌 것은?", options: ["retry-after 존중", "오프라인 작업 배치화", "대용량 단계에 저렴한 모델 사용", "모든 호출 지점에 모델 ID 하드코딩"], answer: 3, explain: "속도 제한 전략은: retry-after 존중, 버스트 스무딩, 오프라인 작업 배치화, 저렴한 모델 사용입니다. 모델 ID를 설정에서 읽는 것(하드코딩하지 않는 것)은 속도 제한이 아니라 모델 마이그레이션 관행입니다."}]} />

## 다음

- [안전, 거부 & 폴백](/docs/api/refusals-and-safety)
- [토큰 & 가격](/docs/api/tokens-and-pricing)
- [API로 에이전트 구축하기](/docs/api/building-agents)
