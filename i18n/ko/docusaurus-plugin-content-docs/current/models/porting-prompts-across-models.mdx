---
sidebar_position: 7
title: "모델 간 프롬프트 이식하기"
description: "Claude, GPT, Gemini 및 오픈 모델 사이에서 프롬프트를 옮기는 법 — 깔끔하게 이전되는 부분, 모델별로 조정할 부분, 그리고 마이그레이션 워크플로."
keywords: [prompt portability, prompts across models, Claude vs GPT prompts, model migration, system prompt differences]
---

<LevelBadge level="intermediate" />

한 모델에서 완벽하게 작동하는 프롬프트가 있습니다. 이제 그것을 다른 모델에서 써야 합니다 — 고객사가 GPT를 쓰거나, 비용 목표 때문에 오픈 모델로 옮겨야 하거나, Claude와 Gemini를 A/B 테스트하는 상황입니다. 다행히도 모든 제공사의 자체 문서에서 반복적으로 강조하는 좋은 소식이 있습니다. **좋은 프롬프트의 뼈대는 보편적이라는 것입니다.** 바뀌는 것은 얇은 표면 관례의 층뿐입니다. 이 페이지는 이 둘을 분리해서, 프롬프트를 다시 쓰지 않고도 옮길 수 있게 해주고, 반복 가능한 마이그레이션 워크플로와 이식 가능한 템플릿을 함께 제공합니다.

<Callout type="objectives" items={[
  "Claude, GPT, Gemini 및 오픈 모델 사이에서 프롬프트의 어떤 부분이 깔끔하게 이전되는지 파악하기",
  "어떤 부분이 모델별 조정을 필요로 하는지 — 그리고 그 이유를 파악하기",
  "시행착오식 재작성 대신 반복 가능한 마이그레이션 워크플로를 실행하기",
  "타깃별로 특화할 수 있는, 이식 가능하고 모델 중립적인 프롬프트 템플릿을 유지하기",
]} />

<VerifyNote lastVerified="2026-06-28" source="https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview">
정확한 모델 이름, 기본 장황도(verbosity), 거부(refusal) 튜닝, 시스템 프롬프트 가중치, 지원 파라미터(프리필, 스톱 시퀀스, 최대 토큰 상한), 그리고 도구 호출 JSON 형식은 **모든 제공사에서 버전 간에 계속 변합니다.** 이 페이지는 지속적인 프레이밍 — 무엇이 이전되고 무엇을 조정해야 하는지 — 를 가르칩니다. 특정 동작, 파라미터 이름, 또는 한도에 대해서는 여기에 외워둔 값을 신뢰하지 말고, Sources에 링크된 현행 제공사 문서와 대조하여 확인하세요.
</VerifyNote>

## 멘탈 모델: 구조는 이전되고, 관례는 이전되지 않는다

모든 프롬프트를 두 개의 층으로 생각하세요.

- **추론 층(reasoning layer)** — 무엇을 요청하는지, 어떤 맥락을 제공하는지, 예시, 원하는 출력. 이것은 *소통*에 관한 것이며, 모델 간에 거의 변경 없이 이전됩니다.
- **관례 층(convention layer)** — 이 특정 모델이 그 소통을 어떻게 포장하기를 원하는지: 시스템 프롬프트가 어디에 들어가고 얼마나 강하게 따라지는지, XML을 선호하는지 Markdown을 선호하는지, 정확한 도구 호출 스키마, 기본 수다스러움과 거부 태도, 어떤 생성 파라미터가 존재하는지.

프롬프트를 이식하는 것은 거의 절대로 추론 층을 다시 쓰는 일이 아닙니다. 그것은 관례 층을 다시 맞추는 일입니다. 이 구분을 제대로 잡으면 마이그레이션은 신비로운 일이 아니라 기계적인 일이 됩니다. (애초에 제공사 중립적인 방식으로 타깃을 *고르는* 방법은 [모델 선택하기](/docs/models/choosing-a-model)를 참고하세요.)

## 깔끔하게 이전되는 것

이것들은 Claude, GPT, Gemini, 그리고 주요 오픈 모델에서 똑같이 유효합니다 — 각 제공사의 모범 사례 문서가 독립적으로 이것들을 권장합니다.

- **명확한 역할 + 과제 + 명시적 지시.** "당신은 X입니다. 당신의 일은 Y입니다. 이 규칙들을 따르세요." 모든 제공사가 페르소나/역할 구성을 문서화하고, 모호한 지시보다 구체적이고 명확한 지시에 보상을 줍니다. <VerifyNote lastVerified="2026-06-28" source="https://ai.google.dev/gemini-api/docs/prompting-strategies">Google의 Gemini 프롬프트 설계 가이드는 페르소나, 규칙, 출력 형식 요구사항을 명시적 시스템 지시로 배치할 것을 권장합니다. OpenAI와 Anthropic도 각자의 가이드에서 같은 조언을 합니다 — 각 출처에서 현행 문구를 확인하세요.</VerifyNote>
- **구체적 예시(few-shot).** 2~5개의 입력→출력 쌍을 보여주는 것이 그것을 설명하는 것보다 패턴을 더 안정적으로 가르칩니다. 세 주요 제공사 모두 few-shot 예시를 명시적으로 권장하며, Gemini 문서는 거의 항상 포함시킬 것을 권장할 정도입니다.
- **명시된 출력 형식.** "X, Y, Z 열을 가진 Markdown 표를 반환하세요" 또는 "산문 없이 JSON만"은 어디서나 작동합니다. 엄격 모드(strict-mode) 메커니즘이 다르더라도 *지시* 자체는 이전됩니다(아래에서 다룸).
- **사고 사슬(chain-of-thought) / "답하기 전에 추론하기".** 어려운 과제에서 단계별 추론을 요청하면 모델 전반에서 결과가 향상됩니다. 다만 모델별인 한 가지 주의점이 있습니다: 전용 추론/사고 모델은 종종 이것을 내부적으로 수행하므로, 명시적인 "단계별로 생각하라"는 것은 중복이거나 심지어 역효과일 수 있습니다 — 조정 목록을 참고하세요.
- **그라운딩 / RAG.** "아래 맥락만 사용하세요. 답이 거기 없으면 모른다고 하세요." 검색된 맥락을 제공하고 모델을 그것에 제약하는 규율은 보편적입니다 — 모든 제공사가 환각을 줄이는 방법으로 RAG식 그라운딩을 문서화합니다.
- **긴 맥락을 앞에, 질문을 마지막에.** 문서/데이터로 시작하고 지시로 끝내세요. 이 순서는 모델 전반에 도움이 되며 Gemini 가이드에서 명시적으로 언급됩니다.

[프롬프팅 기초](/docs/prompting/basics)를 체득했다면, 이미 이식 가능한 80%를 갖춘 것입니다.

## 모델별로 조정이 필요한 것

이것이 관례 층입니다 — 진짜로 차이가 나는 부분입니다. 옮길 때 이것들을 다시 맞추세요.

| 측면 | 모델 간에 무엇이 바뀌는가 | 무엇을 해야 하는가 |
| --- | --- | --- |
| **시스템 프롬프트 처리 & 가중치** | 모든 모델에 시스템/개발자 메시지가 있지만, 그것이 사용자 턴을 얼마나 강하게 재정의하는지는 다릅니다. 어떤 모델은 전용 개발자/시스템 역할을 사용자 지시보다 높게 가중하고, 다른 모델은 그 경계를 흐립니다. | 당신의 시스템 프롬프트가 같은 강도로 따라진다고 가정하지 마세요. 제약이 실제로 유지되는지 다시 테스트하고, 무너지면 핵심 규칙을 더 위로 승격하세요. |
| **XML vs Markdown vs 구분자** | Claude는 지시/맥락/예시를 분리하는 데 **XML 태그**를 특히 잘 파싱합니다. GPT와 Gemini는 XML을 받아들이지만 Markdown 제목과 구분자에도 의존합니다. | *어느 정도의* 명시적 구조는 유지하되, 그 형식만 타깃의 선호에 맞게 바꾸세요. 프롬프트의 형식을 원하는 출력에 맞추면 출력 스타일도 유도됩니다. |
| **도구 / 함수 호출 JSON 형식** | 루프(도구 선언 → 모델이 호출 요청 → 실행 → 결과 반환)는 어디서나 동일하지만, **와이어 형식은 그렇지 않습니다** — 필드 이름, 호출/결과가 메시지 목록에 놓이는 방식, 엄격 모드 옵션이 다릅니다. | 도구 원시 JSON을 제공사 간에 그대로 복사하지 마세요. 타깃 스키마에 다시 매핑하세요. [도구 사용](/docs/api/tool-use)을 참고하세요. |
| **기본 장황도** | 최신 모델은 기본적으로 간결해지는 경향이 있고 상세함을 *요청*하기를 기대합니다. 구형 모델은 더 수다스러웠습니다. | 프롬프트를 이식했는데 답변이 짧아지거나 길어졌다면, 프롬프트 탓을 하지 말고 장황도를 명시적으로 설정하세요. |
| **거부 / 안전 태도** | 각 모델은 경계선상의 요청을 거부하거나 얼버무리는 자체 임계값을 가지며, 이것은 릴리스마다 다시 튜닝됩니다. | 이식 후 엣지 케이스를 다시 테스트하세요. 한 모델에서 거부를 전혀 유발하지 않던 프롬프트가 다른 모델에서는 재프레이밍이 필요할 수 있습니다. |
| **답변 프리필(prefilling)** | 형식을 강제하기 위해 어시스턴트의 입에 말을 넣는 것은 전형적인 Claude 시대의 지렛대였습니다 — 하지만 최신 Claude 모델(4.6+)은 프리필된 최종 어시스턴트 턴을 거부하며, 다른 곳에서의 지원 여부도 완전히 제각각입니다. | 프리필을 직접 지시("서두 없이 응답하라"), 출력 스키마, 또는 도구 호출로 대체하세요. |
| **스톱 시퀀스 & 최대 토큰** | 모두 길이 상한을 노출하고 대부분 스톱 시퀀스를 노출하지만, 파라미터 이름, 기본값, 상한이 다릅니다 — 그리고 일부 사고 예산(thinking-budget) 노브는 effort/`max_tokens`로 대체되며 폐기되는 중입니다. | 타깃에서 파라미터 이름과 상한을 다시 확인하세요. 예전 값이 그대로 이전된다고 가정하지 마세요. |

<VerifyNote lastVerified="2026-06-28" source="https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/claude-prompting-best-practices">
위 표의 두 행은 드리프트의 구체적 예입니다: Anthropic의 현행 모범 사례 문서는 프리필된 최종 어시스턴트 턴이 Claude 4.6 이후 모델에서 오류를 반환한다고(대신 직접 지시나 구조화된 출력을 사용하라고) 명시하며, XML 태그가 Claude에서 지시, 맥락, 예시, 입력을 분리하는 권장 방법이라고 명시합니다. 두 동작 모두 버전별이므로 — 의존하기 전에 출처에서 확인하세요.
</VerifyNote>

## 마이그레이션 워크플로

이식을 추측-검증식 재작성이 아니라 짧고 규율 있는 루프로 다루세요.

<Steps items={[
  {title: "두 층을 분리하라", body: "기존 프롬프트를 읽고 머릿속으로 나누세요: 추론 층(역할, 과제, 맥락, 예시, 출력 명세) vs 관례 층(XML/Markdown 선택, 프리필, 도구 JSON, 파라미터). 첫 번째는 유지하고 두 번째는 다시 맞출 것입니다."},
  {title: "제공사별 골조를 제거하라", body: "의미가 아니라 관례에 불과한 것을 모두 제거하세요: 단지 구조였던 XML 태그, 프리필 턴, 타깃이 내부적으로 추론한다면 모델별 '단계별로 생각하라', 그리고 예전 도구 호출 스키마. 이제 깔끔하고 중립적인 코어를 갖게 됩니다."},
  {title: "타깃에 맞게 관례를 다시 맞춰라", body: "타깃이 선호하는 형식으로 구조를 다시 추가하고(XML이 있던 자리에 Markdown 제목이나 구분자), 시스템 프롬프트를 설정하고 기대한 대로 가중되는지 확인하고, 장황도를 명시적으로 설정하고, 도구 정의를 타깃의 JSON 형식에 다시 매핑하세요."},
  {title: "작은 평가 세트를 만들어라", body: "일반적인 케이스에 더해 엣지/경계선 케이스 몇 개(거부와 장황도 변화를 잡기 위해)를 포함하는 실제 입력 5~15개를 고르세요. 이것이 당신의 전/후 잣대입니다 — 이것 없이는 추측일 뿐입니다."},
  {title: "실행하고, 차이를 비교하고, 표면만 튜닝하라", body: "타깃에서 평가를 실행하세요. 출력이 다른 곳에서는 추론 층을 건드리기 전에 관례 층(형식, 장황도, 시스템 프롬프트 강도)을 먼저 고치세요. 대부분의 격차는 여기서 메워집니다."},
  {title: "버전 관리하고 문서화하라", body: "두 프롬프트 변형을 무엇을 왜 바꿨는지에 대한 메모와 함께 버전 관리에 두세요. 그러면 다시 — 혹은 원래대로 — 모델을 전환하는 데 재발견이 아니라 몇 분이면 됩니다."},
]} />

:::tip 처음부터 다시 쓰지 마세요
역할, 과제, 예시를 다시 만들고 있다면 멈추세요 — 그것이 이식 가능한 층입니다. 깔끔한 이식은 *의미*가 아니라 *포장*을 바꿉니다.
:::

## 이식 가능한 프롬프트 템플릿

프롬프트를 모델 중립적인 형태로 작성한 다음, 타깃별로 관례 층만 특화하세요. 이 코어는 가볍고 보편적으로 이해되는 구조를 사용합니다(Markdown으로 깔끔하게 읽히고, 태그는 Claude용 XML로 쉽게 변환됩니다).

<PromptCard title="모델 중립적 프롬프트 코어 — 타깃별로 관례 층을 특화하세요">{`# ROLE
You are {role}.

# TASK
{One clear sentence describing the single goal.}

# RULES
- Use ONLY the information in CONTEXT below. If the answer is not there, say "I don't know" — do not guess.
- Be concise. Respond directly, with no preamble like "Here is..." or "Based on...".
- {Any other hard constraints.}

# OUTPUT FORMAT
{Exact format — e.g. "A Markdown table with columns Name, Value, Source." or "JSON only matching this schema: {...}".}

# EXAMPLES
Input: {example input 1}
Output: {ideal output 1}

Input: {example input 2}
Output: {ideal output 2}

# CONTEXT
{Retrieved documents / data go here — long content first.}

# REQUEST
{The actual user question, last.}`}</PromptCard>

위에 겹쳐 얹을 타깃별 조정:

- **Claude** — 섹션 표시자를 XML 태그(`<role>`, `<rules>`, `<context>`, `<request>`)로 옮기세요. Claude는 그것들을 특히 깔끔하게 파싱합니다. 현행 모델에서는 프리필된 어시스턴트 턴을 사용하지 말고, "서두 없이" 규칙이나 도구/스키마에 의존하세요.
- **GPT** — RULES를 시스템/개발자 메시지에 넣어 더 큰 가중치를 갖게 하세요. Markdown 제목은 괜찮습니다. 스키마를 산문으로만 설명하지 말고 구조화된 출력/엄격 JSON 모드를 사용하세요.
- **Gemini** — ROLE + RULES + OUTPUT FORMAT을 system-instruction 필드로 전달하고, 프롬프트를 직접적으로 유지하고(최신 Gemini는 장황한 프롬프트를 과잉 해석할 수 있음), CONTEXT를 먼저, REQUEST를 마지막으로 유지하세요.
- **오픈 모델(Llama/Mistral/Qwen 등)** — 모델이 게시한 채팅 템플릿을 정확히 따르고, 명시적 few-shot 예시와 형식 제약에 더 강하게 의존하세요. 지시 준수가 프런티어 폐쇄형 모델보다 대개 덜 견고하기 때문입니다.

## 빠른 확인

<Quiz title="스스로 점검하기" questions={[
  {q: "작동하는 Claude 프롬프트를 GPT로 옮기고 있습니다. 본질적으로 변경 없이 유지될 것으로 기대해야 할 부분은?", options: ["각 섹션을 감싸는 XML 태그", "역할, 과제, 예시 및 출력 명세", "프리필된 어시스턴트 턴", "정확한 도구 호출 JSON"], answer: 1, explain: "역할, 과제, 예시, 출력 명세는 추론 층입니다 — 깔끔하게 이전됩니다. XML 태그, 프리필, 도구 JSON은 모델별로 다시 맞추는 관례 층 세부사항입니다."},
  {q: "이식한 프롬프트가 갑자기 새 모델에서 훨씬 짧은 답변을 냅니다. 가장 유력한 원인은?", options: ["당신 프롬프트의 추론이 이제 틀렸다", "새 모델이 그냥 고장 났다", "새 모델의 기본 장황도가 다른데 명시적으로 설정하지 않았다", "few-shot 예시는 절대 이전되지 않는다"], answer: 2, explain: "기본 장황도는 모델 간에 다르고 버전 간에도 바뀝니다. 이전 기본값이 그대로 이어진다고 가정하지 말고 원하는 상세 수준을 명시적으로 설정하세요."},
  {q: "제공사 간에 이식할 때 도구/함수 호출에 대해 참인 것은?", options: ["개념과 JSON이 동일하므로 그대로 복사하면 된다", "설명→요청→실행→반환 루프는 이전되지만 JSON 와이어 형식은 이전되지 않는다", "도구 사용은 아예 이식할 수 없다", "Claude만 도구 사용을 지원한다"], answer: 1, explain: "아키텍처와 루프는 어디서나 동일합니다. 필드 이름, 메시지 표현, 엄격 모드 옵션이 다릅니다. 원시 JSON을 재사용하지 말고 타깃 스키마에 다시 매핑하세요."},
]} />

<Flashcards title="이식 치트시트" cards={[
  {front: "프롬프트의 두 층", back: "추론 층(역할, 과제, 맥락, 예시, 출력 명세)은 깔끔하게 이전됩니다. 관례 층(XML/Markdown, 프리필, 도구 JSON, 파라미터)은 모델별로 다시 맞춥니다."},
  {front: "깔끔하게 이전되는 것", back: "명확한 역할/과제/지시, few-shot 예시, 명시된 출력 형식, 사고 사슬(비추론 모델에서), RAG 그라운딩, 긴 맥락 우선 순서."},
  {front: "모델별로 조정할 것", back: "시스템 프롬프트 가중치, XML vs Markdown vs 구분자, 도구 호출 JSON 형식, 기본 장황도, 거부 태도, 프리필 지원, 스톱 시퀀스와 최대 토큰 상한."},
  {front: "XML vs Markdown", back: "Claude는 섹션 분리에 XML 태그를 특히 잘 파싱합니다. GPT와 Gemini는 XML을 받아들이지만 Markdown 제목/구분자도 사용합니다. 구조는 유지하고, 형식만 바꾸세요."},
  {front: "프리필 함정", back: "형식을 강제하려고 어시스턴트의 답변을 프리필하는 것은 Claude 시대의 요령이지만, 최신 Claude 모델(4.6+)은 프리필된 최종 턴을 거부합니다. 대신 직접적인 '서두 없이' 지시, 출력 스키마, 또는 도구 호출을 사용하세요."},
  {front: "평가 세트가 중요한 이유", back: "실제 입력 5~15개(엣지 케이스 포함)는 전/후 잣대를 제공해서, 재작성을 추측하는 대신 관례 층을 의도적으로 고치게 해줍니다."},
]} />

<Callout type="takeaways" items={[
  "프롬프트는 추론 층(이전됨)과 관례 층(모델별로 다시 맞춤)으로 이루어집니다 — 후자를 이식하고, 전자는 유지하세요.",
  "명확한 역할/과제/지시, few-shot 예시, 출력 형식 명세, 사고 사슬, RAG 그라운딩은 Claude, GPT, Gemini, 오픈 모델 전반에 이전됩니다.",
  "시스템 프롬프트 가중치, XML vs Markdown 구조, 도구 호출 JSON, 기본 장황도, 거부 태도, 프리필, 길이 파라미터는 타깃별로 조정하세요.",
  "이식 전/후로 실제 입력에 대해 작은 평가 세트를 실행하고, 추론을 건드리기 전에 관례를 고치세요.",
  "모델 중립적 템플릿과 타깃별 조정을 버전 관리에 두어 전환 비용을 낮추세요.",
  "특정 동작은 릴리스마다 변합니다 — 파라미터와 한도는 기억이 아니라 각 제공사의 현행 문서에서 확인하세요.",
]} />

## 출처 & 더 읽을거리

- [프롬프트 엔지니어링 개요 — Anthropic (Claude) 문서](https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview) — Claude의 프롬프팅 기법: 명확성, 예시, XML 구조화, 사고, 역할.
- [Claude 프롬프팅 모범 사례 — Anthropic 문서](https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/claude-prompting-best-practices) — 모델별 튜닝, XML 태그, 출력/장황도 제어, 그리고 프리필 마이그레이션 가이드.
- [프롬프트 엔지니어링 모범 사례 — OpenAI (GPT) API 문서](https://developers.openai.com/api/docs/guides/prompt-engineering) — 메시지 역할/명령 체계, 구분자, few-shot, 추론 모델 vs GPT 가이드.
- [함수 호출 — OpenAI API 문서](https://developers.openai.com/api/docs/guides/function-calling) — 매핑할 도구 호출 스키마와 요청/응답 루프.
- [프롬프트 설계 전략 — Google Gemini API 문서](https://ai.google.dev/gemini-api/docs/prompting-strategies) — 시스템 지시, few-shot, 구조화, 맥락 우선 순서, 출력/길이 파라미터.
- [Gemini 3 개발자 가이드 — Google Gemini API 문서](https://ai.google.dev/gemini-api/docs/gemini-3) — 직접적 프롬프트, 기본 장황도, 지시 배치에 관한 최신 모델 가이드.

## 다음

- 제공사 간 사고 깊이 맞추기 → [추론 모델 비교](/docs/models/reasoning-models-compared)
- 제공사 중립적으로 타깃 고르기 → [모델 선택하기](/docs/models/choosing-a-model)
- 이식 가능한 기초 → [프롬프팅 기초](/docs/prompting/basics)
- Claude의 구조 지렛대 → [XML 태그](/docs/prompting/xml-tags)
- 도구 스키마 다시 매핑하기 → [도구 사용](/docs/api/tool-use)
