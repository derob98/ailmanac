---
sidebar_position: 1
title: "Modelli di partenza per CLAUDE.md"
description: "Modelli CLAUDE.md annotati e pronti da copiare e incollare, suddivisi per tipo di progetto."
keywords: [CLAUDE.md, Claude Code config, memory file, template]
---

<LevelBadge level="all" />

`CLAUDE.md` è la cosa con la maggiore leva che puoi dare a [Claude Code](/docs/claude-code/what-is-claude-code): istruzioni persistenti che legge a ogni sessione. Inseriscine uno di questi nella radice del tuo repository (oppure esegui `/init` per generare un punto di partenza), poi sfoltiscilo finché non calza a pennello. Usa il pulsante di copia su ogni blocco.

:::tip Mantienilo breve e veritiero
Un CLAUDE.md lungo, velleitario o non aggiornato *fa danni* — Claude lo segue alla lettera. Descrivi come funziona **davvero** il progetto e pota senza pietà. Dettagli nella [pagina su CLAUDE.md](/docs/claude-code).
:::

## Modello di partenza universale

```markdown
# Project: <name>

## What this is
<One or two sentences: what the project does and who uses it.>

## Tech stack
- Language/runtime: <e.g. TypeScript, Node 20>
- Framework: <e.g. Next.js 14 App Router>
- Key libraries: <e.g. Prisma, tRPC, Tailwind>

## How to run
- Install: `<cmd>`
- Dev server: `<cmd>`
- Tests: `<cmd>`
- Lint/format: `<cmd>`

## Conventions
- <e.g. Use functional components; no class components.>
- <e.g. Co-locate tests as *.test.ts next to source.>
- <e.g. Conventional Commits for messages.>

## Guardrails
- Run the tests before saying a task is done.
- Don't edit files under `/generated` or `/vendor`.
- Never commit secrets or .env files.

## Good to know
- <Gotchas, non-obvious decisions, links to deeper docs.>
```

## App web Node / TypeScript

```markdown
# Project: <name> (Next.js + TypeScript)

## How to run
- Dev: `npm run dev`
- Build: `npm run build`
- Test: `npm test`
- Lint: `npm run lint` (must pass before commit)

## Conventions
- TypeScript strict; no `any` without a comment justifying it.
- Components in `src/components`, one folder per component.
- Data fetching via tRPC; never call the DB from a client component.
- Styling: Tailwind utility classes; no inline styles.

## Guardrails
- After any change, run `npm run lint && npm test`.
- Keep files under ~300 lines; split when larger.
- Do not modify `prisma/migrations` by hand.
```

## Servizio Python

```markdown
# Project: <name> (FastAPI)

## How to run
- Install: `uv sync` (or `pip install -e .`)
- Dev: `uvicorn app.main:app --reload`
- Test: `pytest`
- Lint/format: `ruff check . && ruff format .`

## Conventions
- Type hints everywhere; prefer `pathlib` over `os.path`.
- Pydantic models for all request/response bodies.
- f-strings, not %-formatting.

## Guardrails
- Run `pytest` and `ruff check` before completing a task.
- Fail fast with descriptive errors; no silent excepts.
```

## Suggerimenti validi per tutti

- **Stratificalo.** Un `CLAUDE.md` nella radice del repository per l'intero progetto; file `CLAUDE.md` annidati nelle sottocartelle per regole specifiche di un'area.
- **Usa gli `@imports`** per richiamare la documentazione esistente invece di duplicarla (vedi il [riferimento di CLAUDE.md](/docs/claude-code)).
- **Rivedilo ogni mese.** Istruzioni obsolete sono peggio di nessuna istruzione.

## Correlati

- La guida completa → [CLAUDE.md e file di memoria](/docs/claude-code)
- Altri modelli di partenza → [Modelli SKILL.md](/docs/templates), [ricette di hook e settings](/docs/templates)
- Migliora questi → [Contribuisci](/docs/contribute/contribute-in-10-minutes)
