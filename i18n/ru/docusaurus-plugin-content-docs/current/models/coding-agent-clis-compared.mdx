---
sidebar_position: 4.5
title: "Сравнение CLI-агентов для программирования"
description: "Claude Code, Codex CLI, Gemini CLI и открытые терминальные агенты — как обвязка (а не только модель) определяет результат, и как сохранить переносимость конфигурации с помощью AGENTS.md."
keywords: [Claude Code vs Codex CLI, Gemini CLI, opencode, aider, coding agent comparison, AGENTS.md, terminal AI agent, coding harness]
---

<LevelBadge level="intermediate" />

Вы уже выбрали модель. Но программа, которая *оборачивает* модель — цикл, читающий файлы, планирующий, запускающий команды, проверяющий собственную работу и повторяющий попытки — это отдельный выбор, и он значит как минимум столько же. В сообществе эту обёртку называют **обвязкой** (harness) или **агентом**, и одна и та же модель может выглядеть блестяще в одной и посредственно в другой. На этой странице разобраны терминальные агенты для программирования 2026 года, что их действительно различает и как сохранить переносимость конфигурации, чтобы издержки перехода оставались низкими.

<Callout type="objectives" items={[
  "Разделить два выбора, которые часто путают: модель и обвязку, которая ею управляет",
  "Знать устойчивую форму каждого крупного CLI-агента — Claude Code, Codex CLI, Gemini CLI и пакет open-source",
  "Выбирать по осям, которые реально всё решают: автономность, открытость, независимость от модели, песочница и экосистема",
  "Сделать конфигурацию переносимой с помощью AGENTS.md, чтобы менять агентов без переписывания контекста проекта",
]} />

<VerifyNote lastVerified="2026-07-08" source="https://agents.md">
Количество звёзд, номера версий, результаты бенчмарков, цены и доступность планов/тарифов на этой странице меняются **быстро** — рейтинги агентов для программирования перетасовываются почти ежемесячно, а вендоры меняют бесплатные тарифы почти без предупреждения. Считайте *архетипы и оси* ниже устойчивыми; любую конкретную цифру перепроверяйте по собственному репозиторию или документации каждого проекта (ссылки в разделе «Источники») до того, как принять решение.
</VerifyNote>

## Модель — это не агент

Передовая модель — это предсказатель текста. **Агент для программирования** — это обвязка вокруг неё: системный промпт, набор инструментов (чтение, редактирование, запуск shell, поиск), цикл планирования, стратегия управления контекстом, модель прав и песочницы, а также логика повторных попыток. Смените обвязку — и вы смените результат: отчёты практиков на протяжении 2026 года описывают, как одна и та же модель колеблется на десятки пунктов на реальных задачах с репозиториями в зависимости от того, какой агент ею управляет.

Из этого следуют два практических вывода:

- **Число в бенчмарке — это *пара*, а не оценка модели.** «83% на Terminal-Bench» — это результат *модель + обвязка + каркас*. Замените любую часть — и число сдвинется.
- **Ваш цикл — вот настоящая единица сравнения.** Лучший агент — тот, что лучше всего работает на *вашем* стеке, в *вашем* терминале, по цене, с которой вы можете жить — ровно та же логика, что и при выборе модели для программирования. См. [Claude против GPT против Gemini для программирования](/docs/models/claude-gpt-gemini-for-coding) для модельной половины решения; эта страница — про половину с обвязкой.

## Расстановка сил в 2026 году

Пространство терминальных агентов разделилось на два лагеря: **фирменные агенты**, настроенные под модели одной лаборатории, и **независимые от модели** агенты, которые вы направляете на любую модель (или локальные веса) по своему выбору.

| Агент | Кто | Открытость | Модели | Форма |
|---|---|---|---|---|
| **Claude Code** | Anthropic | Закрытый исходный код | Claude (фирменная) | Автономная, агентная работа с несколькими файлами; песочница на уровне ОС; глубокая экосистема skills/MCP/субагентов |
| **Codex CLI** | OpenAI | Открытый (Apache-2.0, Rust) | GPT (фирменная) | Автономный терминальный агент; нативная песочница Windows; широкий охват платформ |
| **Gemini CLI** | Google | Открытый исходный код | Gemini (фирменная) | Агент с большим контекстом, интеграции с экосистемой Google (см. заметку об актуальности ниже) |
| **opencode** | Сообщество (anomalyco) | Открытый (MIT, TypeScript) | Любая (независимость от модели) | Самый популярный открытый агент по звёздам; CLI + десктоп; своя модель на выбор |
| **Aider** | Сообщество | Открытый (Apache-2.0) | Любая, вкл. локальные | Git-нативный напарник по программированию, а не полноценный автономный оркестратор |
| **Cline / Goose / другие** | Сообщество | Открытый | Любая | Встроенные в редактор (Cline) или общие открытые агенты (Goose); независимы от модели |

<VerifyNote lastVerified="2026-07-08" source="https://github.com/google-gemini/gemini-cli">
**Gemini CLI в состоянии перемен.** В середине 2026 года Google изменил доступ по бесплатному тарифу и направил часть пользователей к отдельному CLI «Antigravity». Что именно является правильной точкой входа со стороны Google — Gemini CLI, Antigravity или что-то более новое — это ровно тот тип факта, который устаревает: подтвердите текущее состояние в собственной документации Google, прежде чем на что-то полагаться.
</VerifyNote>

## Оси, которые действительно всё решают

Забудьте на минуту про таблицу лидеров. Вот устойчивые вопросы:

<Steps items={[
  {title: "Фирменный или независимый от модели?", body: "Фирменные агенты (Claude Code, Codex CLI, Gemini CLI) настроены под собственную модель и обычно дают самый гладкий пиковый опыт. Независимые от модели агенты (opencode, Aider, Cline) позволяют менять модели — сегодня передовую, завтра дешёвую или локальную — не меняя инструментов. Выбирайте независимый вариант, если для вас важно избежать привязки к вендору или запускать локальные веса."},
  {title: "Сколько автономности вам нужно?", body: "Aider — это напарник по программированию: сфокусированные, пофайловые, git-нативные правки, где вы участвуете в каждом коммите. Claude Code и Codex CLI — оркестраторы: дайте цель, и они спланируют и выполнят работу по множеству файлов и shell-команд. Больше автономности означает больше рычага и больше потребности в ограждениях."},
  {title: "Каков радиус поражения?", body: "Агент, запускающий shell-команды, может удалить файлы, отправить код или потратить деньги. Предпочитайте песочницу на уровне ОС и явную модель прав вместо «доверься мне». Claude Code и Codex CLI в 2026 году оба поставляются с песочницей; проверьте, к чему может прикоснуться любой агент, прежде чем позволить ему работать без присмотра."},
  {title: "Подходит ли экосистема вашей работе?", body: "Помимо основного цикла: MCP-серверы для инструментов, переиспользуемые skills, субагенты, хуки, интеграция с IDE, действия CI. Более тонкий агент на отличной модели может проиграть более толстому агенту на достаточно хорошей, потому что экосистема делает больше работы."},
  {title: "Во что это обойдётся в ВАШЕМ цикле?", body: "Подписка против оплаты API по метрике, окно контекста, кеширование и то, насколько разговорчива обвязка — всё это двигает реальный счёт. Более дешёвая модель в прожорливой на токены обвязке может стоить дороже, чем более дорогая модель в экономной."},
]} />

<Callout type="tip" items={["Не покупайте обвязку по громкому бенчмарку. Покупайте её по 30-минутному пробному прогону на реальной задаче в вашем собственном репозитории — тот же приём «оценивай на своём коде», который бьёт любую таблицу лидеров моделей.", "Открытость — это страховка, а не функция, которую вы будете использовать ежедневно. Её ценность проявляется в тот день, когда меняется цена или исчезает бесплатный тариф, и вы захотите переехать."]} />

## Переносимость: опишите контекст один раз

Самая большая скрытая издержка перехода — это **контекст вашего проекта**: команды сборки, соглашения, правила тестирования, вещи, которые агент не может вывести сам. В 2026 году для этого есть де-факто стандарт: **`AGENTS.md`**, файл в чистом Markdown в корне репозитория, который курирует Agentic AI Foundation под эгидой Linux Foundation и который нативно читают Codex, Gemini CLI, Cursor, Aider, агент для программирования Copilot, opencode, Goose и ещё 20+ инструментов (его приняли 60k+ репозиториев).

- **`AGENTS.md`** = кросс-инструментальный контекст проекта. Напишите один раз; большинство агентов его читают.
- **`CLAUDE.md`** = собственный файл памяти проекта у Claude Code (см. [CLAUDE.md](/docs/claude-code/claude-md)). Можно держать небольшой `CLAUDE.md`, который указывает на `AGENTS.md`, или поддерживать оба.
- **`SKILL.md`** = отдельная спецификация Anthropic для упаковки переиспользуемых [навыков](/docs/claude-code/skills) с frontmatter и вложенными скриптами. Она *дополняет* `AGENTS.md`, а не конкурирует с ним — одна описывает проект, другая упаковывает переиспользуемую процедуру.

Хранение контекста в `AGENTS.md` означает, что в тот день, когда вы попробуете другого агента, правила вашего проекта поедут с вами бесплатно.

<PromptCard title="Минимальный AGENTS.md — пишите только то, что агент не может вывести сам">{`# AGENTS.md

## Setup
- Install: \`pnpm install\`
- Dev server: \`pnpm dev\` (port 3000)

## Build & test (run before every commit)
- Typecheck: \`pnpm typecheck\`
- Lint: \`pnpm lint\`
- Tests: \`pnpm test\` — all must pass

## Conventions
- TypeScript strict; no \`any\`.
- Components in \`src/components\`, one per file.
- Never edit files under \`generated/\` by hand.

## Constraints
- Do NOT run \`git push\` or open PRs without being asked.
- Secrets live in \`.env.local\`; never commit them.
`}</PromptCard>

<Callout type="warning" items={["Помещайте в AGENTS.md только то, что агент не может обнаружить самостоятельно. Повторение очевидного тратит токены при каждом прогоне и хоронит правила, которые действительно важны. Составляйте файл вручную — автогенерируемые файлы контекста обычно превращаются в шум."]} />

## Решение, на одном дыхании

- **Глубокая автономная работа с Claude и богатейшей экосистемой инструментов** → **Claude Code**.
- **Открытый фирменный агент, привязанный к GPT, с сильным охватом платформ/Windows** → **Codex CLI**.
- **Вы хотите свободно менять модели или запускать локальные веса** → **opencode** или **Cline**.
- **Аккуратные, git-нативные правки с человеком в цикле** → **Aider**.
- **Крупная ставка на контекст экосистемы Google** → **Gemini CLI** (сначала подтвердите текущий доступ).

Что бы вы ни выбрали, храните контекст проекта в `AGENTS.md` и подтвердите выбор реальной задачей в собственном репозитории, прежде чем на нём остановиться.

<Quiz questions={[{q: "Что люди имеют в виду под «обвязкой» (harness) агента для программирования?", options: ["Обучающие данные модели", "Программу, оборачивающую модель — её инструменты, цикл планирования, песочницу и логику повторных попыток", "Тему оформления IDE", "GPU, на котором она работает"], answer: 1, explain: "Обвязка — это агент вокруг модели: системный промпт, инструменты, цикл планирования/повторов, управление контекстом и песочница. Она может сдвигать результаты на реальных задачах на десятки пунктов при одной и той же модели."}, {q: "В блоге пишут «83% на бенчмарке по программированию» для агента. Что на самом деле измеряет это число?", options: ["Только модель", "Конкретную пару модель + обвязка + каркас", "Язык программирования", "Навык пользователя"], answer: 1, explain: "Оценка агента для программирования — это пара. Замените модель, обвязку или каркас — и число сдвинется, поэтому единственный тест, который засчитывается, — ваш собственный репозиторий."}, {q: "Зачем выбирать независимого от модели агента, такого как opencode или Aider?", options: ["Он всегда точнее", "Чтобы свободно менять модели и запускать локальные веса, страхуясь от изменений цен и тарифов", "Ему не нужна настройка", "Он работает только с одной моделью"], answer: 1, explain: "Независимые от модели агенты позволяют направить на любую модель — передовую, дешёвую или локальную — так что изменение цены или отмена бесплатного тарифа не вынуждают мигрировать инструмент."}, {q: "Для чего нужен AGENTS.md?", options: ["Список изменений (changelog)", "Кросс-инструментальный контекст проекта (команды сборки/тестов, соглашения, ограничения), читаемый 20+ агентами", "Файл лицензии", "Память только для Claude"], answer: 1, explain: "AGENTS.md — это де-факто стандарт, файл контекста проекта в Markdown, читаемый Codex, Gemini CLI, Cursor, Aider, opencode и многими другими — напишите его один раз, и большинство агентов его используют. CLAUDE.md — собственный эквивалент у Claude Code."}]} />

## Флеш-карточки

<Flashcards title="Словарь CLI-агентов для программирования" cards={[
  {front: "Обвязка (агент)", back: "Программа, оборачивающая модель в агента для программирования: системный промпт, инструменты (чтение/редактирование/shell/поиск), цикл планирования + повторов, управление контекстом, права и песочница. Значит столько же, сколько модель."},
  {front: "Фирменный против независимого от модели", back: "Фирменные агенты (Claude Code→Claude, Codex CLI→GPT, Gemini CLI→Gemini) настроены под модель одной лаборатории. Независимые от модели агенты (opencode, Aider, Cline) направляются на любую модель, включая локальные веса."},
  {front: "Claude Code", back: "Терминальный агент Anthropic с закрытым исходным кодом для Claude. Автономная работа с несколькими файлами, песочница на уровне ОС и глубокая экосистема: MCP, skills, субагенты, хуки, действия CI."},
  {front: "Codex CLI", back: "Терминальный агент OpenAI с открытым исходным кодом (Apache-2.0, Rust) для GPT. Автономный, нативная песочница Windows, широкий охват платформ. ~96k звёзд на GitHub в середине 2026 года."},
  {front: "opencode", back: "Независимый от модели агент от сообщества с открытым исходным кодом (MIT, TypeScript) — самый популярный по звёздам открытый агент для программирования в 2026 году. CLI плюс десктоп-приложение; своя модель на выбор."},
  {front: "Aider", back: "Git-нативный напарник по программированию с открытым исходным кодом (Apache-2.0). Сфокусированные пофайловые правки с вами в цикле, работает почти с любой LLM, включая локальные модели. Не полноценный автономный оркестратор."},
  {front: "AGENTS.md", back: "Де-факто стандартный файл контекста проекта в Markdown в корне репозитория. Курируется Agentic AI Foundation (Linux Foundation); читается 20+ агентами в 60k+ репозиториях. Опишите контекст один раз, большинство агентов его прочтут."},
  {front: "CLAUDE.md против AGENTS.md против SKILL.md", back: "CLAUDE.md = память проекта у Claude Code. AGENTS.md = кросс-инструментальный контекст проекта. SKILL.md = пакет переиспользуемого навыка от Anthropic. Дополняют друг друга, а не конкурируют."},
]} />

## Источники и дальнейшее чтение

- [AGENTS.md — официальный сайт и спецификация](https://agents.md) (Agentic AI Foundation / Linux Foundation)
- [openai/codex — репозиторий Codex CLI](https://github.com/openai/codex) (Apache-2.0, Rust)
- [sst/opencode — репозиторий opencode](https://github.com/sst/opencode) (MIT, TypeScript)
- [Aider-AI/aider — репозиторий Aider](https://github.com/Aider-AI/aider) (Apache-2.0)
- [google-gemini/gemini-cli — репозиторий Gemini CLI](https://github.com/google-gemini/gemini-cli)
- [Anthropic — документация Claude Code](https://docs.claude.com/en/docs/claude-code/overview)
- Похожее на этом сайте: [Claude против GPT против Gemini для программирования](/docs/models/claude-gpt-gemini-for-coding) · [Локальные агенты для программирования](/docs/models/local-coding-agents) · [CLAUDE.md](/docs/claude-code/claude-md) · [Навыки](/docs/claude-code/skills)
