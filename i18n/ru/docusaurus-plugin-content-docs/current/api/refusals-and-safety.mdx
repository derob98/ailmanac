---
sidebar_position: 14
title: "Безопасность, отказы и запасные варианты"
description: "Корректно обрабатывайте отказы и отличайте их от блокировок классификатора."
---

<LevelBadge level="intermediate" />

В продакшене ваш код должен обрабатывать случай, когда Claude **не станет** (или не сможет) ответить ожидаемым образом. Сделанное хорошо, это незаметно для пользователей; сделанное плохо — это сбой или сбивающий с толку ответ.

<Callout type="objectives" items={[
  "Отличить отказ модели от блокировки классификатора/безопасности — и понять, почему это важно",
  "Обрабатывать отказ как нормальный исход, а не падать или показывать пустой ответ",
  "Сократить нежелательные отказы, не пытаясь взломать те, которые обоснованы",
  "Выбрать запасной вариант: уточняющий вопрос, безопасная альтернатива или передача человеку",
]} />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/api">
Причины остановки и формы ответов, связанных с безопасностью, могут меняться — уточняйте в официальном справочнике API.
</VerifyNote>

## Две разные вещи

- **Отказ модели** — Claude отклоняет запрос (например, считает его вредоносным). Ответ сигнализирует об этом (обычно через `stop_reason`/контент отказа). Воспринимайте это как нормальный исход, а не как ошибку.
- **Блокировка классификатора/безопасности** — отдельный слой безопасности может заблокировать контент. Это может выглядеть иначе, чем отказ модели.

Понимание того, что именно вы получили, позволяет реагировать соответствующе, а не повторять запрос вслепую.

## Обрабатывайте это корректно

```python
resp = client.messages.create(...)
if getattr(resp, "stop_reason", None) == "refusal":
    # Don't show a raw/empty result. Offer a safe fallback or a clarifying ask.
    show_user("I can't help with that as asked. Here's what I can do instead…")
else:
    render(resp)
```

## Снижайте нежелательные отказы

- **Добавляйте легитимный контекст.** Запрос может совпасть по шаблону с чем-то чувствительным, хотя намерение безвредно; указание реальной, легитимной цели помогает.
- **Будьте конкретны.** Расплывчатые или провокационные формулировки вызывают осторожность.
- **Не боритесь с этим.** Если запрос действительно недопустим, отказ корректен — спроектируйте изящный путь, а не пытайтесь обойти защиту.

<PromptCard title="Укажите легитимный контекст с самого начала">{`I am a security engineer at [company] reviewing our own authentication code
before a release. Below is a function we wrote and own.

Identify weaknesses in it and explain how an attacker would exploit each one,
so we can fix them before we ship.

[code]`}</PromptCard>

## Шаблоны запасных вариантов

- **Уточняющий вопрос** вместо тупика.
- **Безопасная альтернатива** («Я могу вместо этого суммировать публичную информацию»).
- Для конвейеров — **передавайте человеку**, когда уверенность/право невелики.

<Steps items={[
  {title: "Определяйте, не предполагайте", body: "Проверьте ответ на сигнал отказа, прежде чем отрисовывать его. Никогда не пропускайте непроверенный ответ прямо в ваш UI — так пользователи получают пустое поле или сырой объект."},
  {title: "Классифицируйте", body: "Отказ модели или блокировка классификатора/безопасности? Отказ — это суждение о запросе; блокировка — это отдельный слой безопасности. Повтор идентичного ввода не помогает ни в том, ни в другом случае, но они указывают на разные исправления."},
  {title: "Плавная деградация", body: "Замените тупик уточняющим вопросом или безопасной альтернативой, голосом вашего продукта. Пользователь должен получить следующий шаг, а не ошибку."},
  {title: "Эскалируйте, когда ставки того требуют", body: "В конвейере направляйте случаи с низкой уверенностью или неподходящие случаи в человеческую очередь, вместо того чтобы гадать или молча их отбрасывать."},
]} />

<Quiz title="Проверьте себя" questions={[
  {q: "Ваш код получает отказ от Claude. Как правильно с ним поступить?", options: ["Как с исключением — перехватить и повторить тот же запрос", "Как с нормальным исходом — определить его и показать запасной вариант", "Как с багом в вашем промпте, который нужно обойти взломом"], answer: 1, explain: "Отказ — это нормальный ответ, а не ошибка. Определите его, затем предложите уточняющий вопрос или безопасную альтернативу. Повтор идентичного ввода просто тратит токены."},
  {q: "Безвредный запрос продолжает получать отказы. Что попробовать в первую очередь?", options: ["Перефразировать, чтобы скрыть, о чём вы спрашиваете", "Указать реальную, легитимную цель и быть более конкретным", "Переключиться на модель поменьше"], answer: 1, explain: "Запросы получают отказы, когда совпадают по шаблону с чем-то чувствительным. Добавление легитимного контекста и конкретики — вот решение. Скрывать намерение — это взлом: неверный ход, и он также делает вывод менее полезным."},
  {q: "Почему важно, получили ли вы отказ модели или блокировку классификатора/безопасности?", options: ["Это неважно — обрабатывайте их одинаково", "Они могут выглядеть в ответе по-разному и указывать на разные исправления", "Только один из них тарифицируется"], answer: 1, explain: "Это разные механизмы: отказ модели — это когда Claude отклоняет запрос, а отдельный слой безопасности может заблокировать контент. Понимание того, что именно вы получили, позволяет реагировать соответствующе, а не повторять запрос вслепую."},
]} />

<Callout type="takeaways" items={[
  "Отказ — это исход, а не ошибка — определяйте его до отрисовки, всегда.",
  "Отказ модели и блокировка классификатора — разные вещи; определите, что именно вы получили.",
  "Нежелательные отказы обычно уменьшаются, когда вы добавляете легитимный контекст и становитесь конкретнее.",
  "Если запрос действительно недопустим, отказ корректен — постройте изящный путь, не пытайтесь обойти защиту.",
  "Запасные варианты по порядку: уточняющий вопрос, безопасная альтернатива, передача человеку.",
]} />

## Далее

- [Ошибки, лимиты частоты и надёжность](/docs/api/errors-and-rate-limits)
- [Ответственное использование, этика и верификация](/docs/security/responsible-use)
- [Устранение неполадок: почему Claude так сделал?](/docs/contribute/troubleshooting)
