---
sidebar_position: 2
title: "Токены, контекст и стоимость"
description: "Правильно считайте токены, отличайте max_tokens от окна контекста и оценивайте стоимость."
---

<LevelBadge level="beginner" />

<Callout type="objectives" items={["Правильно считать токены с помощью собственных инструментов Anthropic (а не токенизатора другой модели)", "Отличать max_tokens от окна контекста — и понимать, почему это важно", "Оценивать стоимость API по количеству входных и выходных токенов по тарифам конкретной модели", "Снижать стоимость без потери качества: подбирать размер модели, кэшировать, урезать, группировать в пакеты"]} />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/docs/about-claude/pricing">
Цены за токен и лимиты контекста меняются — всегда сверяйтесь с актуальными цифрами на официальных страницах цен и моделей. Смотрите [таблицу моделей](/docs/whats-new/models-and-pricing).
</VerifyNote>

Стоимость и лимиты в API измеряются в **[токенах](/docs/foundations/tokens-and-context)** (примерно ¾ слова). Три вещи, в которых важно не ошибиться.

## 1. Правильно считайте токены

Не угадывайте и **не используйте токенизатор другой модели** (например, `tiktoken`) — количество токенов различается между семействами моделей. Используйте эндпоинт **подсчёта токенов** Anthropic или соответствующий помощник SDK, чтобы измерить запрос *перед* отправкой.

<Callout type="tip" items={["Грубое правило для планирования: ~750 слов ≈ ~1 000 токенов (токен — это примерно ¾ слова)."]} />

## 2. `max_tokens` ≠ окно контекста

Эти два лимита легко перепутать, но они управляют разными вещами.

| Лимит | Что ограничивает | Когда менять |
| --- | --- | --- |
| **`max_tokens`** | Длину *ответа* (только вывод) | Увеличьте, если ответ обрезается |
| **Окно контекста** | Общий бюджет на **вход + вывод** | Большой ввод оставляет меньше места для вывода |

<Callout type="warning" items={["Устанавливайте max_tokens под нужды задачи. Слишком низкое значение обрежет ответ. Излишне высокое не стоит дороже — вы платите только за реально сгенерированные токены — но может привести к многословным ответам."]} />

## 3. Оцените стоимость

Вам выставляют счёт за **входные токены + выходные токены** по тарифам конкретной модели (Opus > Sonnet > Haiku). Быстрая оценка:

```
cost ≈ (input_tokens × input_rate) + (output_tokens × output_rate)
```

Актуальные тарифы смотрите на [официальной странице цен](https://platform.claude.com/docs/en/docs/about-claude/pricing) — мы намеренно не прописываем их здесь жёстко.

## Снижение стоимости (без потери качества)

<Steps items={[
  {title: "Подберите размер модели", body: "Начните с Sonnet; оставьте Opus для сложных частей. См. Выбор модели (/docs/api/choosing-a-model)."},
  {title: "Используйте кэширование промптов", body: "Переиспользуйте стабильный префикс промпта между вызовами. См. Кэширование промптов (/docs/api/prompt-caching)."},
  {title: "Урезайте ввод", body: "Отправляйте только тот контекст, который важен. Здесь также помогает RAG (/docs/foundations/rag)."},
  {title: "Группируйте офлайн-работу в пакеты", body: "Пакетные задания, где задержка не имеет значения."}
]} />

Больше стратегий в разделе [Компромиссы между стоимостью и задержкой](/docs/foundations/cost-and-latency).

<Quiz title="Проверьте себя" questions={[
  {
    q: "Вы планируете запрос и хотите узнать количество токенов перед отправкой. Чем стоит воспользоваться?",
    options: ["Универсальным токенизатором вроде tiktoken", "Эндпоинтом подсчёта токенов Anthropic или помощником SDK", "Количеством слов, делённым на 0,75, как итоговым числом", "Размером окна контекста из таблицы моделей"],
    answer: 1,
    explain: "Количество токенов различается между семействами моделей, поэтому не переиспользуйте токенизатор другой модели. Используйте эндпоинт подсчёта токенов Anthropic или помощник SDK, чтобы измерить перед отправкой."
  },
  {
    q: "Ваш ответ постоянно обрывается на середине предложения. Какую настройку вы меняете?",
    options: ["Понизить max_tokens", "Повысить max_tokens", "Уменьшить окно контекста", "Переключиться на меньшую модель"],
    answer: 1,
    explain: "max_tokens ограничивает длину ответа. Если вывод обрезается, увеличьте его."
  },
  {
    q: "Как вам выставляется счёт в API?",
    options: ["За запрос, по фиксированному тарифу", "Только за входные токены", "За входные + выходные токены по тарифам конкретной модели", "За каждое слово вывода"],
    answer: 2,
    explain: "cost ≈ (input_tokens × input_rate) + (output_tokens × output_rate), с тарифами, которые различаются по моделям (Opus > Sonnet > Haiku)."
  },
  {
    q: "Какой первый шаг лучше всего снижает стоимость без потери качества?",
    options: ["Всегда использовать Opus для всего", "Начать с Sonnet и оставить Opus для сложных частей", "Установить max_tokens как можно выше", "Использовать токенизатор другой модели, чтобы экономить на вызовах"],
    answer: 1,
    explain: "Подберите размер модели: начните с Sonnet, оставьте Opus для сложных частей. Затем добавьте кэширование промптов, урезание ввода и пакетную обработку."
  }
]} />

<Callout type="takeaways" items={["Всё измеряется в токенах (примерно ¾ слова); ~750 слов ≈ ~1 000 токенов.", "Считайте токены собственными инструментами Anthropic — никогда токенизатором другой модели.", "max_tokens ограничивает ответ; окно контекста — это общий бюджет на вход + вывод.", "Счёт = входные токены + выходные токены по тарифам конкретной модели (Opus > Sonnet > Haiku).", "Снижайте стоимость, подбирая размер модели, кэшируя префиксы, урезая ввод и группируя в пакеты."]} />

## Дальше

- [Выбор модели Claude](/docs/api/choosing-a-model)
- [Кэширование промптов и оптимизация стоимости](/docs/api/prompt-caching)
- [Стриминг и многоходовые диалоги](/docs/api/streaming)
