---
sidebar_position: 7
title: "Продакшен-сниппеты для API"
description: "Обёртки запросов с повторными попытками, стримингом и циклами вызова инструментов — Python и TypeScript."
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/api">
Имена методов SDK и параметры меняются — сверяйтесь с официальным справочником по API. Идентификаторы моделей: см. [таблицу моделей](/docs/whats-new/models-and-pricing).
</VerifyNote>

Проверенные на практике шаблоны для разработки на основе [API](/docs/api/first-call). Адаптируйте и укрепляйте под свой стек.

## Отказоустойчивая обёртка для вызова (повторы + backoff)

<Tabs groupId="lang">
<TabItem value="python" label="Python">

```python
import time, random, anthropic
client = anthropic.Anthropic()

def ask(messages, model="claude-sonnet-5", max_tokens=1024, system=None):
    for attempt in range(5):
        try:
            return client.messages.create(
                model=model, max_tokens=max_tokens,
                system=system or anthropic.NOT_GIVEN, messages=messages,
            )
        except (anthropic.RateLimitError, anthropic.APIStatusError) as e:
            if attempt == 4:
                raise
            time.sleep(min(2 ** attempt + random.random(), 30))
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```ts
import Anthropic from "@anthropic-ai/sdk";
const client = new Anthropic();

export async function ask(messages, { model = "claude-sonnet-5", maxTokens = 1024, system } = {}) {
  for (let attempt = 0; attempt < 5; attempt++) {
    try {
      return await client.messages.create({ model, max_tokens: maxTokens, system, messages });
    } catch (e: any) {
      if (attempt === 4 || ![429, 500, 529].includes(e?.status)) throw e;
      await new Promise((r) => setTimeout(r, Math.min(2 ** attempt * 1000 + Math.random() * 1000, 30000)));
    }
  }
}
```

</TabItem>
</Tabs>

(SDK по умолчанию тоже повторяют временные ошибки — изучите поведение своего клиента, прежде чем накладывать собственную логику. См. [Ошибки и лимиты запросов](/docs/api/errors-and-rate-limits).)

## Стриминг чата

```python
with client.messages.stream(model="claude-sonnet-5", max_tokens=1024,
        messages=[{"role": "user", "content": "Hello"}]) as stream:
    for text in stream.text_stream:
        print(text, end="", flush=True)
```

## Цикл вызова инструментов (каркас)

```python
messages = [{"role": "user", "content": "What's the weather in Rome?"}]
while True:
    resp = client.messages.create(model="claude-sonnet-5", max_tokens=1024,
                                  tools=TOOLS, messages=messages)
    if resp.stop_reason != "tool_use":
        break
    messages.append({"role": "assistant", "content": resp.content})
    results = [run_tool(b) for b in resp.content if b.type == "tool_use"]
    messages.append({"role": "user", "content": results})  # tool_result blocks
```

:::warning Укрепление
Ограничивайте число итераций и стоимость, валидируйте входные данные инструментов и храните секреты в переменных окружения. Для автономного использования прочитайте [Защита агентов](/docs/security/securing-agents).
:::

## Дальше

- [Ваш первый вызов API](/docs/api/first-call) · [Использование инструментов](/docs/api/tool-use)
- [Создание агентов на основе API](/docs/api/building-agents)
- [Ошибки, лимиты запросов и надёжность](/docs/api/errors-and-rate-limits)
