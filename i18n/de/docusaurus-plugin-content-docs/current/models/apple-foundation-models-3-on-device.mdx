---
sidebar_position: 4.7
title: "Apple Foundation Models 3 & der On-Device-LLM-Stack für Claude-Nutzer"
description: "Apples Foundation Models der dritten Generation (AFM 3) landeten auf der WWDC 2026 mit einem 3B-Dense-On-Device-Modell, einem 20B-Sparse-MoE, das auf dem iPhone läuft, und einem Swift-nativen Framework, das dir nun erlaubt, deinen eigenen LLM-Anbieter mitzubringen — einschließlich Claude. Was tatsächlich ausgeliefert wurde, die nicht offensichtlichen Mechanismen, und wie es die On-Device-Story für alle verändert, die Claude-Apps ausliefern."
keywords: [Apple Foundation Models, AFM 3, AFM 3 Core, AFM 3 Core Advanced, on-device LLM, Foundation Models framework, iOS 26, Swift, Generable, guided generation, LanguageModel protocol, on-device AI, Private Cloud Compute, Claude on Apple Silicon]
---

<LevelBadge level="intermediate" />

Auf der **WWDC 2026 (angekündigt am 8. Juni 2026)** lieferte Apple die dritte Generation seiner Foundation Models — **AFM 3** — und erweiterte das **Foundation Models Framework** in einer Weise, die auch dann zählt, wenn du nie Swift schreibst: Das Framework nimmt nun *Drittanbieter-LLM-Anbieter* auf (Anthropic Claude und Google Gemini werden als Swift-Pakete ausgeliefert), und der *On-Device*-Pfad hört auf, ein Spielzeug-3B-Modell zu sein, und ergänzt ein **20-Milliarden-Parameter-Sparse-MoE**, das auf Apple Silicon läuft, indem es die meisten Experten im NAND hält und nur das, was ein Prompt braucht, in den RAM tauscht. Wenn du Claude-Apps baust und „On-Device" bisher als getrenntes Universum von deinen API-Workflows behandelt hast, schließt sich diese Lücke dieses Jahr. Diese Seite entpackt, was tatsächlich ausgeliefert wurde, die nicht offensichtlichen Mechanismen und die konkreten Migrations- und Kompatibilitätsentscheidungen, die du jetzt hast.

<Callout type="objectives" items={[
  "Die vier AFM-3-Modelle benennen — Core (3B), Core Advanced (20B sparse), Cloud, Cloud Pro — und welches wo läuft",
  "Den Flash-plus-DRAM-Experten-Routing-Trick verstehen, der ein 20B-Modell überhaupt auf ein iPhone bringt",
  "Die @Generable / @Guide-Makros des Foundation Models Frameworks für constrained decoding nutzen — das Modell kann buchstäblich kein ungültiges JSON ausgeben",
  "Wissen, was sich auf der WWDC26 änderte: die Protokolle LanguageModel / LanguageModelExecutor, die es dir erlauben, Claude oder jedes andere LLM in dieselbe Swift-Session-API zu stecken",
  "Entscheiden, wann du zum On-Device-3B greifst, wann du zu Private Cloud Compute springst und wann du die Workload auf der Claude-API belässt",
]} />

<VerifyNote lastVerified="2026-08-09" source="Apple ML Research 'Introducing the third generation of Apple's foundation models' (8 June 2026) + Apple Developer WWDC26 session 339 'Bring an LLM provider to the Foundation Models framework'." />

## Die Ein-Satz-Version

**AFM 3** ist Apples zweiter ernsthafter Wurf für On-Device-LLMs: ein neu gebautes **3B-Dense-Modell (Core)**, ein neues **20B-Sparse-MoE (Core Advanced)**, das auf dem iPhone läuft, indem es die meisten Experten im Flash-Speicher hält, ein Paar **Cloud** und **Cloud Pro** auf Apple Silicon plus NVIDIA-GPUs in Google Cloud, und ein **Framework**, das Anthropic Claude und Google Gemini nun als erstklassige, austauschbare Anbieter hinter demselben `LanguageModel`-Protokoll behandelt. Kostenlos für Entwickler, die On-Device aufrufen; kostenlos auf Private Cloud Compute für Mitglieder des App Store Small Business Program mit weniger als 2 Mio. Erstdownloads.

## Drei nicht offensichtliche Dinge über AFM 3

Alle schreiben die Schlagzeile „Apple hat ein 3B-On-Device-Modell". Hier sind die Mechanismen, die die meisten Berichte auslassen.

### 1. Das 20B-Modell passt auf das iPhone, weil das meiste davon im NAND lebt, nicht im RAM

Die naive Lesart „20-Milliarden-Parameter-Modell auf dem iPhone" ist unmöglich: Selbst bei 4-Bit sind 20B Weights ~10 GB, weit über dem, was ein iPhone im RAM halten kann. Apples Trick, laut WWDC-2026-Ankündigung, ist es, **das vollständige AFM-3-Core-Advanced-Modell im Flash-Speicher (NAND)** zu speichern und **nur die eingabeabhängigen routed experts in den DRAM** zur Inferenzzeit zu tauschen. Die Architektur hat *shared experts*, die resident bleiben, und *routed experts*, die ein Router pro Prompt auswählt (Aktivierung von **1–4B Parametern gleichzeitig**, nicht der vollen 20B). Kombiniert mit **Quantization Aware Training** ist das, was Apple erlaubt, ein 20B-Klasse-Modell auf einem Gerät auszuliefern, das ~8 GB nutzbaren RAM hat. Die Konsequenz für dich: iPhone-seitige Latenz wird nun weniger von Matrixmultiplikation dominiert und mehr von NAND-Lesebandbreite und den Entscheidungen des Routers — eine sehr andere Performance-Hülle als ein 3B-Dense-Modell.

### 2. Constrained Decoding ist in den Swift-Compiler eingebacken, nicht in die Runtime

Das `@Generable`-Makro des Frameworks wird oft als „strukturierte Ausgabe" beschrieben, was seinen Umfang untertreibt. `@Generable` läuft zur *Compile-Zeit*: Es erzeugt ein JSON-Schema und einen Parser für deinen Swift-Typ, und das Framework nutzt dieses Schema dann für **constrained decoding** — das On-Device-Modell wird auf Token-Ebene gezwungen, Ausgaben zu erzeugen, die sich zurück in deine Swift-Struct parsen lassen. Es gibt kein „hoffe, es ist gültiges JSON, und wiederhole dann". Eine `@Guide`-Annotation lässt dich Vorgaben hinzufügen (`.anyOf(["PG", "PG-13", "R", "G"])`, Bereiche, Beschreibungen), die ebenfalls zur Decode-Zeit durchgesetzt werden. Wenn du Zeit damit verbracht hast, JSON-Repair-Fallbacks um eine Claude-Tool-Use-Schleife zu schreiben, kann die On-Device-3B-Modell-Seite deiner App diese gesamte Retry-Schicht fallen lassen.

### 3. Das Framework nimmt nun jedes LLM — einschließlich Claude und Gemini — hinter einer Swift-API

Die WWDC-2026-Session **„Bring an LLM provider to the Foundation Models framework"** lieferte zwei neue Protokolle aus: **`LanguageModel`** (deklariert Fähigkeiten und Executor-Konfiguration) und **`LanguageModelExecutor`** (übernimmt die eigentliche Generierung, den KV-Cache und das Streaming). Jeder kann ein Swift-Paket ausliefern, das sich daran hält — Apple nannte explizit Anthropic Claude und Google Gemini als Launch-Time-Anbieter. Die Konsequenz: Aus Sicht der App gelten dieselbe `LanguageModelSession`, dieselben `@Generable`-Typen, derselbe Tool-Calling-Vertrag und dasselbe Transcript-Modell, ob das zugrunde liegende Modell das On-Device-3B, Private Cloud Compute, Claude auf Anthropics API, Gemini auf Google Cloud oder eine MLX-Weights-Datei von Hugging Face ist. Die Modellwahl wird zur Swift-Paket-Abhängigkeit statt zu einem SDK-Rewrite.

## AFM 3 auf einen Blick

<Flashcards cards={[
  { front: "AFM 3 Core", back: "3B-Parameter-Dense-On-Device-Modell. Neu gebaut aus dem 3B der zweiten Generation, nativ multimodal (Text + Bild + Audio), 25 Sprachen. Läuft auf allen Apple-Intelligence-fähigen Geräten. Kostenlos für On-Device-Aufrufe." },
  { front: "AFM 3 Core Advanced", back: "20B Sparse MoE. Vollständige Gewichte liegen im NAND; der Router tauscht eingabeabhängige Experten in den DRAM. Aktiviert 1–4B Parameter pro Prompt. Erfordert Apples leistungsstärkstes Silizium; ältere Apple-Intelligence-Geräte fallen auf Core zurück." },
  { front: "AFM 3 Cloud", back: "Serverseitiges Arbeitspferd auf Apple Silicon in Private Cloud Compute (PCC). Gleicher Datenschutzvertrag wie der On-Device-Pfad: kein persistenter Serverstatus, kein Logging, das du nicht auditieren kannst." },
  { front: "AFM 3 Cloud Pro", back: "Leistungsfähigstes Servermodell. Nutzt Parallel-Track Mixture-of-Experts (PT-MoE). Erweitert auf NVIDIA-GPUs in Google Cloud, weiterhin innerhalb der PCC-Grenze. Zielt auf die Claude-Opus- / GPT-5-Sol-Klasse von Aufgaben." },
  { front: "ADM 3 Cloud", back: "Bildgenerierungs- und Bearbeitungsmodell (Apple Diffusion Model 3). Steht neben AFM 3 in derselben PCC-Grenze. Treibt Image Playground und Genmoji auf der Serverseite an." },
  { front: "Überraschung der Free-Stufe", back: "Mitglieder des App Store Small Business Program mit weniger als 2 Millionen Erstdownloads bekommen Private-Cloud-Compute-Aufrufe ohne Cloud-API-Kosten. Das ist eine echte Subvention — kein Testguthaben." },
]} />

## Was tatsächlich wo läuft

<Steps items={[
  { title: "AFM 3 Core (3B) — immer On-Device", body: "Läuft auf jedem Apple-Intelligence-Gerät. Kostenlos aus Swift über das Foundation Models Framework aufrufbar — kein API-Schlüssel, kein Netzwerk, keine Kosten pro Token. Hier legst du Klassifizierung, Extraktion, Zusammenfassung, On-Device-Rewrites und alles ab, was im Flugmodus funktionieren muss." },
  { title: "AFM 3 Core Advanced (20B) — nur neuere iPhones", body: "Läuft auf Apples leistungsstärkstem Silizium; ältere Apple-Intelligence-Geräte fallen transparent auf Core zurück. Nutze das, wenn das 3B eine Aufgabe verweigert oder halluziniert und die Anfrage auf Expert-Swap-Latenz warten kann, aber On-Device bleiben muss (z. B. privates Gesundheits-/Finanz-Reasoning)." },
  { title: "AFM 3 Cloud / Cloud Pro — Private Cloud Compute", body: "Das Framework routet zu PCC, wenn das Gerätemodell nicht ausreicht. Du wählst nicht die spezifische Server-SKU — du forderst eine Reasoning-Stufe an und Apple wählt aus. Gleicher Datenschutzvertrag wie On-Device: PCCs Node-Attestierung verhindert, dass Apple-Mitarbeiter deine Anfragen lesen." },
  { title: "Drittanbieter — via Swift-Paket", body: "Füge ein Paket hinzu, das sich an LanguageModel + LanguageModelExecutor hält (Anthropic Claude und Google Gemini sind die Launch-Time-Beispiele). Von der Session-API her ist der Code identisch — die Modellwahl ist eine Paketabhängigkeit." },
]} />

## Ein minimaler On-Device-Aufruf

Das Headline-Feature des Frameworks ist, dass du getypte, schema-validierte Ausgaben vom On-Device-3B mit fast keiner Zeremonie bekommen kannst.

<PromptCard title="On-Device-3B-Aufruf mit @Generable strukturierter Ausgabe (Swift)">
{`import FoundationModels

@Generable
struct TripIdea {
    @Guide(description: "Short, evocative title")
    let title: String

    @Guide(description: "One-paragraph pitch, 40–80 words")
    let summary: String

    @Guide(.range(1...14))
    let estimatedDays: Int

    @Guide(.anyOf(["easy", "moderate", "hard"]))
    let difficulty: String
}

let session = LanguageModelSession()   // defaults to on-device Core
let idea = try await session.respond(
    to: "Suggest a shoulder-season trip in Puglia for a family with a 6-year-old.",
    generating: TripIdea.self          // constrained decoding
)

print(idea.title, idea.difficulty, idea.estimatedDays)
`}
</PromptCard>

Zur Laufzeit wird das Modell auf Token-Ebene gezwungen, Tokens auszugeben, die den laufenden Text als `TripIdea` parsebar halten. Du siehst nie einen JSON-String; du erhältst einen `TripIdea`-Wert. Das ist derselbe Vertrag, ob die Session mit dem On-Device-3B, PCC oder einem Drittanbieter spricht, den du als Swift-Paket eingebunden hast.

## Claude in dieselbe Swift-Session einbringen

Die WWDC-2026-Session „Bring an LLM provider" hat das konkret gemacht: Implementiere `LanguageModel` und `LanguageModelExecutor`, veröffentliche es als git-getaggtes Swift-Paket, und App-Code, der bereits das On-Device-3B nutzt, kann deinen Anbieter aufrufen, indem er einen Initializer ändert.

<PromptCard title="Skizze eines Drittanbieters, der LanguageModel implementiert">
{`import FoundationModels

public struct ClaudeSonnet5: LanguageModel {
    public let capabilities = LanguageModelCapabilities(capabilities: [
        .toolCalling, .guidedGeneration, .reasoning
    ])
    public var executorConfiguration: ClaudeExecutor.Configuration
}

public final class ClaudeExecutor: LanguageModelExecutor {
    public struct Configuration: Hashable {   // hashable = KV-cache key
        public var modelID: String
        public var tokenProvider: TokenProvider   // NOT a plain API key string
    }

    public required init(configuration: Configuration) throws { /* … */ }

    public func respond(
        to request: LanguageModelExecutorGenerationRequest,
        model: Model,
        streamingInto channel: LanguageModelExecutorGenerationChannel
    ) async throws {
        // 1. Map Foundation Models transcript → Anthropic /messages payload
        // 2. Stream deltas back via channel.send(.response(action: .appendText(...)))
        // 3. Surface tool calls / reasoning via the same transcript entry types
    }
}
`}
</PromptCard>

Zwei Feinheiten, zu denen die API dich bewusst hindrängt. Erstens: **`Configuration` ist absichtlich `Hashable`**: Das Framework cacht Executors nach Konfiguration, also *gleiche Config = gleicher Executor = erhaltener KV-Cache* über Turns hinweg. Zweitens: Die Leitlinie rät explizit von **plain API-Key-Strings** in Initialisierern ab — bevorzuge einen `TokenProvider` oder einen Sign-in-Flow, persistiere Tokens im Keychain und nutze App Attest für Cloud-Aufrufe, um zu verifizieren, dass das Gerät kein manipulierter Build ist, der deinen Endpunkt von außen trifft.

## Wann was nutzen — eine Entscheidungstabelle für Claude-Builder

<Callout type="tip" items={[
  "Greife zu On-Device AFM 3 Core (3B): Zusammenfassung, Klassifizierung, Entitätsextraktion, On-Device-Rewrites, Tastaturvorschläge — alles, wo ein begrenzter Prompt und getypte Ausgabe einen Netzwerk-Roundtrip schlagen. Kostenlos.",
  "Greife zu AFM 3 Core Advanced (20B): privates Reasoning, das das Gerät nicht verlassen darf (Gesundheit, Finanzen, unveröffentlichte Dokumente) und bei dem das 3B stolpert. Erwarte variable Latenz wegen NAND-Expert-Swap.",
  "Greife zu Private Cloud Compute (Cloud / Cloud Pro): größerer Kontext, längeres Reasoning oder multimodal, wo das 20B On-Device nicht mithält, aber der Datenschutz Drittanbieter-APIs weiterhin ausschließt.",
  "Bleibe bei Claude über die Anthropic-API (oder den neuen Swift-Paket-Anbieter): lange Agent-Läufe, Extended Thinking, Big-Context-Codebase-Arbeit und plattformübergreifende Apps, die dasselbe Modell auf Android und Web brauchen. Siehe die Foundations-Seite Claude vs. andere.",
  "Wickle das On-Device-3B NICHT in eine JSON-Repair-Schleife: Es hat constrained decoding. Wenn deine Ausgabe nicht parst, ist das Schema falsch oder du hast @Generable übersprungen.",
]} />

## Querverweise

- On-Device ist das Gegengewicht zur Local-Agent-Story, die wir in [Lokale KI-Agenten](/docs/models/local-ai-agents) und [Modelle lokal mit Ollama ausführen](/docs/models/run-models-locally-ollama) behandeln. Apples Blickwinkel ist anders: Es ist *Erstpartei-OS-Integration*, nicht ein separater lokaler Server.
- Wenn du zwischen Apples On-Device-Modell und dem parallelen Betrieb von Claude wählst, liegen die Designmuster in [Claude + lokale Modelle: hybride Muster](/docs/models/claude-plus-local-models) und [Lokaler oder Claude-Agent](/docs/models/local-or-claude-agent).
- Für ein breiteres Framing von „wie unterscheiden sich diese Modelle und welches wähle ich", siehe [Ein Modell auswählen](/docs/models/choosing-a-model) und [Prompts über Modelle hinweg portieren](/docs/models/porting-prompts-across-models).
- Sicherheitsimplikationen des Ausliefern eines lokal + Cloud-Hybrids leben in [Lokale und hybride Agenten absichern](/docs/models/securing-local-and-hybrid-agents).

## Kurzer Check

<Quiz questions={[
  {
    q: "Wie bringt AFM 3 Core Advanced ein 20B-Parameter-Modell auf ein iPhone?",
    options: [
      "Es führt eine geschrumpfte, destillierte 3B-Version aus und nennt sie fürs Marketing 20B",
      "Es speichert die vollen Gewichte im NAND-Flash und tauscht nur eingabeabhängige routed experts in den DRAM, aktiviert 1–4B Parameter pro Prompt",
      "Es streamt jedes Token von Private Cloud Compute und cacht lokal",
      "Es nutzt eine neue 1-Bit-Quantisierung, die das Modell auf den RAM schrumpft"
    ],
    answer: 1,
    explain: "AFM 3 Core Advanced ist ein sparse MoE. Das volle Modell lebt im Flash; ein Router wählt Experten pro Prompt und nur diese werden in den DRAM getauscht. Deshalb liegt die Zahl aktiver Parameter bei 1–4B pro Prompt, nicht 20B."
  },
  {
    q: "Was gibt dir das @Generable-Makro, was handgemachtes JSON-Prompting nicht gibt?",
    options: [
      "Eine Retry-Schleife, die malformed JSON automatisch repariert",
      "Einen hübscheren Ausdruck der Rohausgabe des Modells",
      "Compile-Zeit-Schema-Erzeugung plus Token-Level-Constrained-Decoding, sodass die Ausgabe garantiert in deinen Swift-Typ parst",
      "Einen Geschwindigkeitsschub durch Umgehen des Tokenizers"
    ],
    answer: 2,
    explain: "@Generable ist ein Compile-Zeit-Makro, das Schema und Parser erzeugt. Das Framework beschränkt dann das Decoding auf Token-Ebene, sodass das Modell nur Tokens ausgeben kann, die den laufenden Text als deinen Typ parsebar halten."
  },
  {
    q: "Du willst Claude Sonnet 5 aus einer iOS-App aufrufen, die bereits das Foundation Models Framework für On-Device-Zusammenfassung nutzt. Was ist der WWDC26-native Weg?",
    options: [
      "Schreibe die App um das Anthropic-SDK herum neu — das Framework unterstützt nur Apple-Modelle",
      "Füge ein Swift-Paket hinzu, das sich an LanguageModel + LanguageModelExecutor hält, dann tausche das Modell der Session in einer Zeile",
      "Proxy jeden Claude-Aufruf durch Private Cloud Compute",
      "Konvertiere Claudes Gewichte in ein Core-ML-Paket und liefere sie im App-Bundle aus"
    ],
    answer: 1,
    explain: "Genau das hat die WWDC26-Session „Bring an LLM provider\" ausgeliefert. Anthropic Claude und Google Gemini wurden als Launch-Time-Beispiele für Drittanbieter genannt, die durch dieselbe Session-API exponiert werden."
  },
  {
    q: "Welche Entwicklergruppe bekommt Private-Cloud-Compute-Aufrufe kostenlos aus dem Framework?",
    options: [
      "Jedes Free-Tier-Konto des Apple Developer Program",
      "Mitglieder des App Store Small Business Program mit weniger als 2 Millionen Erstdownloads",
      "Nur Apples eigene Erstpartei-Apps",
      "Jeder mit einer Enterprise-Developer-Program-Mitgliedschaft"
    ],
    answer: 1,
    explain: "Apples eigene Entwicklerseite dokumentiert die Free-Cloud-Cost-Stufe für Mitglieder des App Store Small Business Program mit weniger als 2 Millionen Erstdownloads. Alle anderen zahlen Standard-PCC-Preise."
  },
  {
    q: "Warum muss der Executor-`Configuration`-Typ `Hashable` sein?",
    options: [
      "Damit er über Swift-Concurrency-Grenzen gesendet werden kann",
      "Damit das Framework Executors nach Konfiguration cachen kann — gleiche Config hält den KV-Cache über Turns hinweg warm",
      "Um den Compile-Zeit-Schema-Generator zufriedenzustellen",
      "Weil Swift 6 verlangt, dass alle Protokolltypen Hashable sind"
    ],
    answer: 1,
    explain: "Das Framework nutzt die Configuration als Hashable-Lookup-Key für Executor-Instanzen. Gleiche Konfiguration → gleicher Executor → erhaltener KV-Cache. Andere Konfiguration → getrennter Cache."
  }
]} />

## Quellen & weiterführende Literatur

- [Introducing the third generation of Apple's foundation models](https://machinelearning.apple.com/research/introducing-third-generation-of-apple-foundation-models) — Apple Machine Learning Research (8. Juni 2026): AFM 3 Core / Core Advanced / Cloud / Cloud Pro-Architekturen, NAND-plus-Expert-Swap-Beschreibung, 25 Sprachen, Quantization Aware Training, PT-MoE-Server-Architektur.
- [Bring an LLM provider to the Foundation Models framework — WWDC26 session 339](https://developer.apple.com/videos/play/wwdc2026/339/) — das `LanguageModel` / `LanguageModelExecutor`-Protokolldesign, Streaming-/Transcript-Modell, benutzerdefinierte Segmente, Drittanbieter.
- [Apple Intelligence for developers](https://developer.apple.com/apple-intelligence/) — Überblick über das Foundation Models Framework, dynamische Profile, Evaluierung und die App-Store-Small-Business-Notiz zur kostenlosen PCC-Preisgestaltung.
- [Meet the Foundation Models framework — WWDC25 session 286](https://developer.apple.com/videos/play/wwdc2025/286/) und [Deep dive into the Foundation Models framework — WWDC25 session 301](https://developer.apple.com/videos/play/wwdc2025/301/) — ursprüngliche `@Generable` / `@Guide` / Tool-Calling-APIs, auf denen WWDC26 aufbaut.
- [Apple Intelligence Foundation Language Models: Technical Report (arXiv 2507.13575)](https://arxiv.org/pdf/2507.13575) — technische Details früherer Generationen, nützlich, um die On-Device-Architektur-Story zu erden.
