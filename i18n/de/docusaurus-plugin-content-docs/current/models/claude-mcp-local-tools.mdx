---
sidebar_position: 14
title: "Claude mit lokalen Tools & Agenten über MCP verbinden"
description: "Das Model Context Protocol lässt Claude Tools, Daten und Agenten orchestrieren, die privat auf deiner eigenen Maschine laufen — die Synergie aus Claude-als-Gehirn und lokalen Fähigkeiten. Füge einen lokalen MCP-Server hinzu oder baue selbst einen."
keywords: [MCP, Model Context Protocol, lokaler MCP-Server, lokale Claude-Tools, Claude Agenten-Synergie, MCP-Server bauen]
---

<LevelBadge level="intermediate" />

Du willst, dass Claude das **Gehirn** ist und deine Maschine die **Hände** liefert — deine Dateien lesen, deine Datenbank abfragen, dein Skript ausführen, sogar ein lokales Modell oder einen lokalen Agenten aufrufen, alles ohne dass deine Daten den Laptop verlassen. Für diese Synergie gibt es bereits einen Standard: das **Model Context Protocol (MCP)**. In einem Atemzug: MCP ist ein offener Standard, der einem KI-Client erlaubt, externe Tools und Daten über kleine Programme namens **MCP-Server** aufzurufen — und diese Server können privat, lokal, auf deiner eigenen Maschine laufen. Diese Seite zeigt, was MCP ist, wie die lokale Architektur zusammenpasst, wie du Claude einen lokalen Server **hinzufügst** und wie du selbst einen winzigen **baust**.

<Callout type="objectives" items={[
  "MCP in einem Satz erklären — ein offener Standard, damit ein KI-Client externe Tools und Daten über MCP-Server aufrufen kann",
  "Die lokale Architektur sehen: ein MCP-Client (Claude Code / Claude Desktop), der über stdio mit einem lokalen MCP-Server spricht",
  "Verstehen, warum genau DAS die Synergie aus Claude-als-Gehirn + lokalen Fähigkeiten ist, die du willst",
  "Claude mit einem einzigen Befehl einen lokalen MCP-Server hinzufügen",
  "Einen winzigen MCP-Server bauen, der ein Tool bereitstellt",
  "Die Vertrauensgrenze kennen — MCP-Server handeln mit DEINEN Rechten, also installiere nur, was du vertraust",
]} />

## Was MCP ist (in einem Atemzug)

**MCP ist ein offener Standard zur Verbindung von KI-Anwendungen mit externen Systemen.** Ein KI-Client (wie Claude) spricht ein Protokoll; jeder **MCP-Server**, der es spricht, kann sich einklinken und dem Client neue Fähigkeiten anbieten. Die offizielle Analogie ist ein **USB-C-Anschluss für KI**: statt einer maßgeschneiderten Integration pro Tool baust du für einen Anschluss, und er funktioniert überall.

Anthropic hat MCP eingeführt und als Open Source veröffentlicht, und vom ersten Tag an wurde es mit **lokaler MCP-Server-Unterstützung** ausgeliefert, sodass Claude sich mit internen Systemen und Datensätzen verbinden konnte, die auf deiner eigenen Maschine laufen.

<VerifyNote lastVerified="2026-06-28" source="https://modelcontextprotocol.io/">
Adoptionszahlen, das Revisionsdatum der Spezifikation, die Governance (die Verwaltung von MCP) und die genaue Liste der Clients, die MCP unterstützen, ändern sich häufig. Behandle jede konkrete Zahl als verderblich und bestätige sie unter <a href="https://modelcontextprotocol.io/">modelcontextprotocol.io</a> und in Anthropics Ankündigung. Die dauerhaften Fakten — MCP ist ein offener Standard, Client ↔ Server, Server können lokal laufen, breit über Clients hinweg adoptiert — sind stabil.
</VerifyNote>

Ein MCP-Server kann dem Client drei Arten von Dingen bereitstellen:

- **Tools** — Aktionen, die das Modell aufrufen kann (eine Datei lesen, eine Abfrage ausführen, eine API aufrufen). Das hat dieselbe Form wie die native [Tool-Nutzung](/docs/api/tool-use) des Modells: eine benannte Fähigkeit mit typisierten Eingaben.
- **Resources** — Daten, die der Client lesen kann (Dateien, Datensätze, Dokumente).
- **Prompts** — wiederverwendbare, parametrisierte Prompt-Vorlagen, die der Server anbietet.

Für diese Seite lautet die Schlagzeile **Tools**: Ein lokaler MCP-Server ist die Art, wie du Claude eine neue, private Fähigkeit übergibst.

## Warum das die Synergie ist, die du willst

Was die Leute mit „Claude als Gehirn, mein Computer als Körper“ meinen, ist genau die MCP-Client/Server-Aufteilung:

- **Claude ist der Orchestrator.** Es liest dein Ziel, entscheidet, welches Tool aufzurufen ist, ruft es auf, liest das Ergebnis und entscheidet über den nächsten Schritt.
- **Lokale MCP-Server sind die Fähigkeiten.** Jeder ist ein kleines Programm, das *du* auf *deiner* Maschine ausführst: ein Dateisystem-Server, ein Datenbank-Server, ein Wrapper um ein lokales Skript, sogar ein Server, der ein [lokales Modell](/docs/models/local-ai-agents) aufruft oder Arbeit an einen lokalen Agenten übergibt.

Weil der Server lokal läuft und Claude über die standardmäßige Ein-/Ausgabe deiner eigenen Maschine mit ihm spricht, **bleiben deine Daten und Aktionen auf deiner Hardware** — das Modell orchestriert, aber das Lesen von Dateien, das SQL, die Skript-Ausführung passieren lokal. Das ist die datenschutzwahrende Version agentischer Arbeit: Gehirn in der Cloud (oder auch lokal), Hände strikt auf deiner Kiste.

Und weil MCP **ein offener Standard ist, der über viele Clients hinweg adoptiert wurde**, ist ein Server, den du baust, nicht an eine einzige App gebunden. Derselbe lokale Server, den du in [Claude Code](/docs/claude-code/what-is-claude-code) einbindest, kann von anderen MCP-sprechenden Clients genutzt werden — einmal bauen, überall wiederverwenden.

## Die lokale Architektur

Das kleinste nützliche Bild hat zwei Teile und einen Kanal:

- **MCP-Client (Host).** Die KI-App — zum Beispiel [Claude Code](/docs/claude-code/what-is-claude-code) oder Claude Desktop. Sie hält das Modell und entscheidet, welche Tools aufzurufen sind.
- **MCP-Server.** Ein separater Prozess, der Tools/Resources/Prompts anbietet und sie auf Anforderung ausführt.
- **Transport.** Bei lokalen Servern startet der Client den Server als Subprozess, und sie sprechen über **stdio** (Standard-Ein-/Ausgabe) mit JSON-RPC-Nachrichten. Kein Netzwerk, kein Port — nur eine lokale Pipe zwischen zwei Prozessen auf deiner Maschine. (Es gibt auch Remote-Server, über HTTP; diese Seite behandelt den lokalen stdio-Fall.)

```
┌─────────────────────────┐         stdio (JSON-RPC)        ┌──────────────────────────┐
│  MCP CLIENT / HOST       │ ──── launches as subprocess ──▶ │  LOCAL MCP SERVER         │
│  Claude Code / Desktop   │ ◀── tools/resources/prompts ─── │  filesystem · db · script │
│  (the model = the brain) │                                 │  · local model / agent    │
└─────────────────────────┘                                  └──────────────────────────┘
                                                              runs on YOUR machine, YOUR data
```

Das Modell berührt deine Festplatte nie direkt — es fragt den lokalen Server, und der Server (der mit deinen Berechtigungen läuft) erledigt die Arbeit und gibt ein Ergebnis zurück.

## Claude einen lokalen MCP-Server hinzufügen

Der schnellste Weg ist der Befehl `claude mcp add` von Claude Code, der einen Server registriert, den der Client über stdio startet. Die allgemeine Form ist ein Name, dann der Befehl, der den Server startet.

<Steps items={[
  {title: "Wähle einen Server, dem du vertraust", body: "Beginne mit einem offiziellen Referenz-Server (z. B. dem Dateisystem-Server), damit das Verhalten bekannt ist. Anthropic pflegt eine Sammlung von Referenz-MCP-Servern, die du lokal ausführen kannst — filesystem, git, memory, fetch und mehr."},
  {title: "Registriere ihn beim Client", body: "Verwende `claude mcp add <name> -- <command...>`. Der Teil nach `--` ist genau der Befehl, den Claude Code ausführt, um den Server als lokalen Subprozess zu starten; es spricht mit ihm über stdio."},
  {title: "Bestätige, dass er geladen ist", body: "Führe `claude mcp list` aus, um registrierte Server zu sehen, starte dann eine Sitzung und frage Claude, welche Tools es jetzt hat. Die Tools des Servers sollten als aufrufbare Fähigkeiten erscheinen."},
  {title: "Nutze ihn in einer Aufgabe", body: "Gib Claude ein Ziel, das die neue Fähigkeit braucht (\"lies die Dateien unter ./notes und fasse sie zusammen\"). Claude ruft die Tools des Servers auf; die Arbeit passiert lokal."},
]} />

Ein minimales `claude mcp add` für einen lokalen Dateisystem-Server (alles nach `--` ist der Startbefehl):

<PromptCard title="Einen lokalen Dateisystem-MCP-Server zu Claude Code hinzufügen">{`claude mcp add filesystem -- npx -y @modelcontextprotocol/server-filesystem /path/to/allowed/dir`}</PromptCard>

Wenn du die Konfiguration lieber direkt bearbeitest, wird ein stdio-Server durch den Befehl deklariert, der ihn startet. Die Form sieht so aus:

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/dir"]
    }
  }
}
```

<VerifyNote lastVerified="2026-06-28" source="https://modelcontextprotocol.io/">
Die genauen `claude mcp`-Unterbefehle, Konfigurationsbereiche, der Speicherort der Konfigurationsdatei und die präzisen Paketnamen der Referenz-Server entwickeln sich weiter. Bestätige die aktuelle Syntax im <a href="/docs/claude-code/mcp">Claude Code MCP-Leitfaden</a> und im Repo der Referenz-Server, bevor du dich auf ein bestimmtes Flag oder einen Paketnamen verlässt.
</VerifyNote>

## Einen winzigen MCP-Server bauen

Ein Server ist einfach ein Programm, das ein MCP-SDK verwendet, ein Tool deklariert und einen stdio-Transport startet. Hier ist die ganze Idee in TypeScript — ein Tool, das zwei Zahlen addiert (tausche den Rumpf gegen „führe mein Skript aus“, „frage meine DB ab“, „rufe mein lokales Modell auf“):

```bash
npm install @modelcontextprotocol/sdk zod
```

```ts
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import { z } from "zod";

const server = new McpServer({ name: "my-local-tools", version: "0.1.0" });

// Declare ONE tool. Inputs are typed; the body runs locally with your privileges.
server.registerTool(
  "add",
  {
    description: "Add two numbers",
    inputSchema: { a: z.number(), b: z.number() },
  },
  async ({ a, b }) => ({
    content: [{ type: "text", text: String(a + b) }],
  }),
);

// Talk to the client over stdio — this is what makes it a LOCAL server.
await server.connect(new StdioServerTransport());
```

Registriere ihn bei Claude genau wie jeden anderen stdio-Server — richte den Startbefehl auf deine Datei aus:

<PromptCard title="Deinen eigenen lokalen Server zu Claude Code hinzufügen">{`claude mcp add my-local-tools -- node /abs/path/to/server.js`}</PromptCard>

Das ist die vollständige Schleife: Claude (Gehirn) sieht ein neues Tool, ruft es bei Bedarf auf, und dein Code (Hände) läuft lokal und gibt das Ergebnis zurück. Weil er dasselbe offene Protokoll spricht, funktioniert dieser Server auch ohne Änderungen in anderen MCP-Clients.

<Callout type="warning" items={["MCP-Server laufen mit deinen Rechten und können echte Aktionen ausführen — installiere nur Server, denen du vertraust, und achte auf Prompt-Injection über Tool-Ergebnisse."]} />

## Überprüfe dich selbst

<Quiz title="Überprüfe dich selbst" questions={[{q: "Was ist MCP, in einem Satz?", options: ["Ein neues Claude-Modell", "Ein offener Standard, der einem KI-Client erlaubt, externe Tools und Daten über MCP-Server aufzurufen", "Eine reine Cloud-Datenbank für Prompts"], answer: 1, explain: "MCP ist ein offener Standard, damit eine KI-Anwendung (der Client) sich über MCP-Server, die Tools, Resources und Prompts bereitstellen, mit externen Systemen verbinden kann."}, {q: "Wie spricht der Client bei einem LOKALEN MCP-Server üblicherweise mit ihm?", options: ["Über einen öffentlichen HTTPS-Endpunkt", "Indem er ihn als Subprozess startet und JSON-RPC über stdio austauscht", "Indem er deine Dateien zu Anthropic hochlädt"], answer: 1, explain: "Ein lokaler Server wird vom Client als Subprozess gestartet; sie kommunizieren über stdio (Standard-Ein-/Ausgabe) — kein Netzwerk, die Daten bleiben auf deiner Maschine."}, {q: "Warum zahlt sich der Bau eines MCP-Servers über Tools hinweg aus?", options: ["Weil jeder Client sein eigenes maßgeschneidertes Protokoll braucht", "Weil MCP ein offener Standard ist, der von vielen Clients adoptiert wurde, sodass ein Server, den du baust, über sie hinweg funktioniert", "Weil Server nur in Claude Desktop laufen"], answer: 1, explain: "MCP ist breit über Clients hinweg adoptiert. Baue einen Server einmal nach dem offenen Standard, und mehrere MCP-sprechende Clients können ihn nutzen."}, {q: "Was ist der Hauptgrund, einen Server vor der Installation zu prüfen?", options: ["Er könnte langsam sein", "MCP-Server laufen mit deinen Rechten und können echte Aktionen ausführen, außerdem können Tool-Ergebnisse Prompt-Injection enthalten", "Server kosten Geld pro Aufruf"], answer: 1, explain: "Ein lokaler MCP-Server wird mit deinen Berechtigungen ausgeführt und kann auf deiner Maschine handeln; nicht vertrauenswürdige Server — oder bösartige Inhalte, die als Tool-Ergebnis zurückgegeben werden — sind ein echtes Risiko."}]} />

<Flashcards cards={[{front: "MCP, in einem Atemzug?", back: "Ein offener Standard, damit ein KI-Client (Claude) externe Tools und Daten über MCP-Server aufrufen kann."}, {front: "Was läuft wo im lokalen Setup?", back: "Das Modell/Gehirn ist der MCP-Client; die Fähigkeit ist ein lokaler MCP-Server — ein kleines Programm auf DEINER Maschine, das der Client startet und mit dem er über stdio spricht."}, {front: "Was kann ein Server bereitstellen?", back: "Tools (aufrufbare Aktionen), Resources (lesbare Daten) und Prompts (wiederverwendbare Vorlagen). Tools sind die Schlagzeile, um Claude eine neue lokale Fähigkeit zu geben."}, {front: "Warum ist das die Synergie aus 'Claude-als-Gehirn + lokale-Hände'?", back: "Claude orchestriert; der lokale Server erledigt das Lesen von Dateien / SQL / Skript / lokale Modellaufrufe auf deiner Hardware, sodass deine Daten und Aktionen auf deiner Maschine bleiben."}, {front: "Einen lokalen Server zu Claude Code hinzufügen?", back: "claude mcp add <name> -- <command to launch the server> — der Befehl nach -- wird als lokaler Subprozess ausgeführt und über stdio angesprochen."}, {front: "Einen bauen — die drei Teile?", back: "Verwende ein MCP-SDK, registerTool(...) mit einem typisierten Eingabeschema, dann verbinde einen StdioServerTransport. Der Tool-Rumpf ist dein lokaler Code."}, {front: "Einmal bauen, wo wiederverwenden?", back: "Über jeden MCP-sprechenden Client hinweg — MCP ist breit adoptiert, sodass ein Server nicht an eine App gebunden ist."}, {front: "Die Vertrauensregel?", back: "Server laufen mit deinen Rechten und können echte Aktionen ausführen; installiere nur welche, denen du vertraust, und hüte dich vor Prompt-Injection, die über Tool-Ergebnisse eintrifft."}]} />

<Callout type="takeaways" items={["MCP ist ein offener Standard, damit ein KI-Client externe Tools und Daten über MCP-Server aufrufen kann — Anthropic hat es von Anfang an mit lokaler Server-Unterstützung eingeführt.", "Die lokale Architektur ist einfach: ein MCP-Client (Claude Code / Desktop) startet einen lokalen MCP-Server als Subprozess und spricht mit ihm über stdio — deine Daten bleiben auf deiner Maschine.", "Das ist die Synergie aus Claude-als-Gehirn + lokalen Fähigkeiten: Claude orchestriert; lokale Server (Dateisystem, DB, ein Skript, sogar ein lokales Modell oder ein Agent) liefern die Hände.", "Füge einen Server mit einem einzigen Befehl hinzu (claude mcp add ... -- <Startbefehl>); baue einen mit einem MCP-SDK, indem du ein Tool registrierst und einen stdio-Transport verbindest.", "MCP ist breit adoptiert, sodass ein lokaler Server, den du baust, über viele Clients hinweg funktioniert — einmal bauen, überall wiederverwenden.", "Server handeln mit deinen Rechten: installiere nur, was du vertraust, und behandle Tool-Ergebnisse als möglichen Prompt-Injection-Vektor."]} />

## Weiter

- MCP täglich vom Terminal aus nutzen → [Claude Code: MCP](/docs/claude-code/mcp)
- Was der Orchestrator ist → [Was ist Claude Code](/docs/claude-code/what-is-claude-code)
- Die Form des Tool-Aufrufs, der MCP-Tools folgen → [Tool-Nutzung](/docs/api/tool-use)
- Das lokale Modell oder den Agenten ausführen, das/den ein Server umhüllen kann → [Lokale KI-Agenten](/docs/models/local-ai-agents)

## Quellen & weiterführende Literatur

- [Model Context Protocol — offizielle Website](https://modelcontextprotocol.io/)
- [MCP-Architektur & Konzepte](https://modelcontextprotocol.io/docs/learn/architecture)
- [Einen MCP-Server bauen (offizieller Leitfaden)](https://modelcontextprotocol.io/docs/develop/build-server)
- [Einführung des Model Context Protocol — Anthropic](https://www.anthropic.com/news/model-context-protocol)
- [Referenz-MCP-Server (filesystem, git, fetch, …)](https://github.com/modelcontextprotocol/servers)
- [Model Context Protocol — Wikipedia (Überblick & Adoption)](https://en.wikipedia.org/wiki/Model_Context_Protocol)
