---
sidebar_position: 11.4
title: "Cloudflares security-audit-Skill: Adversariale KI-Sicherheitsaudits mit Claude Code (2026)"
description: "cloudflare/security-audit-skill ist diese Woche das Repo mit den meisten Sternen auf GitHub (18,5k Sterne, +2,4k an einem Tag am 21. September 2026). Es ist kein Scanner: Es ist eine SKILL.md, die einen Coding-Agenten in eine sechsphasige Audit-Pipeline verwandelt, in der Hunter ihre eigenen Funde niemals verifizieren können, jeder Kandidat einen frischen Verifier bekommt, der ihn zu widerlegen versucht, und nur sandboxed, quellenbasierte Ergebnisse in einer schemavalidierten findings.json landen. Wie die Phasen funktionieren, was die drei Verdikte bedeuten, warum Prompt Injection allein kein Finding ist, was es kostet (HN-Nutzer verbrannten 150k bis 1M Token) und wie man es ohne diese Rechnungen betreibt."
keywords: [security-audit-skill, Cloudflare security audit skill, Claude Code security audit, AI security audit, vulnerability discovery harness, findings.json, coverage ledger, adversarial validation, agent skills security, npx skills add, SKILL.md, hunters and verifiers, needs_validation, prompt injection finding, AI code audit cost, Semgrep vs AI audit, Grant Bourzikas]
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-09-21" source="github.com/cloudflare/security-audit-skill (README; skills/security-audit/SKILL.md, RECONNAISSANCE.md, HUNTING.md, VALIDATION-AND-REPORTING.md, AI-AND-LLM.md, report-schema.json; 18.5k stars and +2,428 on GitHub Trending on Sep 21, 2026; MIT); Cloudflare blog 'Build your own vulnerability harness' (June 18, 2026, Grant Bourzikas); Hacker News item 49736466 (Sep 17, 2026, 211 points); github.com/vercel-labs/skills. Token figures are individual HN reports, not a benchmark. The skill is actively edited (14 commits on main at the time of writing): re-check phase names and file names against SKILL.md." />

<Callout type="objectives" items={[
  "Erklären, warum 'bitte finde Schwachstellen' selbstbewussten Müll produziert und was die Gewaltenteilung zwischen Huntern und Verifiern behebt",
  "Die sechs Phasen durchgehen und das Artefakt benennen, das jede erzeugt, von architecture.md bis NEEDS-VALIDATION.md",
  "Eine findings.json richtig lesen: was confirmed, needs_validation und rejected jeweils verlangen und warum nur confirmed-Einträge eine Schwere bekommen",
  "Den Skill in Claude Code in einer Sandbox ausführen, die seine Regeln erfüllt, die Ausgaben mechanisch validieren und für mehr Abdeckung erneut laufen lassen",
  "Budgetieren: wie die Token-Rechnung aussieht, die Regel, die den Skill Hunter verweigern lässt, und die drei Wege, einen Lauf bezahlbar zu halten"
]} />

Cloudflare hat Mitte September 2026 einen einzelnen Skill als Open Source veröffentlicht, und er wurde zum am schnellsten wachsenden Repo auf GitHub: **[cloudflare/security-audit-skill](https://github.com/cloudflare/security-audit-skill)** stand am 21. September bei 18,5k Sternen, nachdem es an einem Tag 2,4k dazugewonnen hatte, und hatte bereits am 17. September einen [Hacker-News-Thread](https://news.ycombinator.com/item?id=49736466) mit 211 Punkten ausgelöst. Der Grund, warum das wichtig ist, ist nicht die Zahl der Sterne. Es ist, dass dieser Skill der *Keim* des flottenweiten Schwachstellen-Harness ist, den Cloudflare im Juni beschrieben hat — jener, der über 128 Repositories lief und 7.245 umsetzbare Funde hervorbrachte. Der öffentliche Skill ist die Einzel-Repo-Version, die du heute mit Claude Code ausführen kannst.

Es lohnt sich, präzise zu sein, was er ist. Es gibt kein Binary, keine Rules Engine, kein eigenes Modell. Es ist eine `SKILL.md` plus vierzehn Hunting-Leitfäden und zwei abhängigkeitsfreie Node-Validatoren. Alles, was er tut, tut er, indem er *deinen* Agenten dazu bringt, isolierte Sub-Agenten mit engen Aufträgen zu starten, und deren Ausgabe erst akzeptiert, nachdem ein anderer Agent versucht hat, sie zu zerlegen.

## Warum "finde die Schwachstellen" für sich genommen scheitert

Cloudflares Juni-Artikel enthält den Satz, der das ganze Design erklärt: Ein unbeschränkter Agent "will edit the source code so its own exploit works, then triumphantly report the bug it just created." Und die begleitende Regel: "If a Hunter is allowed to grade its own homework, it will confidently validate everything it outputs."

Das ist der Fehlermodus jedes One-Shot-Prompts vom Typ "audit my repo". Das Modell, das 80k Token darauf verwendet hat, eine Theorie zu bilden, ist das letzte Modell, das diese Theorie beurteilen sollte. Die Antwort des Skills ist strukturell:

- **Hunter können keine Findings einreichen.** Sie liefern *Kandidaten* zurück und dürfen weder den Ziel-Quellcode noch die Architektur-Notizen, das Ledger oder die Findings-Datei bearbeiten.
- **Verifier dürfen nicht jagen.** Ein frischer Verifier erhält einen Kandidaten, die Architekturfakten und das Schema — und sonst nichts, nicht einmal die Schlussfolgerung eines anderen Verifiers. Seine Aufgabe ist es, zu widerlegen.
- **Nichts wird behalten, wenn deterministischer Code es nicht akzeptiert.** Der Eltern-Agent schreibt die gemeinsamen Dateien; `validate-findings.cjs` und `validate-coverage-ledger.cjs` weisen alles zurück, was das Schema verletzt.

Cloudflares interne Version geht weiter und verwendet für die Validierung ein *anderes Modell* als für die Entdeckung — mit der Begründung, dass getrennte Gewichte eine unvoreingenommene Zweitmeinung ergeben. Der öffentliche Skill erzwingt das nicht, aber nichts hindert dich daran, die Verifier-Sub-Agenten an ein anderes Modell zu routen.

## Die sechs Phasen und was jede hinterlässt

| Phase | Wer läuft | Ausgabe |
|---|---|---|
| 1. Reconnaissance | 4 parallele Recherche-Agenten (Stack und Build; Prinzipale und Auth; Eintrittsflächen und Sinks; was offline testbar ist), die nur Fakten zurückliefern | `architecture.md` (rund 1.000 Wörter) und ein initialisiertes `coverage-ledger.json` |
| 2. Abdeckungsgeführtes Hunting | Wellen isolierter Hunter, je eine oder mehrere Ledger-*Units*; ein Coverage-Kritiker nach jeder Welle | Kandidaten mit `proposed_verdict`, Unit-Dispositionen, Härtungsnotizen, neu entdeckte Lücken |
| 3. Kandidatenvalidierung | Ein frischer Verifier pro eindeutigem Kandidaten-Fingerprint | Eine Entscheidung pro Kandidat: confirmed, needs_validation oder rejected |
| 4. Strukturierte Ausgabe | Nur der Eltern-Agent | `findings.json`, nach Fingerprint sortiert, schemavalidiert |
| 5. Unabhängige Datensatzverifikation | Ein frischer Verifier pro confirmed- oder needs_validation-Eintrag | `verified` oder `replace`; jede materielle Ersetzung bekommt einen *zweiten* frischen Verifier |
| 6. Zielneutrale Berichterstattung | Der Eltern-Agent, nachdem Phase 5 bestanden ist | `REPORT.md`, `FINDINGS-DETAIL.md` (nur ab medium aufwärts), `NEEDS-VALIDATION.md` |

Alles landet unter `~/security-audit-skill/<repo-name>/run-<N>/`, mit Verzeichnissen `agents/<id>/scratch/` und `agents/<id>/artifacts/` pro Agent. Nur der Eltern-Agent schreibt die gemeinsamen Dateien.

Das **Coverage-Ledger** ist der Mechanismus, den die meisten übersehen. Eine *Unit* ist eine materielle Kombination aus Eintrittsfläche, Vertrauensgrenze, Subsystem und Angriffsklasse, mit Startpfaden im Repo. Units durchlaufen `planned`, `in_progress`, `blocked`, `covered` oder `candidate`, und jeder Zustand hat Regeln dazu, welche Evidenzfelder ausgefüllt sein müssen. Ein **Coverage-Kritiker** liest nach einer Welle das gesamte Ledger und sucht nach nicht erfassten Eintrittspunkten, ungeprüften Parallelpfaden und unbegründeten Ausschlüssen; er kann eine Unit wieder öffnen (und den vorherigen Versuch archivieren), aber niemals ein Finding vorschlagen. Genau das macht Wiederholungsläufe *additiv*: Ein zweiter Lauf greift die Units auf, die der erste als `blocked` hinterlassen oder wieder geöffnet hat, statt alles neu zu auditieren.

## Die drei Verdikte — und warum nur eines eine Schwere trägt

Das Schema ist strikt darin, welche Felder jedes Verdikt verwenden darf, und der Validator erzwingt es.

| Verdikt | Bedeutet | Muss haben | Darf nicht haben |
|---|---|---|---|
| `confirmed` | In der Sandbox unabhängig aus dem Quellcode reproduziert, nur minimal beobachteter Effekt | root_cause, intended_behavior, trace (Entrypoint bis Sink), evidence, conditions, execution, remediation, severity, confidence | claimed_root_cause, blockers, validation_plan, reason |
| `needs_validation` | Quellenbasiert, aber blockiert durch genau einen Fakt, der lokal nicht geklärt werden kann (eine Deployment-Einstellung, eine nur dem Eigentümer zugängliche Konfiguration) | claimed_root_cause, trace, evidence, blockers, validation_plan | severity, execution, remediation |
| `rejected` | Der Verifier hat es widerlegt | claimed_root_cause, trace, evidence, reason | severity, execution, remediation, blockers |

Daraus folgen drei Konsequenzen:

- **Ein `needs_validation`-Finding hat per Design keine Schwere.** Es ist kein "medium"; es ist eine Frage an den Code-Eigentümer mit einem exakten Plan, sie zu beantworten. `NEEDS-VALIDATION.md` existiert, damit diese Liste nicht für eine Schwachstellenliste gehalten wird.
- **Schwere verlangt nachgewiesene Auswirkung, kein verfehltes Checklisten-Item.** Die Kalibrierung in `SKILL.md`: critical ist unauthentifizierte Codeausführung, vollständiger Datenzugriff oder Account-Übernahme; high ist die vollständige Aushebelung einer expliziten Kontrolle (Auth-Bypass, mandantenübergreifender Zugriff); medium ist begrenzter Wirkungsradius oder ungewöhnliche Vorbedingungen; low ist Offenlegung nicht geheimer Daten; informational ist eine bestätigte Beobachtung mit minimaler Auswirkung. "Fehlende Best Practice" ist ausdrücklich kein Finding.
- **Ein sauberer Lauf meldet null.** Der Reporting-Leitfaden sagt, ein verhältnismäßiger Bericht darf keine bestätigten Findings enthalten und muss das klar sagen. Wenn dein Lauf in einem kleinen Service zwanzig Criticals "findet", verdächtige zuerst das Sandbox-Setup und nicht den Service.

Für Leserinnen und Leser dieses Abschnitts ist der KI/LLM-Hunting-Leitfaden der schärfste Teil. Seine Eröffnungsregel: **"Prompt injection alone is not a finding. Require a code-level boundary failure."** Ein Hunter muss den Angreifer, den betroffenen Prinzipal, die Ausführungsidentität und eine unautorisierte Aktion oder Offenlegung zeigen — mit einem Quellcode-Trace von der angreiferkontrollierten Eingabe bis zum Seiteneffekt. Die Muster, auf die er Hunter ansetzt, sind die aus unseren Seiten zu [Prompt Injection](/docs/security/prompt-injection) und [MCP Tool Poisoning](/docs/security/mcp-tool-poisoning-and-rug-pulls), aber als *Code*-Defekte formuliert: Tool-Argumente, die ohne handlerseitige Validierung in SQL oder eine Shell gelangen; Sub-Agenten, die volle Zugangsdaten statt minimaler Rechte erben; Tool-Beschreibungen, die als Autorisierung behandelt werden; Modellausgabe, die ohne Kodierung in HTML oder einen Befehl gerendert wird; mandantenübergreifendes Context-Bleed durch zu breite Cache-Keys. Das Modell dazu zu überreden, etwas Unhöfliches zu sagen, zählt nicht.

## Die Sandbox-Regel ist kein Ratschlag

`SKILL.md` verlangt, dass jeglicher zielkontrollierter Code (Tests, Fixtures, der eigene Build des Projekts) in einer betriebssystemseitig erzwungenen Sandbox läuft: ohne externes Netzwerk, höchstens mit einem isolierten Loopback-Namespace, mit schreibgeschütztem Zielquellcode, mit auf `scratch/` beschränkten Schreibzugriffen und mit strikten Grenzen für CPU, Speicher, Prozesse und Dateigröße. Verifiern wird gesagt: Ist die Sandbox nicht verfügbar, bleibt der Kandidat schlicht unvalidiert. Die Beförderung von `scratch/` in behaltene `artifacts/` verweigert Symlinks, mehrfach verlinkte Dateien, Archive und rekursive Kopien und öffnet nichts erneut über den Pfad.

Das ist dasselbe Bedrohungsmodell wie auf unserer Seite [Coding-Agenten unter Angriff](/docs/security/coding-agents-under-attack), nur nach innen gedreht: Das Repository, das du auditierst, kann feindlich sein, und seine Testsuite ist Code, den du gleich ausführst. In Claude Code heißt das, das Audit mit den Kontrollen für [Sandbox und Credential Masking](/docs/claude-code/sandbox-credential-masking) zu fahren und nicht in deiner täglichen Shell mit deinen echten Umgebungsvariablen. Ein Stolperstein aus Cloudflares eigenem Harness gilt, wenn du Docker in einem Container verschachtelst: Es braucht `seccomp=unconfined` und `apparmor=unconfined`, "or it will silently fail."

<Steps items={[
  { title: "Installiere den Skill dort, wo dein Agent ihn sehen kann", body: "npx skills add https://github.com/cloudflare/security-audit-skill --skill security-audit. Die skills-CLI (vercel-labs/skills) legt ihn in .claude/skills/ für Claude Code oder in .agents/skills/ für Codex, Cursor und Gemini CLI ab; -g für eine globale Installation, -a claude-code, um einen bestimmten Agenten anzusprechen. Lies SKILL.md, bevor du ihn ausführst: Es ist der Vertrag, den du gleich ausführst." },
  { title: "Öffne das Repo in einer Sandbox-Sitzung", body: "Starte Claude Code im Ziel-Repo mit aktiviertem Sandboxing und Credential Masking, ohne Produktions-Zugangsdaten in der Umgebung und mit Netzwerk-Egress, der auf deinen Modellanbieter beschränkt ist. Der Skill braucht einen Agenten, der parallele Sub-Agenten starten kann; das Agent-Tool von Claude Code erfüllt das." },
  { title: "Löse den Full-Audit-Modus bewusst aus", body: "Sag 'security audit this codebase' oder 'find security vulnerabilities in ./src'. Eine engere Frage ('is this JWT check safe?') löst stattdessen den Guidance-Modus aus, der antwortet, ohne ein Run-Verzeichnis anzulegen. Richte einen ersten Lauf auf ein abgegrenztes Verzeichnis, nicht auf die Monorepo-Wurzel." },
  { title: "Beobachte das Run-Verzeichnis, nicht den Chat", body: "Alles erscheint unter ~/security-audit-skill/<repo>/run-1/. architecture.md sollte innerhalb von Minuten existieren; coverage-ledger.json wächst Unit für Unit; findings.json erscheint erst nach Phase 4. Ist nach der Reconnaissance nichts gelandet, prüfe, ob der Agent eine Sandbox hat, in der er ausführen kann." },
  { title: "Validiere mechanisch, bevor du Prosa liest", body: "node <skill-dir>/validate-findings.cjs run-1/findings.json und node <skill-dir>/validate-coverage-ledger.cjs run-1/coverage-ledger.json. Der Skill führt diese selbst aus, aber sie selbst auszuführen ist der billigste Weg, einen Lauf zu erwischen, der von einem Sitzungslimit abgeschnitten wurde." },
  { title: "Lies zuerst NEEDS-VALIDATION.md, dann lass für die Abdeckung erneut laufen", body: "Beantworte jeden Blocker (er ist als Frage zu deinem Deployment formuliert) und befördere oder verwirf den Eintrag. Dann lass erneut laufen: Die prior_status-Felder des Ledgers sorgen dafür, dass der zweite Lauf blockierte und wieder geöffnete Units anvisiert, statt von vorn zu beginnen. Cloudflares eigene Daten sagen, dass ein Lauf ungefähr die Hälfte dessen findet, was wiederholte Läufe finden." }
]} />

<PromptCard title="Auslösephrasen und die Validator-Befehle">{`# Full audit (creates ~/security-audit-skill/<repo>/run-N/)
security audit this codebase
find security vulnerabilities in ./src

# Guidance mode (no run directory, no sub-agent fleet)
is the session cookie handling in auth/session.ts safe against fixation?

# After a run: mechanical checks, zero dependencies beyond Node
node .claude/skills/security-audit/validate-findings.cjs ~/security-audit-skill/<repo>/run-1/findings.json
node .claude/skills/security-audit/validate-coverage-ledger.cjs ~/security-audit-skill/<repo>/run-1/coverage-ledger.json

# What the schema hard-limits (validator rejects beyond these)
# findings.json: 5 MiB, 1,000 findings, 64 nesting levels
# coverage-ledger.json: 5 MiB, 10,000 units, 1,000 entries per collection`}</PromptCard>

## Was es kostet und wie man es bezahlbar hält

Im HN-Thread geht es vor allem um die Rechnung. Ein Nutzer berichtete: "I threw 1M tokens for nothing in a medium codebase"; ein anderer setzte die praktische Obergrenze bei 150k Token an, "where you start hitting the dumb zone" nachlassender Aufmerksamkeit. Das sind Anekdoten, keine Messungen, aber sie passen zur Form des Designs: vier Recon-Agenten, eine Welle Hunter, ein Verifier pro Kandidat, ein Kritiker pro Welle, dann ein zweiter Verifier pro überlebendem Eintrag. Cloudflares Flottenzahlen geben den ehrlichen Maßstab: Ein Standard-Repository mit 30.000 Zeilen brauchte **3 bis 4 Stunden** für rund 100 Rohfunde, und über die Flotte hinweg überlebten **58 %** der Rohkandidaten die Validierung (12.057 von 20.799).

Drei Dinge im Skill selbst verhindern, dass er davonläuft:

1. **Die Budgetreserve-Regel.** Vor jeder Welle muss der Eltern-Agent Budget für die Hunter, den Validierungsdurchgang, den Kritiker nach der Welle und einen abschließenden Clean-Kritiker reservieren. `HUNTING.md` sagt: Wenn diese verpflichtenden Reserven nicht hineinpassen, starte aus dieser Welle **keinen** Hunter und markiere die Units als zurückgestellt, mit dem Budget als Grund. Ein Lauf, der mit zurückgestellten Units endet, ist ein Lauf, der dein Budget respektiert hat — kein kaputter Lauf.
2. **Enge Aufträge.** Jeder Sub-Agent bekommt eine Unit oder einen Kandidaten. Cloudflares Harness hielt jeden Agenten unter 25 % seines Kontextfensters und schreibt dem — mehr als jeder Prompt-Formulierung — die Qualität der Funde zu.
3. **Guidance-Modus.** Die meisten Sicherheitsfragen brauchen die Pipeline nicht. Stell sie direkt, und der Skill antwortet, ohne irgendetwas zu starten.

Auf deiner Seite: Begrenze den ersten Lauf auf ein Verzeichnis; nutze für die Hunter ein Modell mit großem Kontext und günstigen Cache-Reads und für die Verifier ein stärkeres; setze ein [Task-Budget](/docs/api/task-budgets) auf die Sitzung; und behandle einen von einem Sitzungslimit abgeschnittenen Lauf als unvollständig, nicht als gescheitert — denn das Ledger hält genau fest, wo er aufgehört hat.

## Wo er neben Semgrep, CodeQL und KI-Code-Review steht

Er ist kein Ersatz für statische Analyse und versucht das auch nicht. Cloudflares Beitrag enthält ein vielsagendes Detail: Sie banden Semgrep in den Harness ein, und die Agenten "ignored it completely" — sie lasen und führten den Code lieber selbst aus. Statische Werkzeuge liefern deterministische, günstige, wiederholbare Abdeckung bekannter Muster; dieser Skill liefert teure, probabilistische, schlussfolgerungsintensive Abdeckung von Grenzverletzungen, die Pattern-Matcher nicht ausdrücken können — mit einem Validierungsschritt, den statische Werkzeuge nicht haben. Fahre beides. Und verwechsle ihn nicht mit Review zur PR-Zeit: Alibabas [open-code-review](/docs/models/open-code-review-hybrid-ai-reviewer) ist ein Diff-Reviewer, der pro Pull Request in Sekunden läuft; dies ist ein mehrstündiges Audit einer ruhenden Codebasis.

<Flashcards title="Begriffe, die dir im Run-Verzeichnis begegnen" cards={[
  {front: "Unit", back: "Eine Zeile der coverage-ledger.json: eine Eintrittsfläche + Vertrauensgrenze + Subsystem + Angriffsklasse, mit Startpfaden. Der Status durchläuft planned, in_progress, blocked, covered, candidate."},
  {front: "Fingerprint", back: "Ein stabiler Bezeichner, abgeleitet aus der Grundursache im Quellcode: keine Zeilennummern, Wellen-IDs oder Schweregrade, damit Varianten desselben Bugs über Phasen und Läufe hinweg zusammengeführt werden."},
  {front: "Coverage-Kritiker", back: "Ein Agent nach einer Welle, der das gesamte Ledger liest, nicht erfasste Eintrittspunkte und unbegründete Ausschlüsse findet und Units wieder öffnen kann. Er schlägt nie Findings vor."},
  {front: "confirmed", back: "Lokal aus dem Quellcode reproduziert, nur minimal beobachteter Effekt, mit trace, conditions, execution, remediation, severity und confidence."},
  {front: "needs_validation", back: "Quellenbasiert, aber blockiert durch genau einen Fakt, der lokal nicht geklärt werden kann. Hat einen Validierungsplan; hat keine Schwere."},
  {front: "rejected", back: "Ein Verifier hat es widerlegt. Bleibt mit der Begründung in findings.json, damit der nächste Lauf es nicht erneut entdeckt."},
  {front: "Promotion", back: "Der Schritt des Eltern-Agenten, eine Datei aus dem scratch/ eines Agenten in behaltene artifacts/ zu verschieben: nur reguläre Dateien, einfach verlinkt, größenbegrenzt, keine Symlinks, keine Archive, nie erneut über den Pfad geöffnet."},
  {front: "Guidance-Modus", back: "Die Antwort des Skills auf eine gezielte Frage: kein Run-Verzeichnis, keine Sub-Agenten. Der Full-Audit-Modus startet nur auf eine ausdrückliche Audit-Anfrage hin."}
]} />

<Quiz title="Teste dich selbst" questions={[
  {q: "Ein Hunter ist überzeugt, einen Auth-Bypass gefunden zu haben. Was darf er damit tun?", options: ["Ihn in findings.json schreiben", "Den Code patchen, um ihn zu beweisen", "Ihn als Kandidat mit einem Fingerprint zurückliefern; ein anderer Agent wird versuchen, ihn zu widerlegen", "Ihn als confirmed mit Schwere high markieren"], answer: 2, explain: "Hunter liefern ausschließlich Kandidaten. Sie dürfen weder den Ziel-Quellcode noch gemeinsame Dateien bearbeiten, und Verdikte und Schweregrade kommen von frischen Verifiern und vom Eltern-Agenten."},
  {q: "Ein Eintrag in findings.json hat das Verdikt needs_validation und die Schwere high. Was ist passiert?", options: ["Es ist ein gültiges high-Finding", "Der Validator hätte es zurückweisen müssen: needs_validation-Einträge dürfen keine Schwere tragen", "Es wurde von einem Kritiker befördert", "Schwere ist überall optional"], answer: 1, explain: "Das Schema verbietet severity, execution und remediation bei needs_validation-Einträgen. Nur confirmed-Einträge werden bewertet."},
  {q: "Deine Testsuite macht ausgehende HTTP-Aufrufe. Was sagt der Skill Huntern und Verifiern, was zu tun ist?", options: ["Erlaube es, Tests sind vertrauenswürdig", "Führe sie in der betriebssystemseitig erzwungenen Sandbox ohne externes Netzwerk aus; ist diese Sandbox nicht verfügbar, bleibt der Kandidat unvalidiert", "Ersetze das Netzwerk durch einen Mock", "Überspringe die Tests"], answer: 1, explain: "Zielkontrollierter Code läuft nur innerhalb der genehmigten Sandbox mit deaktiviertem Netzwerk. Verifier kontaktieren niemals deployte Endpunkte oder externe Dienste."},
  {q: "Ein Hunter zeigt, dass eine präparierte E-Mail den Assistenten dazu bringt, etwas Anstößiges zu sagen. Nach AI-AND-LLM.md ist das:", options: ["Critical", "High", "Kein Finding: Prompt Injection allein braucht eine Grenzverletzung auf Code-Ebene mit einem beobachtbaren unautorisierten Ergebnis", "needs_validation"], answer: 2, explain: "Der Leitfaden verlangt eine überschrittene Grenze: Angreifer, betroffener Prinzipal, Ausführungsidentität, unautorisierte Aktion oder Offenlegung, mit einem Quellcode-Trace. Modellausgabe allein qualifiziert sich nicht."},
  {q: "Der Lauf endet mit mehreren Units, die als zurückgestellt und mit Budgetgrund markiert sind. Was bedeutet das?", options: ["Der Skill ist abgestürzt", "Der Eltern-Agent hat die Reserveregel angewandt: Er startet keine Hunter, die er nicht auch validieren und kritisieren kann", "Diese Units sind sicher", "Das Repo ist zu groß"], answer: 1, explain: "Passen die verpflichtenden Reserven für Validierung und Kritiker nicht hinein, startet aus dieser Welle kein Hunter. Ein zweiter Lauf greift diese Units auf."},
  {q: "Cloudflares Flottendaten sagen, dass ein einzelner Lauf ungefähr welchen Anteil dessen findet, was wiederholte Läufe finden?", options: ["Etwa 95 %", "Etwa 50 %", "Etwa 10 %", "Alles, wenn das Modell groß genug ist"], answer: 1, explain: "Ihr Juni-Beitrag nennt ungefähr die Hälfte, wobei einfachere Bugs zuerst auftauchen. Das additive Ledger ist genau dafür gebaut: Lass erneut laufen, und es zielt auf die Lücken."}
]} />

## Quellen & weiterführende Literatur

- [cloudflare/security-audit-skill auf GitHub](https://github.com/cloudflare/security-audit-skill) — README, `SKILL.md`, die Phasenleitfäden (`RECONNAISSANCE.md`, `HUNTING.md`, `VALIDATION-AND-REPORTING.md`), die vierzehn Angriffsklassen-Leitfäden einschließlich `AI-AND-LLM.md`, `report-schema.json` und die beiden Validatoren; MIT
- [Build your own vulnerability harness](https://blog.cloudflare.com/build-your-own-vulnerability-harness/) — Cloudflare-Blog, 18. Juni 2026 (Grant Bourzikas): wie der Skill zum Flotten-Harness heranwuchs, der Trichter von 20.799 über 12.057 zu 7.245, die Zahl von 3–4 Stunden pro 30k-Zeilen-Repo, die "grade its own homework"-Regel sowie die Stolpersteine mit verschachteltem Docker und "200 OK mit Fehler"
- [Hacker-News-Diskussion (item 49736466)](https://news.ycombinator.com/item?id=49736466) — Token-Berichte aus erster Hand und der Widerspruch zu den Kosten
- [vercel-labs/skills](https://github.com/vercel-labs/skills) — die `npx skills add`-CLI und wo sie Skills für jeden Agenten installiert
- [Offener Standard für Agent Skills](/docs/models/skill-md-open-standard) — warum eine einzige `SKILL.md` in Claude Code, Codex, Cursor und Gemini CLI läuft

## Weiter

- [Agent-Skills prüfen, die du installierst](/docs/security/vetting-agent-skills) — dieser Skill startet Agenten, die den Code deines Repos ausführen; lies das, bevor du irgendeinem Skill vertraust, auch diesem
- [Autonome Läufe härten](/docs/security/hardening-autonomous-runs) — das Sandbox-, Secret- und Egress-Setup, das ein mehrstündiges unbeaufsichtigtes Audit braucht
- [Open Code Review: Alibabas hybrider KI-Reviewer](/docs/models/open-code-review-hybrid-ai-reviewer) — das Gegenstück zur PR-Zeit und dieselbe Lektion zu Precision versus Recall
