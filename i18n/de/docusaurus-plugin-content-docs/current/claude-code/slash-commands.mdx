---
sidebar_position: 7
title: "Eigene Slash-Befehle"
description: "Verwandle einen wiederkehrenden Arbeitsablauf in ein einziges Wort."
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://code.claude.com/docs/en/slash-commands">
Speicherorte von Befehlsdateien und Frontmatter-Optionen können sich ändern — prüfe es in der offiziellen slash-commands-Dokumentation.
</VerifyNote>

Ein eigener Slash-Befehl verpackt einen Prompt, den du immer wieder neu eintippst, in ein einziges `/wort`. Einmal definieren, für immer wiederverwenden (und mit deinem Team teilen).

<Callout type="objectives" items={["Erklären, was ein eigener Slash-Befehl ist und warum er besser ist als das erneute Eintippen eines Prompts", "Einen Befehl erstellen, indem du eine Markdown-Datei im richtigen Verzeichnis ablegst", "Projekt-Befehle von persönlichen Befehlen anhand ihres Speicherorts unterscheiden", "Die Bausteine nutzen: $ARGUMENTS, eingebettete Bash-Ausgabe, Dateiverweise und Frontmatter", "Für eine gegebene Aufgabe zwischen Slash-Befehl, Skill und Subagent wählen"]} />

## Wie sie funktionieren

Ein Befehl ist einfach eine Markdown-Datei. Lege sie in deinem Befehlsverzeichnis ab, und ihr Dateiname wird zum Befehl. Der Inhalt der Datei ist der Prompt, den Claude ausführt, wenn du sie aufrufst.

Der *Speicherort* der Datei bestimmt ihren Gültigkeitsbereich:

<Steps items={[{title: "Projekt-Befehl (teilbar)", body: "Speichere ihn unter .claude/commands/changelog.md → /changelog. Er liegt im Repo, also bekommt ihn das gesamte Team."}, {title: "Persönlicher Befehl (alle Projekte)", body: "Speichere ihn unter ~/.claude/commands/changelog.md → /changelog. Er folgt dir über jedes Projekt auf deinem Rechner hinweg."}, {title: "Rufe ihn auf", body: "Tippe das /wort in Claude Code ein. Der Inhalt der Datei wird zum Prompt, den Claude ausführt."}]} />

<Callout type="tip" items={["Der Dateiname — nicht das Frontmatter — wird zum Befehlsnamen. changelog.md ist immer /changelog."]} />

## Ein minimales Beispiel

Hier ist eine vollständige Befehlsdatei. Das Frontmatter enthält die `description`; alles darunter ist der Prompt.

<PromptCard title="commit.md — einen Conventional Commit entwerfen">{`---
description: Draft a Conventional Commit message from the staged diff
---

Look at the staged changes with \`git diff --cached\` and write a single
Conventional Commit message (feat/fix/docs/refactor/chore). Output only the
message, no preamble.`}</PromptCard>

Speichere das als `commit.md`, und `/commit` erledigt es jedes Mal. (Nenne es so wie die Datei — derselbe Inhalt als `changelog.md` gespeichert wäre `/changelog`.)

## Die nützlichen Bausteine

Vier Zutaten machen aus einem statischen Prompt einen flexiblen Befehl:

| Baustein | Was er tut |
| --- | --- |
| **`$ARGUMENTS`** | Fügt ein, was der Nutzer nach dem Befehl eingetippt hat. `/review src/auth.ts` → der Pfad landet in deinem Prompt. |
| **Bash ausführen und Ausgabe einbetten** | Befehle können Shell-Ausgabe enthalten (z. B. das aktuelle Diff), damit Claude auf dem aktuellen Stand handelt. |
| **Dateien referenzieren** | Verweise auf Dateien, um deren Inhalt einzubinden. |
| **Frontmatter** | `description` (im Menü angezeigt) und weitere Optionen. |

Die genaue Syntax für Argumente, Bash und Dateiverweise findest du in der [offiziellen Dokumentation](https://code.claude.com/docs/en/slash-commands).

## Befehle, die es wert sind, übernommen zu werden

Ein Starter-Set findest du in unserer [Slash-Befehl-Bibliothek](/docs/templates/slash-commands): Commit-Nachricht, PR-Beschreibung, Code-Review, Changelog, Komponente scaffolden.

## Befehl vs. Skill vs. Subagent

Drei Wege, Arbeit zu verpacken — der Unterschied liegt darin, *wer sie auslöst* und *wie isoliert sie läuft*:

- **Slash-Befehl** — *du* löst einen bekannten Arbeitsablauf bei Bedarf aus.
- **[Skill](/docs/claude-code/skills)** — Claude lädt Expertise *automatisch*, wenn sie relevant ist.
- **[Subagent](/docs/claude-code/subagents)** — delegiere einen Teil der Arbeit an einen isolierten Agenten.

<Flashcards cards={[{front: "Slash-Befehl", back: "Du löst einen bekannten Arbeitsablauf bei Bedarf aus, indem du /wort eintippst."}, {front: "Skill", back: "Claude lädt Expertise automatisch, wenn sie relevant ist — du rufst sie nicht auf."}, {front: "Subagent", back: "Delegiert einen Teil der Arbeit an einen isolierten Agenten."}, {front: ".claude/commands/x.md", back: "Projekt-Befehl /x — liegt im Repo, mit dem Team teilbar."}, {front: "~/.claude/commands/x.md", back: "Persönlicher Befehl /x — folgt dir über alle Projekte."}, {front: "$ARGUMENTS", back: "Fügt das, was der Nutzer nach dem Befehl eingetippt hat, in den Prompt ein."}]} />

<Quiz title="Teste dich selbst" questions={[{q: "Wo speicherst du einen Slash-Befehl, damit dein gesamtes Team ihn im Repo bekommt?", options: [".claude/commands/", "~/.claude/commands/", "Irgendwo im Repo-Wurzelverzeichnis"], answer: 0, explain: ".claude/commands/changelog.md → /changelog ist ein Projekt-Befehl — er liegt im Repo und ist teilbar. Der ~/.claude/-Pfad ist persönlich, für alle deine Projekte."}, {q: "Was bestimmt den Namen eines Slash-Befehls?", options: ["Die description im Frontmatter", "Der Dateiname der Markdown-Datei", "Die erste Überschrift im Inhalt"], answer: 1, explain: "Der Dateiname wird zum Befehl: changelog.md → /changelog. Der Inhalt ist der Prompt, den Claude ausführt."}, {q: "Was macht $ARGUMENTS in einem Befehl?", options: ["Listet alle verfügbaren Befehle auf", "Fügt das, was der Nutzer nach dem Befehl eingetippt hat, in den Prompt ein", "Führt ein Bash-Skript aus"], answer: 1, explain: "$ARGUMENTS fügt ein, was der Nutzer nach dem Befehl eingetippt hat — z. B. /review src/auth.ts landet den Pfad in deinem Prompt."}, {q: "Welche Verpackungsoption lädt Claude automatisch, wenn sie relevant ist, statt dass du sie auslöst?", options: ["Slash-Befehl", "Skill", "Subagent"], answer: 1, explain: "Ein Skill lädt Expertise automatisch, wenn sie relevant ist. Ein Slash-Befehl wird von dir bei Bedarf ausgelöst; ein Subagent delegiert Arbeit an einen isolierten Agenten."}]} />

## Weiter

- [Skills: Expertise auf Abruf](/docs/claude-code/skills)
- [Hooks: Deterministische Automatisierung](/docs/claude-code/hooks)
- [Slash-Befehl-Bibliothek](/docs/templates/slash-commands)
