---
sidebar_position: 6
title: "Frameworks open source pour agents IA"
description: "Une carte neutre vis-à-vis des fournisseurs des principaux frameworks ouverts pour construire des agents LLM — LangGraph, LlamaIndex, AutoGen, CrewAI et l'approche à boucle minimale — et comment choisir."
keywords: [frameworks agents IA, LangChain, LangGraph, AutoGen, CrewAI, LlamaIndex, agents open source]
---

<LevelBadge level="advanced" />

Une fois que vous avez construit quelques agents à la main, la même plomberie revient sans cesse : une boucle qui appelle le modèle, exécute l'outil qu'il a demandé, réinjecte le résultat, et s'arrête quand le travail est terminé. Les **frameworks d'agents** empaquettent cette plomberie — plus l'état, la mémoire et la coordination multi-agents — pour que vous écriviez moins de code de liaison. Cette page est une carte durable et neutre vis-à-vis des fournisseurs des principales options **open source**, de ce qu'elles vous apportent réellement, et de comment choisir. Presque toutes sont **agnostiques au modèle** : elles fonctionnent avec Claude, GPT, Gemini et des modèles locaux à poids ouverts.

<Callout type="objectives" items={[
  "Savoir ce qu'un framework d'agent vous apporte par rapport à une boucle codée à la main — et ce qu'il ne vous apporte pas",
  "Reconnaître les trois archétypes : graphes avec état, équipes basées sur les rôles, et boucles minimales",
  "Utiliser une procédure reproductible pour en choisir un — en arbitrant entre complexité et contrôle",
  "Se rappeler qu'ils sont pour la plupart agnostiques au modèle — le framework vous enferme rarement chez un seul fournisseur",
]} />

<VerifyNote lastVerified="2026-06-28" source="https://github.com/langchain-ai/langgraph">
Le paysage des frameworks évolue vite : nombre d'étoiles, numéros de version, API par défaut, et même **quel projet est activement maintenu** changent souvent. AutoGen, par exemple, est entré en mode maintenance et oriente les nouveaux utilisateurs vers un successeur. Traitez chaque affirmation précise ci-dessous comme périssable et confirmez-la sur le dépôt/la doc du projet lui-même (liens dans les Sources) avant de miser un projet dessus.
</VerifyNote>

## Ce qu'un framework d'agent vous apporte réellement

Débarrassé de son branding, un framework vous offre un sous-ensemble de quatre choses :

- **Orchestration** — la boucle de contrôle. Étapes séquentielles, branches, réessais, boucles-jusqu'à-terminé, et (de plus en plus) une **exécution durable** qui survit à un crash et reprend là où elle s'était arrêtée.
- **Outils** — une manière standard de déclarer une fonction que le modèle peut appeler, de valider ses arguments, de l'exécuter et de renvoyer le résultat. La même boucle décrire→appeler→exécuter→renvoyer que vous connaissez déjà avec l'[utilisation d'outils](/docs/api/tool-use).
- **Mémoire et état** — un endroit pour conserver l'historique de conversation, les faits de brouillon et les documents récupérés d'un tour à l'autre, pour que l'agent ne soit pas amnésique entre les étapes.
- **Multi-agent** — des schémas pour plusieurs agents spécialisés qui se transmettent le travail : un superviseur déléguant à des exécutants, ou des pairs collaborant sur une tâche. (Conceptuellement la même idée que les [sous-agents](/docs/claude-code/subagents) de Claude Code.)

Un framework en vaut la peine quand vous devriez sinon écrire et maintenir à la main les quatre. Il n'en vaut *pas* la peine quand votre tâche est « appeler le modèle, exécuter un ou deux outils, renvoyer une réponse » — là, le framework est une surcharge contre laquelle vous passerez du temps à lutter.

## Les trois archétypes

Les frameworks diffèrent moins que leur marketing ne le suggère. Il n'existe vraiment que trois formes, et la plupart des projets sont une variation de l'une d'elles :

- **Graphe avec état / workflow.** Vous modélisez l'agent comme un graphe explicite de nœuds et d'arêtes (ou d'étapes et de transitions). Contrôle maximal, état inspectable, adapté aux flux de longue durée et à ceux avec humain dans la boucle. Plus à apprendre au départ. → **LangGraph**, **LlamaIndex Workflows**.
- **Équipe basée sur les rôles.** Vous décrivez les agents par *rôle* (« chercheur », « rédacteur », « relecteur ») et les laissez collaborer ou exécuter un processus. Rapide pour exprimer une équipe multi-agents ; vous échangez un peu de contrôle fin contre l'abstraction de haut niveau. → **CrewAI**, et le style conversationnel multi-agents d'**AutoGen**.
- **Boucle minimale / peu d'abstractions.** Une fine couche au-dessus de l'appel d'outils natif du modèle, avec des transferts entre quelques agents et pas grand-chose d'autre. Facile à lire de bout en bout, facile à abandonner. → **OpenAI Agents SDK** (le successeur de production de l'expérimental **Swarm**), et la **boucle simple que vous écrivez vous-même**.

<Callout type="tip" items={[
  "Commencez par la chose la plus simple qui fonctionne — souvent une simple boucle d'appel d'outils bat un framework lourd.",
]} />

## Un tour d'horizon rapide (vérifiez le positionnement avant de vous engager)

Voici les projets ouverts à connaître. Ils ne sont nommés que parce que le vrai dépôt de chacun a été vérifié ; tout ce qui est volatil vit derrière la VerifyNote ci-dessus.

- **LangGraph** — un framework d'orchestration bas niveau pour des agents avec état et de longue durée, modélisés comme des graphes ; l'exécution durable et l'humain dans la boucle sont de première classe. Utilisable seul ou avec l'écosystème plus large **LangChain**. Agnostique au modèle. <VerifyNote lastVerified="2026-06-28" source="https://github.com/langchain-ai/langgraph" />
- **LlamaIndex** — a démarré comme un framework de données/RAG (connecteurs, index, récupération) et propose désormais aussi une couche **Workflows** pilotée par événements pour les agents. Solide quand votre agent est fondamentalement *une récupération sur vos documents*. <VerifyNote lastVerified="2026-06-28" source="https://github.com/run-llama/llama_index" />
- **Microsoft AutoGen** — un framework pour systèmes conversationnels multi-agents. À la mi-2026, il est en **mode maintenance** ; Microsoft oriente les nouveaux projets vers un successeur unifié (**Microsoft Agent Framework**, fusionnant AutoGen + Semantic Kernel). Vérifiez le statut actuel avant de commencer. <VerifyNote lastVerified="2026-06-28" source="https://github.com/microsoft/autogen" />
- **CrewAI** — un framework basé sur les rôles : définissez les agents par rôle et objectif, organisez-les en **Crews** (collaboration autonome) ou en **Flows** (contrôle piloté par événements). Voie rapide vers une équipe multi-agents. <VerifyNote lastVerified="2026-06-28" source="https://github.com/crewAIInc/crewAI" />
- **OpenAI Agents SDK** — un framework délibérément léger, à peu d'abstractions, pour les workflows multi-agents avec transferts. Malgré son nom, il est **agnostique au fournisseur** (sa doc note la prise en charge de plus de 100 LLM), et c'est le successeur prêt pour la production de l'expérimental **Swarm**. <VerifyNote lastVerified="2026-06-28" source="https://github.com/openai/openai-agents-python" />
- **La boucle d'agent simple** — pas de framework du tout : votre propre boucle `while` autour de l'appel d'outils natif du modèle. Le bon choix par défaut pour les agents simples, et ce que tous les frameworks ci-dessus enveloppent en fin de compte.

## Comment choisir un framework

<Steps items={[
  {title: "Écrivez la tâche et ses contraintes dures", body: "Une phrase sur ce que fait l'agent, plus les non-négociables : latence, plafond de coût, confidentialité des données, si les exécutions doivent survivre à un crash, et si un humain doit approuver des étapes."},
  {title: "Demandez-vous si vous avez besoin d'un framework du tout", body: "Si c'est un agent appelant un ou deux outils, écrivez une boucle simple. Beaucoup d'agents en production n'ont jamais besoin de plus. N'adoptez un framework que lorsque vous devriez sinon réimplémenter vous-même l'orchestration, la mémoire ET le multi-agent."},
  {title: "Faites correspondre la forme au travail", body: "Longue durée / humain dans la boucle / état inspectable → un graphe avec état (LangGraph, LlamaIndex Workflows). Une équipe de spécialistes → une équipe basée sur les rôles (CrewAI). Quelques agents qui se transmettent le travail, en restant simple → une boucle minimale (OpenAI Agents SDK, ou la vôtre)."},
  {title: "Vérifiez qu'il est vivant et agnostique au modèle", body: "Confirmez que le dépôt est activement maintenu (pas en mode maintenance) et qu'il prend proprement en charge le modèle que vous utilisez réellement — Claude, GPT, Gemini, ou un modèle local. La plupart sont neutres vis-à-vis des fournisseurs ; vérifiez, ne supposez pas."},
  {title: "Prototypez les 20 % les plus risqués", body: "Construisez l'étape unique la plus dure — l'outil délicat, le transfert, la reprise après échec — dans deux candidats. L'abstraction qui rend cette étape lisible est votre gagnante."},
  {title: "Gardez une sortie de secours", body: "Gardez vos outils et prompts sous forme de simples fonctions et chaînes, pour que le framework enveloppe votre logique plutôt que de la posséder. Changer de framework devrait vouloir dire recâbler l'orchestration, pas tout réécrire."},
]} />

## La chose que tout framework enveloppe

Avant de saisir la moindre bibliothèque, il aide de voir la boucle sur laquelle elles sont toutes bâties. C'est toute l'idée — le modèle décide, vous exécutez, on répète jusqu'à terminé :

```python
# Provider-neutral agent loop — the core every framework wraps.
# `model_call` and `run_tool` are yours; swap in Claude, GPT, Gemini, or a local model.

def agent_loop(task, tools, max_steps=10):
    messages = [{"role": "user", "content": task}]

    for _ in range(max_steps):
        # 1. Ask the model what to do next (it sees the tool schemas).
        response = model_call(messages, tools=tools)

        # 2. No tool requested → the model is done. Return its answer.
        if not response.tool_calls:
            return response.text

        # 3. Run each requested tool and feed results back in.
        messages.append(response.as_message())
        for call in response.tool_calls:
            result = run_tool(call.name, call.arguments)
            messages.append({
                "role": "tool",
                "tool_call_id": call.id,
                "content": result,
            })

    return "Stopped: hit max_steps without finishing."
```

Si vous savez lire ceci, vous comprenez ce que fait chaque framework de cette page sous le capot. Les graphes ajoutent l'état et le branchement explicites ; les équipes ajoutent les rôles et la délégation ; les SDK minimaux ajoutent des transferts soignés — mais le battement de cœur est toujours cette boucle.

<PromptCard title="Prompt système d'agent minimal (à associer à la boucle ci-dessus)">{`You are a task-completing agent with access to tools.

Loop:
1. Think briefly about the next single step toward the goal.
2. If a tool would help, call exactly ONE tool with valid arguments.
3. When you have enough to answer, stop calling tools and give the final answer.

Rules:
- Prefer the fewest tool calls that get the job done.
- If a tool fails, read the error and adjust — do not repeat the same call.
- Never invent tool results; use only what tools actually returned.
- If the goal is impossible with the available tools, say so and stop.

Goal: {one-sentence task}`}</PromptCard>

## Une note sur le battage médiatique

Aucun framework ici n'est « le meilleur » — cette question est mal posée. Les frameworks à graphe l'emportent sur le contrôle et la durabilité ; les frameworks à équipe l'emportent sur l'expression rapide d'une équipe ; les minimaux l'emportent sur la lisibilité et le faible verrouillage ; et une boucle simple l'emporte plus souvent que les README des frameworks ne l'admettent. Le bon choix est le plus petit outil qui rend claire *votre* étape la plus dure. Comme pour le choix d'un modèle, laissez votre propre tâche — pas un nombre d'étoiles — décider. La même discipline que vous appliqueriez avec les [évaluations](/docs/power-user/evals) s'applique ici : prototypez, mesurez sur des cas réels, gardez une sortie de secours.

<Quiz title="Testez-vous" questions={[
  {q: "Votre agent fait un appel au modèle, exécute un seul outil de recherche, et renvoie une réponse. Quel est le bon point de départ ?", options: ["Adopter le plus gros framework multi-agents pour être prêt à passer à l'échelle", "Écrire une simple boucle d'appel d'outils", "Utiliser une équipe basée sur les rôles avec des agents chercheur et rédacteur"], answer: 1, explain: "Pour un agent et un ou deux outils, une boucle écrite à la main est la plus simple et n'a aucun verrouillage. Ne saisissez un framework que lorsque vous devriez sinon réimplémenter vous-même l'orchestration, la mémoire et la coordination multi-agents."},
  {q: "Vous avez besoin d'un agent qui s'exécute longtemps, doit survivre à un crash et reprendre, et laisse un humain approuver certaines étapes. Quel archétype convient le mieux ?", options: ["Une boucle minimale à peu d'abstractions", "Un framework de graphe avec état / workflow", "Celui qui a le plus d'étoiles GitHub"], answer: 1, explain: "L'exécution durable, l'état inspectable et l'humain dans la boucle sont exactement ce pour quoi l'archétype de graphe avec état (p. ex. LangGraph, LlamaIndex Workflows) est conçu. Le nombre d'étoiles n'est pas un critère de sélection."},
  {q: "Un coéquipier dit « OpenAI Agents SDK veut dire qu'on est enfermés avec GPT ». Est-ce exact ?", options: ["Oui — le nom implique qu'il ne fonctionne qu'avec les modèles OpenAI", "Non — il est agnostique au fournisseur et prend en charge de nombreux modèles, dont Claude, Gemini et les locaux", "Seulement si vous installez aussi LangChain"], answer: 1, explain: "Malgré son nom, la doc du SDK le décrit comme agnostique au fournisseur, prenant en charge plus de 100 LLM. La plupart des frameworks d'agents ouverts sont agnostiques au modèle — vérifiez par projet plutôt que de supposer d'après le nom."},
]} />

<Flashcards title="Frameworks d'agents — idées clés" cards={[
  {front: "Qu'est-ce qu'un framework d'agent vous apporte ?", back: "Un sous-ensemble de : orchestration (la boucle de contrôle), outils (déclarer/valider/exécuter), mémoire et état d'un tour à l'autre, et coordination multi-agents. N'en vaut la peine que lorsque vous devriez sinon construire et maintenir les quatre."},
  {front: "Les trois archétypes", back: "1) Graphe/workflow avec état — nœuds explicites, durable, inspectable (LangGraph, LlamaIndex Workflows). 2) Équipe basée sur les rôles — agents par rôle, rapide à exprimer (CrewAI ; style AutoGen). 3) Boucle minimale — peu d'abstractions, faible verrouillage (OpenAI Agents SDK ; votre propre boucle)."},
  {front: "Sont-ils liés à un seul fournisseur de modèle ?", back: "Pour la plupart non — ils sont agnostiques au modèle et fonctionnent avec Claude, GPT, Gemini et des modèles locaux à poids ouverts. Le OpenAI Agents SDK est agnostique au fournisseur malgré son nom. Vérifiez toujours par projet."},
  {front: "Quand ne devriez-vous PAS utiliser de framework ?", back: "Quand la tâche est un agent appelant un ou deux outils. Une simple boucle d'appel d'outils est plus simple, entièrement lisible et sans verrouillage — et c'est exactement ce que tout framework enveloppe."},
  {front: "Le statut d'AutoGen (à la mi-2026)", back: "En mode maintenance — API stable, correctifs critiques seulement, pas de nouvelles fonctionnalités majeures. Microsoft oriente les nouveaux projets vers un successeur unifié (Microsoft Agent Framework). Vérifiez toujours le statut actuel du dépôt avant d'adopter."},
  {front: "Comment choisir sans battage médiatique", back: "Faites correspondre l'archétype au travail, confirmez que le projet est vivant et agnostique au modèle, prototypez les 20 % les plus risqués dans deux candidats, et gardez outils/prompts sous forme de simples fonctions pour conserver une sortie de secours."},
]} />

<Callout type="takeaways" items={[
  "Un framework d'agent empaquette l'orchestration, les outils, la mémoire et la coordination multi-agents — n'en adoptez un que lorsque vous devriez sinon construire les quatre vous-même.",
  "Trois archétypes couvrent le domaine : graphes avec état (contrôle/durabilité), équipes basées sur les rôles (équipes rapides), et boucles minimales (lisibilité, faible verrouillage).",
  "Presque tous sont agnostiques au modèle — ils tournent sur Claude, GPT, Gemini et des modèles locaux ; vérifiez par projet plutôt que de supposer.",
  "Aucun framework n'est universellement « le meilleur » ; choisissez le plus petit outil qui rend claire VOTRE étape la plus dure, et gardez une sortie de secours.",
  "La simple boucle d'appel d'outils est le choix par défaut honnête — et c'est exactement ce que tout framework enveloppe.",
  "Le statut de maintenance change (p. ex. AutoGen → un successeur) ; confirmez qu'un projet est activement maintenu sur son propre dépôt avant de vous engager.",
]} />

## Sources et lectures complémentaires

- [LangGraph — GitHub](https://github.com/langchain-ai/langgraph) — framework d'orchestration bas niveau pour des agents avec état et de longue durée.
- [Vue d'ensemble de LangGraph — doc LangChain](https://docs.langchain.com/oss/python/langgraph/overview) — concepts, exécution durable et humain dans la boucle.
- [LangChain — GitHub](https://github.com/langchain-ai/langchain) — l'écosystème plus large avec lequel LangGraph s'intègre.
- [LlamaIndex — GitHub](https://github.com/run-llama/llama_index) — framework de données/RAG avec une couche Workflows pilotée par événements pour les agents.
- [Doc LlamaIndex](https://developers.llamaindex.ai/) — RAG, moteurs de requête, agents et Workflows.
- [Microsoft AutoGen — GitHub](https://github.com/microsoft/autogen) — framework conversationnel multi-agents (vérifiez le statut de maintenance).
- [Microsoft Agent Framework — GitHub](https://github.com/microsoft/agent-framework) — le successeur unifié (AutoGen + Semantic Kernel) pour construire et orchestrer des agents.
- [CrewAI — GitHub](https://github.com/crewAIInc/crewAI) — framework multi-agents basé sur les rôles avec Crews et Flows.
- [Doc CrewAI](https://docs.crewai.com/) — agents, tâches, crews, flows et outils.
- [OpenAI Agents SDK — GitHub](https://github.com/openai/openai-agents-python) — framework multi-agents léger et agnostique au fournisseur avec transferts.
- [Doc OpenAI Agents SDK](https://openai.github.io/openai-agents-python/) — le modèle d'agent à abstractions minimales.
- [OpenAI Swarm — GitHub](https://github.com/openai/swarm) — le prédécesseur expérimental que le Agents SDK remplace (éducatif, désormais supplanté).

## Suite

- La façon neutre vis-à-vis des fournisseurs de choisir un modèle → [Choisir un modèle](/docs/models/choosing-a-model)
- La boucle que ces frameworks enveloppent → [Utilisation d'outils](/docs/api/tool-use)
- Le multi-agent à la manière de Claude Code → [Sous-agents](/docs/claude-code/subagents)
- Comment comparer réellement les candidats → [Évaluations](/docs/power-user/evals)
