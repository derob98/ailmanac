---
sidebar_position: 3
title: "Votre premier appel à l'API"
description: "Installez le SDK, authentifiez-vous et effectuez un véritable appel à Claude en Python, TypeScript ou cURL."
keywords: [Anthropic API, Claude API, messages.create, SDK, first call]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<LevelBadge level="beginner" />

<VerifyNote lastVerified="2026-06-20" source="https://docs.anthropic.com/en/api/getting-started">
La forme de l'API ci-dessous est stable, mais les identifiants de modèles et les versions du SDK évoluent. Consultez le [tableau des modèles actuels](/docs/whats-new/models-and-pricing).
</VerifyNote>

L'API permet à *votre* programme de communiquer avec Claude. Trois étapes : obtenir une clé, installer le SDK, envoyer un message.

## 1. Obtenir une clé API

Créez-en une dans la [Console Anthropic](https://console.anthropic.com). Définissez-la ensuite comme variable d'environnement afin qu'elle ne réside jamais dans votre code :

```bash
export ANTHROPIC_API_KEY="sk-ant-..."
```

:::warning Ne committez jamais votre clé
Gardez vos clés dans des variables d'environnement ou un gestionnaire de secrets — jamais dans le contrôle de version. Voir [Sécurité](/docs/security).
:::

## 2. Installer le SDK

<Tabs groupId="lang">
<TabItem value="python" label="Python">

```bash
pip install anthropic
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```bash
npm install @anthropic-ai/sdk
```

</TabItem>
<TabItem value="curl" label="cURL">

Rien à installer — il vous suffit de `curl`.

</TabItem>
</Tabs>

## 3. Effectuer l'appel

Chaque requête est une liste de `messages`. Le modèle répond avec du `content`.

<Tabs groupId="lang">
<TabItem value="python" label="Python">

```python
import anthropic

client = anthropic.Anthropic()  # reads ANTHROPIC_API_KEY from the environment

message = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=1024,
    messages=[
        {"role": "user", "content": "In one sentence, what is an API?"}
    ],
)

print(message.content[0].text)
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```ts
import Anthropic from "@anthropic-ai/sdk";

const client = new Anthropic(); // reads ANTHROPIC_API_KEY from the environment

const message = await client.messages.create({
  model: "claude-sonnet-4-6",
  max_tokens: 1024,
  messages: [
    { role: "user", content: "In one sentence, what is an API?" },
  ],
});

console.log(message.content[0].text);
```

</TabItem>
<TabItem value="curl" label="cURL">

```bash
curl https://api.anthropic.com/v1/messages \
  --header "x-api-key: $ANTHROPIC_API_KEY" \
  --header "anthropic-version: 2023-06-01" \
  --header "content-type: application/json" \
  --data '{
    "model": "claude-sonnet-4-6",
    "max_tokens": 1024,
    "messages": [
      {"role": "user", "content": "In one sentence, what is an API?"}
    ]
  }'
```

</TabItem>
</Tabs>

## Ce qui vient de se passer

- **`model`** — quel Claude utiliser. Ne le codez pas en dur à l'aveugle ; voir [Choisir un modèle](/docs/api/choosing-a-model).
- **`max_tokens`** — une limite sur la longueur de la réponse (en [tokens](/docs/foundations)). Cela ne définit *pas* la fenêtre de contexte.
- **`messages`** — la conversation jusqu'ici. L'API est **sans état** : pour poursuivre une conversation, renvoyez tout l'historique à chaque fois.
- **`system`** (optionnel) — une instruction de haut niveau qui définit le rôle de Claude pour l'appel.

## Suite

- Choisir le bon modèle et estimer le coût → [Choisir un modèle](/docs/api/choosing-a-model) · [Tokens et tarification](/docs/api)
- Diffuser les réponses en streaming et tenir une conversation → [Streaming et multi-tours](/docs/api)
- Laisser Claude appeler vos fonctions → [Utilisation des outils](/docs/api)
- Extraits prêts pour la production → [Extraits d'API](/docs/templates)
