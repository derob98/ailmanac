---
sidebar_position: 7
title: "Extraits d'API pour la production"
description: "Wrappers de requêtes avec relances, streaming et boucles d'outils — Python et TypeScript."
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/api">
Les noms et paramètres des méthodes du SDK évoluent — vérifiez-les dans la référence officielle de l'API. ID de modèles : voir le [tableau des modèles](/docs/whats-new/models-and-pricing).
</VerifyNote>

Des structures éprouvées pour développer sur l'[API](/docs/api/first-call). Adaptez-les et renforcez-les pour votre stack.

## Un wrapper d'appel résilient (relances + backoff)

<Tabs groupId="lang">
<TabItem value="python" label="Python">

```python
import time, random, anthropic
client = anthropic.Anthropic()

def ask(messages, model="claude-sonnet-5", max_tokens=1024, system=None):
    for attempt in range(5):
        try:
            return client.messages.create(
                model=model, max_tokens=max_tokens,
                system=system or anthropic.NOT_GIVEN, messages=messages,
            )
        except (anthropic.RateLimitError, anthropic.APIStatusError) as e:
            if attempt == 4:
                raise
            time.sleep(min(2 ** attempt + random.random(), 30))
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```ts
import Anthropic from "@anthropic-ai/sdk";
const client = new Anthropic();

export async function ask(messages, { model = "claude-sonnet-5", maxTokens = 1024, system } = {}) {
  for (let attempt = 0; attempt < 5; attempt++) {
    try {
      return await client.messages.create({ model, max_tokens: maxTokens, system, messages });
    } catch (e: any) {
      if (attempt === 4 || ![429, 500, 529].includes(e?.status)) throw e;
      await new Promise((r) => setTimeout(r, Math.min(2 ** attempt * 1000 + Math.random() * 1000, 30000)));
    }
  }
}
```

</TabItem>
</Tabs>

(Les SDK relancent aussi les erreurs transitoires par défaut — connaissez le comportement de votre client avant d'empiler vos propres relances. Voir [Erreurs et limites de débit](/docs/api/errors-and-rate-limits).)

## Chat en streaming

```python
with client.messages.stream(model="claude-sonnet-5", max_tokens=1024,
        messages=[{"role": "user", "content": "Hello"}]) as stream:
    for text in stream.text_stream:
        print(text, end="", flush=True)
```

## Boucle d'utilisation d'outils (squelette)

```python
messages = [{"role": "user", "content": "What's the weather in Rome?"}]
while True:
    resp = client.messages.create(model="claude-sonnet-5", max_tokens=1024,
                                  tools=TOOLS, messages=messages)
    if resp.stop_reason != "tool_use":
        break
    messages.append({"role": "assistant", "content": resp.content})
    results = [run_tool(b) for b in resp.content if b.type == "tool_use"]
    messages.append({"role": "user", "content": results})  # tool_result blocks
```

:::warning Renforcement
Limitez les itérations et le coût, validez les entrées des outils et gardez les secrets dans des variables d'environnement. Pour un usage autonome, lisez [Sécuriser les agents](/docs/security/securing-agents).
:::

## Suite

- [Votre premier appel d'API](/docs/api/first-call) · [Utilisation d'outils](/docs/api/tool-use)
- [Construire des agents sur l'API](/docs/api/building-agents)
- [Erreurs, limites de débit et fiabilité](/docs/api/errors-and-rate-limits)
