---
sidebar_position: 6
title: "Modelos de Prompt Reutilizáveis"
description: "Prompts mapeados por tarefa, estruturados em XML, com saída esperada e notas de falha."
---

<LevelBadge level="all" />

Prompts prontos para usar em tarefas comuns. Eles usam [tags XML](/docs/prompting/xml-tags) para uma estrutura limpa e incorporam os hábitos de [Fundamentos de Prompting](/docs/prompting/basics). Preencha as `{chaves}`.

## Resumir (fundamentado)

```text
<instructions>
Summarize the document for {audience} in {N} bullet points. Use ONLY the document;
if something isn't there, say "not stated." Then list any action items.
</instructions>
<document>
{paste}
</document>
```
*Esperado:* tópicos enxutos + itens de ação. *Falha comum:* inventa fatos → a cláusula "ONLY the document" protege contra isso.

## Extrair dados estruturados

```text
Extract the following from the text as JSON: {field1, field2, field3}.
Return ONLY valid JSON, no prose. If a field is missing, use null.
<text>{paste}</text>
```
*Combine com* [saída estruturada](/docs/api/structured-output) para confiabilidade.

## Reescrever / editar

```text
Rewrite the text to be {clearer/shorter/more formal} without changing meaning.
Then list the changes you made as bullets.
<text>{paste}</text>
```

## Revisão de código

```text
Review this diff for correctness bugs, missing edge cases, and security issues.
Report ONLY high-confidence findings as a checklist with file:line. Diff:
<diff>{paste}</diff>
```

## Auxiliar de decisão

```text
I'm deciding between {A} and {B} for {situation}. Build a trade-off table across
the criteria that matter, then recommend one and name the biggest risk.
```

## Meta-prompt (melhorar um prompt)

```text
Here's my prompt: "{prompt}". Ask me the 3 questions that would most improve it,
then rewrite it with sensible defaults.
```

## Wrapper de papel + proteção

```text
You are {role}. {task}.
Constraints: {format, length, tone}. If you're unsure or lack info, say so rather
than guessing.
```

:::tip Torne-os seus
Salve os que você usa como [slash commands](/docs/templates/slash-commands) ou um [Projeto](/docs/claude-app/projects) para nunca mais redigitá-los.
:::

## A seguir

- [Biblioteca de Padrões de Prompt](/docs/prompting/pattern-library)
- [Estruturando Prompts com Tags XML](/docs/prompting/xml-tags)
- [Snippets de API para Produção](/docs/templates/api-snippets)
