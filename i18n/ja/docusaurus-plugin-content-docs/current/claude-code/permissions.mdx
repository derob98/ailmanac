---
sidebar_position: 6
title: "権限と権限モード"
description: "allow/ask/deny のルールとモード——安全性を失わずプロンプトを減らす。"
---

<LevelBadge level="intermediate" />

<VerifyNote lastVerified="2026-06-20" source="https://code.claude.com/docs/en/settings">
正確なルール構文とモード名は進化します。公式の Claude Code の設定／権限ドキュメントで確認してください。
</VerifyNote>

<Callout type="objectives" items={["3 つの権限の判定（allow / ask / deny）の意味", "権限ルールがツールとパターンにどうマッチするか", "4 つの権限モードと、それぞれをいつ使うか", "安全性を失わずプロンプトを減らす、まともな初期 allowlist の作り方", "プロジェクト用と個人用の権限ルールをどこに保存するか"]} />

権限は、**Claude Code が立ち止まってあなたに尋ねることなく何ができるか**を決めます。うまく調整すれば制御を失わず流れに乗れます。下手に調整すれば、すべてをゴム印で承認するか、プロンプトに溺れることになります。

## 3 つの判定

すべての潜在的なアクションは次のいずれかに解決されます：

<Flashcards cards={[{front: "allow", back: "実行する、プロンプトなし。"}, {front: "ask", back: "一時停止してあなたに尋ねる——明示的に許可も拒否もされていないものすべてのデフォルト。"}, {front: "deny", back: "決して、尋ねられても。"}]} />

ルールは通常ツールとパターンにマッチします。例：allow `Bash(npm run test:*)` や deny `Read(./.env)`。

<PromptCard title="ツール + パターンを許可">{`Bash(npm run test:*)`}</PromptCard>

<PromptCard title="ツール + パターンを拒否">{`Read(./.env)`}</PromptCard>

## 権限モード

モードはセッション全体の姿勢を設定します：

| モード | 振る舞い | 使うとき |
|---|---|---|
| **default / ask** | 事前許可されていないものすべてに尋ねる | 日常の作業 |
| **plan** | 読み取り専用。提案するが決して実行しない | 大きい／リスクの高いタスク——[プランモード](/docs/claude-code/plan-mode)を参照 |
| **acceptEdits** | ファイル編集を自動承認 | 信頼できる、よくスコープされた編集セッション |
| **bypass / yolo** | プロンプトを完全にスキップ | サンドボックス／CI のみ——シークレットのあるマシンでは**決して**使わない |

<Callout type="warning" items={["bypass はサンドボックスに属します。実際のマシンですべてのプロンプトをオフにして実行することは、エージェントが触るべきでないものに触る原因になります。使い捨て環境のために取っておいてください。/docs/security/hardening-autonomous-runs の自律実行のハードニングを参照。"]} />

## まともな初期 allowlist

目標：安全で繰り返しの多いものを事前許可し、破壊的なものは **ask** か **deny** に保つこと。

<Steps items={[{title: "安全 + 繰り返しを許可", body: "ファイルの読み取り、テスト／リント／ビルドコマンドの実行、git status/diff。"}, {title: "中リスクの前には尋ねる", body: "依存関係のインストール、プロジェクト外へのファイル書き込み、ネットワーク呼び出し。"}, {title: "破壊的なものを拒否", body: "シークレットファイル（.env、キーファイル）の読み取り、force-push、rm -rf。"}]} />

プロジェクトのルールは [`settings.json`](/docs/claude-code/settings)（共有）に、個人のオーバーライドは `settings.local.json` に保存します。

<Callout type="tip" items={["プロンプトから学ばせましょう：同じ安全なコマンドを数回承認すれば、allowlist に何を追加すべきか正確に分かります——繰り返されるプロンプトを一度きりのルールに変えられます。"]} />

<Callout type="takeaways" items={["3 つの判定：allow（プロンプトなし）、ask（デフォルト——一時停止して確認）、deny（決して）。", "ルールはツールとパターンにマッチする。Bash(npm run test:*) や Read(./.env) のように。", "4 つのモードがセッションの姿勢を設定する：default/ask、plan（読み取り専用）、acceptEdits、bypass/yolo（サンドボックスのみ）。", "allowlist を作る：安全 + 繰り返しを許可、中リスクには尋ねる、破壊的なものを拒否。", "共有ルールは settings.json に、個人のオーバーライドは settings.local.json に。"]} />

<Quiz title="確認しよう" questions={[{q: "明示的に許可も拒否もされていないアクションに対して、デフォルトの 'ask' 判定は何をしますか？", options: ["黙って実行する", "一時停止してあなたに確認を求める", "永久にブロックする"], answer: 1, explain: "ask は明示的に許可／拒否されていないものすべてのデフォルトです——一時停止してあなたに尋ねます。"}, {q: "サンドボックスや CI でのみ安全で、シークレットのあるマシンでは決して使わない権限モードはどれですか？", options: ["plan", "acceptEdits", "bypass / yolo"], answer: 2, explain: "bypass / yolo はプロンプトを完全にスキップします。使い捨て環境のために取っておきましょう——シークレットのあるマシンでは決して使わないこと。"}, {q: "共有のプロジェクト権限ルールと個人のオーバーライドをそれぞれどこに保存すべきですか？", options: ["両方とも settings.local.json に", "プロジェクトのルールは settings.json に、個人のオーバーライドは settings.local.json に", "プロジェクトのルールは settings.local.json に、個人のオーバーライドは settings.json に"], answer: 1, explain: "プロジェクトのルールは settings.json（共有）に、個人のオーバーライドは settings.local.json に入れます。"}, {q: "まともな初期 allowlist で、これらのうち 'deny' リストに属するのはどれですか？", options: ["git status / diff", "rm -rf と .env の読み取り", "テスト／リント／ビルドコマンドの実行"], answer: 1, explain: "破壊的なものを拒否する：シークレットファイル（.env、キーファイル）の読み取り、force-push、rm -rf。"}]} />

## 次へ

- [settings.json：設定システム](/docs/claude-code/settings)
- [フック](/docs/claude-code/hooks) — allow/deny を超えて決定論的にルールを強制する
- [セキュリティと責任ある利用](/docs/security/responsible-use)
