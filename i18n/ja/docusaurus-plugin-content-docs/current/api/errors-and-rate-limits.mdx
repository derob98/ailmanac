---
sidebar_position: 15
title: "エラー、レート制限 & 信頼性"
description: "エラーマップ、バックオフ付きリトライ、レート制限のティア、モデル移行。"
---

<LevelBadge level="intermediate" />

<Callout type="objectives" items={["HTTP エラーマップを読み、リトライすべきステータスと修正すべきステータスを見分ける", "一時的なエラーを、上限付きの指数バックオフとジッターでリトライする", "retry-after、平滑化、バッチ処理、より安価なモデルでレート制限に対処する", "モデルの廃止と移行から自分のコードを切り離す"]} />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/api/errors">
正確なステータスコード、ヘッダー、レート制限のティアは変わります — 公式のエラー/レート制限ドキュメントで確認してください。
</VerifyNote>

本番コードはネットワークサービスと通信するため、失敗を前提にしなければなりません。ここで少し構造を持たせるだけで、不安定な連携と信頼できる連携の差が生まれます。

## エラーマップ

扱うことになる典型的な HTTP ステータス：

| ステータス | 意味 | 対処 |
|---|---|---|
| 400 | 不正なリクエスト | ペイロードを修正；そのままリトライしない |
| 401 | API キーが不正/欠落 | 認証情報を確認 |
| 403 | 許可されていない | アクセス権/権限を確認 |
| 429 | レート制限 | **バックオフしてリトライ**（`retry-after` を尊重） |
| 500/529 | サーバーエラー / 過負荷 | **バックオフ付きでリトライ** |

<Callout type="tip" items={["SDK はこれらを型付きの例外として表面化するので、文字列を解析する代わりにきれいに分岐できます。"]} />

## バックオフ付きリトライ

一時的なエラー（429、5xx）には、上限付きの**指数バックオフ + ジッター**でリトライします：

```python
import time, random
for attempt in range(5):
    try:
        return client.messages.create(...)
    except (RateLimitError, APIStatusError) as e:
        if attempt == 4 or not should_retry(e):
            raise
        time.sleep(min(2 ** attempt + random.random(), 30))
```

<Callout type="warning" items={["多くの SDK は一時的なエラーを自動でリトライします — 自前のリトライを追加する前にクライアントのデフォルトを把握しないと、リトライが二重になることがあります。"]} />

## レート制限

制限はアカウント/ティアごと（1分あたりのリクエスト数とトークン数）に適用されます。制限に達すると、タイミングのヒント付きで **429** が返ります。上限を超えないための戦略：

<Steps items={[{title: "retry-after を尊重する", body: "429 を受け取ったら、レスポンスのタイミングのヒントを読み、その時間だけ待ってからリトライします。"}, {title: "バーストを平滑化する", body: "一度にすべて送るのではなく、リクエストを時間に分散させます。"}, {title: "オフライン作業をバッチ化する", body: "大量・非対話的なジョブをバッチ処理に移します。"}, {title: "より安価なモデルを使う", body: "大量のステップをより安価なモデルに振り分けます — モデルの選び方を参照。"}]} />

大量のステップに適したモデルの選び方については [モデルの選び方](/docs/api/choosing-a-model) を参照してください。

## モデル移行

モデル ID は日付/バージョン付きで、廃止されます。自分を切り離しておきましょう：

- **モデル ID は設定から読む**——あちこちにリテラルを散らさない。
- **廃止を監視する** — [廃止 & 移行ウォッチ](/docs/whats-new/deprecations) と [モデル一覧表](/docs/whats-new/models-and-pricing) を参照。
- モデルを切り替えたら**[評価](/docs/foundations/evals)を再実行する**。

<Callout type="takeaways" items={["400/401/403 はあなたの問題——リクエストか認証情報を修正し、やみくもにリトライしない。429 と 500/529 はリトライ可能。", "一時的なエラーは、上限付きの指数バックオフ + ジッターでリトライする（例：min(2 ** attempt + random(), 30)）。", "429 では：retry-after を尊重し、バーストを平滑化し、オフライン作業をバッチ化し、大量のステップをより安価なモデルに振り分ける。", "モデル ID は設定から読み、廃止を監視し、モデル移行時には評価を再実行する。"]} />

<Quiz title="理解度チェック" questions={[{q: "400 不正なリクエストを受け取りました。どうすべきですか？", options: ["同じペイロードをバックオフしてリトライする", "ペイロードを修正；そのままリトライしない", "API キーをローテーションする", "retry-after を待つ"], answer: 1, explain: "400 はリクエスト自体が不正という意味です——ペイロードを修正してください。同じペイロードのリトライは再び失敗するだけです。"}, {q: "バックオフ付きでリトライすべきステータスはどれですか？", options: ["400 と 401", "403 と 401", "429 と 500/529", "400 と 429"], answer: 2, explain: "429（レート制限）と 500/529（サーバーエラー / 過負荷）は一時的——バックオフしてリトライします。400/401/403 は代わりに修正すべき呼び出し側の問題です。"}, {q: "指数バックオフにジッターを加えるのはなぜですか？", options: ["リトライを正確で予測可能にするため", "待機時間をランダム化して、リトライが一斉に衝突しないようにするため", "retry-after ヘッダーをスキップするため", "トークン使用量を減らすため"], answer: 1, explain: "例では min(2 ** attempt + random.random(), 30) を使っています——ランダムな成分（ジッター）がリトライを分散させ、多数のクライアントが足並みをそろえてリトライするのを防ぎ、上限が待機を有界に保ちます。"}, {q: "推奨されるレート制限戦略では「ない」ものはどれですか？", options: ["retry-after を尊重する", "オフライン作業をバッチ化する", "大量のステップにより安価なモデルを使う", "すべての呼び出し箇所にモデル ID をハードコードする"], answer: 3, explain: "レート制限戦略は：retry-after を尊重する、バーストを平滑化する、オフライン作業をバッチ化する、より安価なモデルを使う、です。モデル ID を設定から読む（ハードコードしない）のは、レート制限ではなくモデル移行の実践です。"}]} />

## 次へ

- [安全性、拒否 & フォールバック](/docs/api/refusals-and-safety)
- [トークン & 料金](/docs/api/tokens-and-pricing)
- [API でエージェントを構築する](/docs/api/building-agents)
