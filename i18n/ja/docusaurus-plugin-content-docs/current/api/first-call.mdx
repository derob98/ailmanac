---
sidebar_position: 3
title: "初めての API 呼び出し"
description: "SDK をインストールし、認証し、Python・TypeScript・cURL で Claude への実際の呼び出しを行います。"
keywords: [Anthropic API, Claude API, messages.create, SDK, first call]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<LevelBadge level="beginner" />

<VerifyNote lastVerified="2026-06-20" source="https://docs.anthropic.com/en/api/getting-started">
以下の API の形は安定していますが、モデル ID と SDK のバージョンは変化します。[現行モデル表](/docs/whats-new/models-and-pricing)を参照してください。
</VerifyNote>

API は、*あなたの*プログラムから Claude と対話できるようにするものです。手順は 3 つ: キーを取得し、SDK をインストールし、メッセージを送信します。

## 1. API キーを取得する

[Anthropic Console](https://console.anthropic.com) で 1 つ作成します。次に、コード内に残らないよう環境変数として設定します:

```bash
export ANTHROPIC_API_KEY="sk-ant-..."
```

:::warning キーを絶対にコミットしない
キーは環境変数またはシークレットマネージャーに保管してください — バージョン管理に入れてはいけません。[セキュリティ](/docs/security)を参照。
:::

## 2. SDK をインストールする

<Tabs groupId="lang">
<TabItem value="python" label="Python">

```bash
pip install anthropic
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```bash
npm install @anthropic-ai/sdk
```

</TabItem>
<TabItem value="curl" label="cURL">

インストールするものはありません — `curl` があれば十分です。

</TabItem>
</Tabs>

## 3. 呼び出しを行う

すべてのリクエストは `messages` のリストです。モデルは `content` で応答します。

<Tabs groupId="lang">
<TabItem value="python" label="Python">

```python
import anthropic

client = anthropic.Anthropic()  # reads ANTHROPIC_API_KEY from the environment

message = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=1024,
    messages=[
        {"role": "user", "content": "In one sentence, what is an API?"}
    ],
)

print(message.content[0].text)
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```ts
import Anthropic from "@anthropic-ai/sdk";

const client = new Anthropic(); // reads ANTHROPIC_API_KEY from the environment

const message = await client.messages.create({
  model: "claude-sonnet-4-6",
  max_tokens: 1024,
  messages: [
    { role: "user", content: "In one sentence, what is an API?" },
  ],
});

console.log(message.content[0].text);
```

</TabItem>
<TabItem value="curl" label="cURL">

```bash
curl https://api.anthropic.com/v1/messages \
  --header "x-api-key: $ANTHROPIC_API_KEY" \
  --header "anthropic-version: 2023-06-01" \
  --header "content-type: application/json" \
  --data '{
    "model": "claude-sonnet-4-6",
    "max_tokens": 1024,
    "messages": [
      {"role": "user", "content": "In one sentence, what is an API?"}
    ]
  }'
```

</TabItem>
</Tabs>

## いま何が起きたのか

- **`model`** — どの Claude を使うか。むやみにハードコードしないこと。[モデルの選び方](/docs/api/choosing-a-model)を参照。
- **`max_tokens`** — 応答の長さの上限（[トークン](/docs/foundations)単位）。コンテキストウィンドウを設定するものでは*ありません*。
- **`messages`** — これまでの会話。API は**ステートレス**です。チャットを継続するには、毎回履歴全体を送り返します。
- **`system`**（任意） — その呼び出しにおける Claude の役割を設定する、トップレベルの指示。

## 次へ

- 適切なモデルを選び、コストを見積もる → [モデルの選び方](/docs/api/choosing-a-model) · [トークン & 料金](/docs/api)
- 応答をストリーミングし、会話を維持する → [ストリーミング & マルチターン](/docs/api)
- Claude にあなたの関数を呼び出させる → [ツールの利用](/docs/api)
- 本番対応のスニペット → [API スニペット](/docs/templates)
