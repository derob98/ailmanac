---
sidebar_position: 14
title: "安全性、拒否とフォールバック"
description: "拒否を優雅に処理し、分類器によるブロックと区別する。"
---

<LevelBadge level="intermediate" />

本番環境では、Claudeが期待通りに答え**られない**(または答え**ない**)ケースをコードが処理しなければなりません。うまくやればユーザーには見えず、下手にやればクラッシュや紛らわしい返答になります。

<Callout type="objectives" items={[
  "モデルの拒否と分類器/安全性ブロックを区別する — そしてなぜそれが重要なのか",
  "拒否をクラッシュや空の返答ではなく、通常の結果として処理する",
  "望まない拒否を減らしつつ、正しい拒否をジェイルブレイクしようとしない",
  "フォールバックを選ぶ:明確化の質問、安全な代替案、または人間へのエスカレーション",
]} />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/api">
停止理由と安全性レスポンスの形式は変わることがあります — 公式APIリファレンスで確認してください。
</VerifyNote>

## 2つの異なるもの

- **モデルの拒否** — Claudeがリクエストを断ります(例:有害だと判断する)。レスポンスがこれを示します(一般には拒否の`stop_reason`/コンテンツで)。エラーではなく通常の結果として扱ってください。
- **分類器/安全性ブロック** — 別個の安全性レイヤーがコンテンツをブロックすることがあります。これはモデルの拒否とは見た目が異なることがあります。

どちらを受け取ったかを知ることで、盲目的に再試行するのではなく、適切に対応できます。

## 優雅に処理する

```python
resp = client.messages.create(...)
if getattr(resp, "stop_reason", None) == "refusal":
    # Don't show a raw/empty result. Offer a safe fallback or a clarifying ask.
    show_user("I can't help with that as asked. Here's what I can do instead…")
else:
    render(resp)
```

## 望まない拒否を減らす

- **正当なコンテキストを加える。** 意図が善意でも、リクエストが機微なものにパターンマッチすることがあります。実際の正当な目的を述べることが役立ちます。
- **具体的に。** 曖昧または尖った言い回しは慎重さを招きます。
- **戦わない。** リクエストが本当に禁止されているなら、拒否は正しい — 優雅な道筋を設計してください。ジェイルブレイクしようとしないこと。

<PromptCard title="最初に正当なコンテキストを加える">{`I am a security engineer at [company] reviewing our own authentication code
before a release. Below is a function we wrote and own.

Identify weaknesses in it and explain how an attacker would exploit each one,
so we can fix them before we ship.

[code]`}</PromptCard>

## フォールバックのパターン

- 行き止まりの代わりに**明確化の質問**。
- **安全な代替案**(「代わりに公開情報を要約できます」)。
- パイプラインでは、信頼度/適格性が低いときは**人間にルーティング**。

<Steps items={[
  {title: "検出せよ、仮定するな", body: "レンダリングする前に、拒否シグナルがないかレスポンスを検査する。未確認のレスポンスをそのままUIに流し込まないこと — それがユーザーに空の箱や生のオブジェクトを見せる原因です。"},
  {title: "分類する", body: "モデルの拒否か、分類器/安全性ブロックか。拒否はリクエストに対する判断で、ブロックは別個の安全性レイヤー。同一入力で再試行してもどちらにも効かないが、指す修正は異なる。"},
  {title: "優雅に劣化させる", body: "行き止まりを、あなたのプロダクトの声で、明確化の質問や安全な代替案に置き換える。ユーザーが得るべきはエラーではなく次の一手。"},
  {title: "利害が正当化するときはエスカレートする", body: "パイプラインでは、低信頼または不適格なケースを推測したり黙って落としたりせず、人間のキューにルーティングする。"},
]} />

<Quiz title="理解度チェック" questions={[
  {q: "コードがClaudeから拒否を受け取りました。正しい扱い方は?", options: ["例外として — キャッチして同じリクエストを再試行する", "通常の結果として — 検出してフォールバックを表示する", "ジェイルブレイクすべきプロンプトのバグとして"], answer: 1, explain: "拒否はエラーではなく通常のレスポンスです。検出したうえで、明確化の質問や安全な代替案を提示してください。同一入力での再試行はトークンを浪費するだけです。"},
  {q: "善意のリクエストが繰り返し拒否されます。最初に試すことは?", options: ["何を尋ねているか隠すよう言い換える", "本当の正当な目的を述べ、より具体的にする", "より小さいモデルに切り替える"], answer: 1, explain: "リクエストは機微なものにパターンマッチすると拒否されます。正当なコンテキストを加え、具体的にすることが修正法です。意図を隠すのはジェイルブレイク — 誤った動きであり、出力の有用性も下がります。"},
  {q: "モデルの拒否と分類器/安全性ブロックのどちらを受け取ったかがなぜ重要ですか?", options: ["重要ではない — 同じように扱えばよい", "レスポンス上見え方が異なることがあり、指す修正も異なる", "どちらか一方だけが課金される"], answer: 1, explain: "これらは異なる仕組みです:モデルの拒否はClaudeが辞退することで、別個の安全性レイヤーはコンテンツをブロックできます。どちらを受け取ったか知ることで、盲目的に再試行するのではなく適切に対応できます。"},
]} />

<Callout type="takeaways" items={[
  "拒否はエラーではなく結果 — レンダリング前に必ず検出する。",
  "モデルの拒否と分類器のブロックは別物;どちらを受け取ったか識別する。",
  "望まない拒否は通常、正当なコンテキストを加えて具体的にすると減る。",
  "リクエストが本当に禁止なら、拒否は正しい — 優雅な道筋を作り、ジェイルブレイクしないこと。",
  "フォールバックの順序:明確化の質問、安全な代替案、人間へのエスカレーション。",
]} />

## 次のステップ

- [エラー、レート制限と信頼性](/docs/api/errors-and-rate-limits)
- [責任ある使用、倫理と検証](/docs/security/responsible-use)
- [トラブルシューティング:なぜClaudeはそうしたのか?](/docs/contribute/troubleshooting)
