---
sidebar_position: 6
title: "Plantillas de prompts reutilizables"
description: "Prompts estructurados con XML y mapeados a tareas, con la salida esperada y notas sobre fallos."
---

<LevelBadge level="all" />

Prompts listos para usar en tareas habituales. Emplean [etiquetas XML](/docs/prompting/xml-tags) para una estructura limpia e incorporan los hábitos de [Fundamentos del prompting](/docs/prompting/basics). Rellena las `{llaves}`.

## Resumir (con base en el texto)

```text
<instructions>
Summarize the document for {audience} in {N} bullet points. Use ONLY the document;
if something isn't there, say "not stated." Then list any action items.
</instructions>
<document>
{paste}
</document>
```
*Esperado:* viñetas concisas + tareas accionables. *Fallo común:* inventa hechos → la cláusula "ONLY the document" lo evita.

## Extraer datos estructurados

```text
Extract the following from the text as JSON: {field1, field2, field3}.
Return ONLY valid JSON, no prose. If a field is missing, use null.
<text>{paste}</text>
```
*Combínalo con* [salida estructurada](/docs/api/structured-output) para mayor fiabilidad.

## Reescribir / editar

```text
Rewrite the text to be {clearer/shorter/more formal} without changing meaning.
Then list the changes you made as bullets.
<text>{paste}</text>
```

## Revisión de código

```text
Review this diff for correctness bugs, missing edge cases, and security issues.
Report ONLY high-confidence findings as a checklist with file:line. Diff:
<diff>{paste}</diff>
```

## Ayuda para decidir

```text
I'm deciding between {A} and {B} for {situation}. Build a trade-off table across
the criteria that matter, then recommend one and name the biggest risk.
```

## Metaprompt (mejorar un prompt)

```text
Here's my prompt: "{prompt}". Ask me the 3 questions that would most improve it,
then rewrite it with sensible defaults.
```

## Envoltorio de rol + protección

```text
You are {role}. {task}.
Constraints: {format, length, tone}. If you're unsure or lack info, say so rather
than guessing.
```

:::tip Hazlos tuyos
Guarda los que más uses como [comandos de barra](/docs/templates/slash-commands) o un [Proyecto](/docs/claude-app/projects) para no volver a escribirlos.
:::

## Siguiente

- [Biblioteca de patrones de prompts](/docs/prompting/pattern-library)
- [Estructurar prompts con etiquetas XML](/docs/prompting/xml-tags)
- [Fragmentos de API para producción](/docs/templates/api-snippets)
