---
sidebar_position: 4.5
title: "CLIs de agentes de codificación comparadas"
description: "Claude Code, Codex CLI, Gemini CLI y los agentes de terminal de código abierto: cómo el harness (no solo el modelo) decide tus resultados, y cómo mantener tu configuración portable con AGENTS.md."
keywords: [Claude Code vs Codex CLI, Gemini CLI, opencode, aider, coding agent comparison, AGENTS.md, terminal AI agent, coding harness]
---

<LevelBadge level="intermediate" />

Ya elegiste un modelo. Pero el programa que *envuelve* al modelo — el bucle que lee archivos, planifica, ejecuta comandos, revisa su propio trabajo y reintenta — es una elección aparte, y importa al menos tanto como el modelo. El nombre que la comunidad da a esa envoltura es **harness** (o **agente**), y el mismo modelo puede parecer brillante en uno y mediocre en otro. Esta página mapea los agentes de codificación de terminal de 2026, qué los distingue realmente, y cómo mantener tu configuración portable para que los costes de cambio sigan siendo bajos.

<Callout type="objectives" items={[
  "Separar dos elecciones que la gente confunde: el modelo frente al harness que lo dirige",
  "Conocer la forma duradera de cada CLI agente principal: Claude Code, Codex CLI, Gemini CLI y el grupo de código abierto",
  "Elegir según los ejes que realmente lo deciden: autonomía, apertura, agnosticismo de modelo, sandboxing y ecosistema",
  "Hacer tu configuración portable con AGENTS.md para poder cambiar de agente sin reescribir el contexto de tu proyecto",
]} />

<VerifyNote lastVerified="2026-07-08" source="https://agents.md">
Los recuentos de estrellas, números de versión, puntuaciones de benchmark, precios y disponibilidad de planes/niveles en esta página cambian **rápido** — los rankings de agentes de codificación se reordenan casi cada mes y los proveedores modifican los niveles gratuitos con poco aviso. Trata los *arquetipos y ejes* de abajo como duraderos; vuelve a verificar cualquier número concreto contra el propio repo o documentación de cada proyecto (enlazados en Fuentes) antes de decidir.
</VerifyNote>

## El modelo no es el agente

Un modelo de frontera es un predictor de texto. Un **agente de codificación** es el harness que lo rodea: el prompt de sistema, el conjunto de herramientas (leer, editar, ejecutar shell, buscar), el bucle de planificación, la estrategia de gestión de contexto, el modelo de permisos y sandbox, y la lógica de reintento. Cambia el harness y cambias los resultados — informes de profesionales a lo largo de 2026 describen el mismo modelo oscilando decenas de puntos en tareas reales de repositorio según qué agente lo dirija.

Eso tiene dos consecuencias prácticas:

- **Un número de benchmark es un *emparejamiento*, no la puntuación de un modelo.** "83% en Terminal-Bench" es un resultado de *modelo + harness + andamiaje*. Cambia cualquier parte y se mueve.
- **Tu bucle es la verdadera unidad de comparación.** El mejor agente es el que funciona mejor en *tu* stack, en *tu* terminal, a un precio que puedas asumir — exactamente la misma lógica que elegir un modelo de codificación. Consulta [Claude vs GPT vs Gemini para programar](/docs/models/claude-gpt-gemini-for-coding) para la mitad de la decisión que corresponde al modelo; esta página es la mitad del harness.

## El panorama en 2026

El espacio de los agentes de terminal se dividió en dos bandos: **agentes de primera parte** afinados para los modelos de un solo laboratorio, y agentes **agnósticos de modelo** que apuntas a cualquier modelo (o pesos locales) que quieras.

| Agente | Quién | Apertura | Modelos | Forma |
|---|---|---|---|---|
| **Claude Code** | Anthropic | Código cerrado | Claude (primera parte) | Trabajo autónomo, agéntico, multi-archivo; sandbox impuesto por el SO; ecosistema profundo de skills/MCP/subagentes |
| **Codex CLI** | OpenAI | Abierto (Apache-2.0, Rust) | GPT (primera parte) | Agente de terminal autónomo; sandbox nativo de Windows; amplio alcance de plataformas |
| **Gemini CLI** | Google | Código abierto | Gemini (primera parte) | Agente de gran contexto, integraciones con el ecosistema de Google (ver nota de actualidad abajo) |
| **opencode** | Comunidad (anomalyco) | Abierto (MIT, TypeScript) | Cualquiera (agnóstico de modelo) | El agente abierto con más estrellas; CLI + escritorio; trae tu propio modelo |
| **Aider** | Comunidad | Abierto (Apache-2.0) | Cualquiera, incl. local | Compañero de programación nativo de Git, no un orquestador autónomo completo |
| **Cline / Goose / otros** | Comunidad | Abierto | Cualquiera | Embebido en el editor (Cline) o agentes abiertos generales (Goose); agnósticos de modelo |

<VerifyNote lastVerified="2026-07-08" source="https://github.com/google-gemini/gemini-cli">
**Gemini CLI está en cambio constante.** A mediados de 2026 Google modificó el acceso al nivel gratuito y dirigió a algunos usuarios hacia una CLI separada llamada "Antigravity". Si Gemini CLI, Antigravity o algo más nuevo es el punto de entrada correcto de Google es exactamente el tipo de dato que queda obsoleto — confirma el estado actual en la propia documentación de Google antes de comprometerte.
</VerifyNote>

## Los ejes que realmente lo deciden

Ignora la tabla de clasificación por un minuto. Estas son las preguntas duraderas:

<Steps items={[
  {title: "¿De primera parte o agnóstico de modelo?", body: "Los agentes de primera parte (Claude Code, Codex CLI, Gemini CLI) están afinados para su propio modelo y suelen dar la experiencia pico más fluida. Los agentes agnósticos de modelo (opencode, Aider, Cline) te dejan intercambiar modelos — frontera hoy, barato o local mañana — sin cambiar de herramientas. Elige agnóstico si evitar el bloqueo de proveedor o ejecutar pesos locales te importa."},
  {title: "¿Cuánta autonomía quieres?", body: "Aider es un compañero de programación: ediciones enfocadas, a nivel de archivo, nativas de git, contigo en el bucle en cada commit. Claude Code y Codex CLI son orquestadores: das un objetivo y planifican y ejecutan a través de muchos archivos y comandos de shell. Más autonomía significa más apalancamiento y más necesidad de barreras de protección."},
  {title: "¿Cuál es el radio de impacto?", body: "Un agente que ejecuta comandos de shell puede borrar archivos, subir código o gastar dinero. Prefiere un sandbox impuesto por el SO y un modelo de permisos explícito antes que un 'confía en mí'. Claude Code y Codex CLI incorporan sandboxing en 2026; verifica qué puede tocar cualquier agente antes de dejarlo ejecutar sin supervisión."},
  {title: "¿Encaja el ecosistema con tu trabajo?", body: "Más allá del bucle central: servidores MCP para herramientas, skills reutilizables, subagentes, hooks, integración con el IDE, acciones de CI. Un agente más delgado sobre un gran modelo puede perder frente a un agente más grueso sobre uno suficientemente bueno porque el ecosistema hace más del trabajo."},
  {title: "¿Cuánto cuesta en TU bucle?", body: "Suscripción frente a medición por API, ventana de contexto, caché y lo hablador que sea el harness mueven la factura real. Un modelo más barato en un harness ávido de tokens puede costar más que un modelo más caro en uno frugal."},
]} />

<Callout type="tip" items={["No compres un harness por un benchmark de titular. Cómpralo por una prueba de 30 minutos contra una tarea real en tu propio repo — el mismo movimiento de 'evaluar sobre tu propio código' que gana a cualquier tabla de clasificación de modelos.", "La apertura es una cobertura, no una función que usarás a diario. Su valor aparece el día que cambia un precio o desaparece un nivel gratuito y quieres mudarte."]} />

## Portabilidad: escribe tu contexto una sola vez

El mayor coste de cambio oculto es el **contexto de tu proyecto** — comandos de compilación, convenciones, reglas de pruebas, cosas que el agente no puede inferir. En 2026 hay un estándar de facto para eso: **`AGENTS.md`**, un archivo de Markdown plano en la raíz de tu repo, gestionado por la Agentic AI Foundation bajo la Linux Foundation y leído de forma nativa por Codex, Gemini CLI, Cursor, Aider, el agente de codificación de Copilot, opencode, Goose y más de 20 herramientas más (más de 60.000 repos lo han adoptado).

- **`AGENTS.md`** = contexto de proyecto entre herramientas. Escríbelo una vez; la mayoría de los agentes lo leen.
- **`CLAUDE.md`** = el propio archivo de memoria de proyecto de Claude Code (ver [CLAUDE.md](/docs/claude-code/claude-md)). Puedes mantener un pequeño `CLAUDE.md` que apunte a `AGENTS.md`, o mantener ambos.
- **`SKILL.md`** = la especificación separada de Anthropic para empaquetar [skills](/docs/claude-code/skills) reutilizables con frontmatter y scripts incluidos. Es *complementaria* a `AGENTS.md`, no una competidora — una describe el proyecto, la otra empaqueta un procedimiento reutilizable.

Mantener el contexto en `AGENTS.md` significa que el día que pruebes un agente distinto, las reglas de tu proyecto vienen contigo gratis.

<PromptCard title="AGENTS.md mínimo — escribe solo lo que un agente no puede inferir">{`# AGENTS.md

## Setup
- Install: \`pnpm install\`
- Dev server: \`pnpm dev\` (port 3000)

## Build & test (run before every commit)
- Typecheck: \`pnpm typecheck\`
- Lint: \`pnpm lint\`
- Tests: \`pnpm test\` — all must pass

## Conventions
- TypeScript strict; no \`any\`.
- Components in \`src/components\`, one per file.
- Never edit files under \`generated/\` by hand.

## Constraints
- Do NOT run \`git push\` or open PRs without being asked.
- Secrets live in \`.env.local\`; never commit them.
`}</PromptCard>

<Callout type="warning" items={["Pon en AGENTS.md solo lo que el agente no puede descubrir por sí mismo. Repetir lo obvio cuesta tokens en cada ejecución y entierra las reglas que importan. Cúralo a mano — los archivos de contexto autogenerados tienden a ser ruido."]} />

## Una decisión, de un tirón

- **Trabajo profundo y autónomo con Claude y el ecosistema de herramientas más rico** → **Claude Code**.
- **Agente de primera parte de código abierto ligado a GPT, con fuerte alcance de plataforma/Windows** → **Codex CLI**.
- **Quieres intercambiar modelos libremente o ejecutar pesos locales** → **opencode** o **Cline**.
- **Ediciones cuidadosas, nativas de git, con humano en el bucle** → **Aider**.
- **Gran apuesta por el contexto del ecosistema de Google** → **Gemini CLI** (confirma primero el acceso actual).

Elijas lo que elijas, mantén el contexto de tu proyecto en `AGENTS.md` y demuestra la elección con una tarea real en tu propio repo antes de comprometerte.

<Quiz questions={[{q: "¿Qué quiere decir la gente con el 'harness' del agente de codificación?", options: ["Los datos de entrenamiento del modelo", "El programa que envuelve el modelo — sus herramientas, bucle de planificación, sandbox y lógica de reintento", "El tema del IDE", "La GPU en la que se ejecuta"], answer: 1, explain: "El harness es el agente que rodea al modelo: prompt de sistema, herramientas, bucle de planificación/reintento, gestión de contexto y sandbox. Puede mover los resultados en tareas reales decenas de puntos con el mismo modelo."}, {q: "Un blog informa de un '83% en un benchmark de codificación' para un agente. ¿Qué está midiendo realmente ese número?", options: ["El modelo solo", "Un emparejamiento concreto de modelo + harness + andamiaje", "El lenguaje de programación", "La habilidad del usuario"], answer: 1, explain: "La puntuación de un agente de codificación es un emparejamiento. Cambia el modelo, el harness o el andamiaje y el número se mueve — por eso tu propio repo es la única prueba que cuenta."}, {q: "¿Por qué elegir un agente agnóstico de modelo como opencode o Aider?", options: ["Siempre es más preciso", "Para intercambiar modelos libremente y ejecutar pesos locales, cubriéndote ante cambios de precio y de nivel", "No necesita configuración", "Solo funciona con un modelo"], answer: 1, explain: "Los agentes agnósticos de modelo te dejan apuntar a cualquier modelo — de frontera, barato o local — para que un cambio de precio o la retirada de un nivel gratuito no fuerce una migración de herramienta."}, {q: "¿Para qué sirve AGENTS.md?", options: ["Un changelog", "Contexto de proyecto entre herramientas (comandos de compilación/pruebas, convenciones, restricciones) leído por más de 20 agentes", "Un archivo de licencia", "Memoria solo para Claude"], answer: 1, explain: "AGENTS.md es un archivo de contexto de proyecto en Markdown, estándar de facto, leído por Codex, Gemini CLI, Cursor, Aider, opencode y muchos otros — escríbelo una vez y la mayoría de los agentes lo usan. CLAUDE.md es el equivalente propio de Claude Code."}]} />

## Flashcards

<Flashcards title="Vocabulario de las CLIs de agentes de codificación" cards={[
  {front: "Harness (agente)", back: "El programa que envuelve un modelo para convertirlo en un agente de codificación: prompt de sistema, herramientas (leer/editar/shell/buscar), bucle de planificación + reintento, gestión de contexto, permisos y sandbox. Importa tanto como el modelo."},
  {front: "Primera parte vs agnóstico de modelo", back: "Los agentes de primera parte (Claude Code→Claude, Codex CLI→GPT, Gemini CLI→Gemini) están afinados para el modelo de un solo laboratorio. Los agentes agnósticos de modelo (opencode, Aider, Cline) apuntan a cualquier modelo, incluidos pesos locales."},
  {front: "Claude Code", back: "El agente de terminal de código cerrado de Anthropic para Claude. Trabajo autónomo multi-archivo, sandbox impuesto por el SO y un ecosistema profundo: MCP, skills, subagentes, hooks, acciones de CI."},
  {front: "Codex CLI", back: "El agente de terminal de código abierto de OpenAI (Apache-2.0, Rust) para GPT. Autónomo, sandbox nativo de Windows, amplio alcance de plataformas. ~96k estrellas en GitHub a mediados de 2026."},
  {front: "opencode", back: "Agente comunitario, de código abierto (MIT, TypeScript) y agnóstico de modelo — el agente de codificación abierto con más estrellas en 2026. CLI más aplicación de escritorio; trae tu propio modelo."},
  {front: "Aider", back: "Compañero de programación de código abierto (Apache-2.0) nativo de git. Ediciones enfocadas a nivel de archivo contigo en el bucle, funciona con casi cualquier LLM, incluidos modelos locales. No es un orquestador autónomo completo."},
  {front: "AGENTS.md", back: "Archivo de contexto de proyecto en Markdown, estándar de facto, en la raíz del repo. Gestionado por la Agentic AI Foundation (Linux Foundation); leído por más de 20 agentes en más de 60.000 repos. Escribe el contexto una vez y la mayoría de los agentes lo leen."},
  {front: "CLAUDE.md vs AGENTS.md vs SKILL.md", back: "CLAUDE.md = memoria de proyecto de Claude Code. AGENTS.md = contexto de proyecto entre herramientas. SKILL.md = paquete de skill reutilizable de Anthropic. Complementarios, no competidores."},
]} />

## Fuentes y lecturas adicionales

- [AGENTS.md — sitio oficial y especificación](https://agents.md) (Agentic AI Foundation / Linux Foundation)
- [openai/codex — repo de Codex CLI](https://github.com/openai/codex) (Apache-2.0, Rust)
- [sst/opencode — repo de opencode](https://github.com/sst/opencode) (MIT, TypeScript)
- [Aider-AI/aider — repo de Aider](https://github.com/Aider-AI/aider) (Apache-2.0)
- [google-gemini/gemini-cli — repo de Gemini CLI](https://github.com/google-gemini/gemini-cli)
- [Anthropic — documentación de Claude Code](https://docs.claude.com/en/docs/claude-code/overview)
- Relacionado en este sitio: [Claude vs GPT vs Gemini para programar](/docs/models/claude-gpt-gemini-for-coding) · [Agentes de codificación locales](/docs/models/local-coding-agents) · [CLAUDE.md](/docs/claude-code/claude-md) · [Skills](/docs/claude-code/skills)
