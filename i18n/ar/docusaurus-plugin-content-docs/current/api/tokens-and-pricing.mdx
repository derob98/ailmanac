---
sidebar_position: 2
title: "الرموز والسياق والتسعير"
description: "عُدّ الرموز بشكل صحيح، و max_tokens مقابل نافذة السياق، وتقدير التكلفة."
---

<LevelBadge level="beginner" />

<Callout type="objectives" items={["عُدّ الرموز بشكل صحيح باستخدام أدوات Anthropic نفسها (لا مُجزّئ رموز نموذج آخر)", "ميّز بين max_tokens ونافذة السياق — ولماذا يهمّ هذا الفرق", "قدّر تكلفة الواجهة البرمجية من رموز المدخلات + المخرجات بأسعار كل نموذج", "اخفض التكلفة دون خسارة الجودة: اختر الحجم المناسب، خزّن مؤقتًا، قلّص، جمّع في حِزَم"]} />

<VerifyNote lastVerified="2026-06-20" source="https://platform.claude.com/docs/en/docs/about-claude/pricing">
تتغيّر الأسعار لكل رمز وحدود السياق — اقرأ دائمًا الأرقام الحالية على صفحات التسعير/النماذج الرسمية. راجع [جدول النماذج](/docs/whats-new/models-and-pricing).
</VerifyNote>

التكلفة والحدود على الواجهة البرمجية تُقاس جميعها بـ**[الرموز](/docs/foundations/tokens-and-context)** (~ثلاثة أرباع الكلمة). ثلاثة أمور ينبغي ضبطها بدقة.

## 1. عُدّ الرموز بشكل صحيح

لا تخمّن، و**لا تستخدم مُجزّئ رموز نموذج آخر** (مثل `tiktoken`) — إذ تختلف أعداد الرموز بين عائلات النماذج. استخدم **نقطة نهاية عدّ الرموز** من Anthropic أو مساعد SDK لقياس الطلب *قبل* إرساله.

<Callout type="tip" items={["قاعدة تخطيط تقريبية: ~750 كلمة ≈ ~1000 رمز (الرمز يساوي تقريبًا ثلاثة أرباع الكلمة)."]} />

## 2. `max_tokens` ≠ نافذة السياق

من السهل الخلط بين هذين الحدّين، لكنهما يحكمان أمرين مختلفين.

| الحدّ | ما الذي يقيّده | متى تغيّره |
| --- | --- | --- |
| **`max_tokens`** | طول *الردّ* (المخرجات فقط) | ارفعه إذا اقتُطِعت المخرجات |
| **نافذة السياق** | الميزانية الكلّية لـ**المدخلات + المخرجات** | المدخلات الكبيرة تترك مساحة أقلّ للمخرجات |

<Callout type="warning" items={["اضبط max_tokens بحسب ما تحتاجه المهمة. القيمة المنخفضة جدًا تقتطع الردّ. والعالية بلا داعٍ لا تكلّف أكثر — فأنت تدفع فقط مقابل الرموز المُولَّدة فعليًا — لكنها قد تجعل الردود تستطرد."]} />

## 3. قدّر التكلفة

تُحاسَب على **رموز المدخلات + رموز المخرجات**، بأسعار لكل نموذج (Opus > Sonnet > Haiku). تقدير سريع:

```
cost ≈ (input_tokens × input_rate) + (output_tokens × output_rate)
```

احصل على الأسعار الحالية من [صفحة التسعير الرسمية](https://platform.claude.com/docs/en/docs/about-claude/pricing) — نحن لا نُرمّزها هنا عن قصد.

## خفض التكلفة (دون خسارة الجودة)

<Steps items={[
  {title: "اختر حجم النموذج المناسب", body: "ابدأ بـ Sonnet؛ واحفظ Opus للأجزاء الصعبة. راجع اختيار نموذج (/docs/api/choosing-a-model)."},
  {title: "استخدم التخزين المؤقت للمطالبات", body: "أعِد استخدام بادئة مطالبة ثابتة عبر الاستدعاءات. راجع التخزين المؤقت للمطالبات (/docs/api/prompt-caching)."},
  {title: "قلّص المدخلات", body: "أرسل فقط السياق المهمّ. وهنا يساعد RAG (/docs/foundations/rag) أيضًا."},
  {title: "جمّع العمل غير المتزامن في حِزَم", body: "حزّم المهام التي لا يهمّ فيها زمن الاستجابة."}
]} />

مزيد من الاستراتيجية في [مفاضلات التكلفة والزمن](/docs/foundations/cost-and-latency).

<Quiz title="اختبر نفسك" questions={[
  {
    q: "تخطّط لطلب وتريد معرفة عدد رموزه قبل إرساله. ماذا ينبغي أن تستخدم؟",
    options: ["مُجزّئ رموز عام مثل tiktoken", "نقطة نهاية عدّ الرموز من Anthropic أو مساعد SDK", "عدد الكلمات مقسومًا على 0.75 كرقم نهائي", "حجم نافذة السياق من جدول النماذج"],
    answer: 1,
    explain: "تختلف أعداد الرموز بين عائلات النماذج، لذا لا تُعِد استخدام مُجزّئ رموز نموذج آخر. استخدم نقطة نهاية عدّ الرموز من Anthropic أو مساعد SDK للقياس قبل الإرسال."
  },
  {
    q: "يستمرّ ردّك في الاقتطاع في منتصف الجملة. أيّ إعداد تغيّر؟",
    options: ["خفّض max_tokens", "ارفع max_tokens", "قلّص نافذة السياق", "انتقل إلى نموذج أصغر"],
    answer: 1,
    explain: "يضع max_tokens حدًّا لطول الردّ. إذا اقتُطِعت المخرجات، فارفعه."
  },
  {
    q: "كيف تُحاسَب على الواجهة البرمجية؟",
    options: ["لكل طلب، بسعر ثابت", "رموز المدخلات فقط", "رموز المدخلات + رموز المخرجات، بأسعار لكل نموذج", "لكل كلمة من المخرجات"],
    answer: 2,
    explain: "cost ≈ (input_tokens × input_rate) + (output_tokens × output_rate)، بأسعار تختلف لكل نموذج (Opus > Sonnet > Haiku)."
  },
  {
    q: "ما أفضل خطوة أولى لخفض التكلفة دون خسارة الجودة؟",
    options: ["استخدم Opus دائمًا لكل شيء", "ابدأ بـ Sonnet واحفظ Opus للأجزاء الصعبة", "اضبط max_tokens على أعلى قيمة ممكنة", "استخدم مُجزّئ رموز نموذج آخر لتوفير الاستدعاءات"],
    answer: 1,
    explain: "اختر حجم النموذج المناسب: ابدأ بـ Sonnet، واحفظ Opus للأجزاء الصعبة. ثم أضِف فوق ذلك التخزين المؤقت للمطالبات، وتقليص المدخلات، والتجميع في حِزَم."
  }
]} />

<Callout type="takeaways" items={["كل شيء يُقاس بالرموز (~ثلاثة أرباع الكلمة)؛ ~750 كلمة ≈ ~1000 رمز.", "عُدّ الرموز بأدوات Anthropic نفسها — لا بمُجزّئ رموز نموذج آخر أبدًا.", "يضع max_tokens حدًّا للردّ؛ ونافذة السياق هي ميزانية المدخلات + المخرجات الكلّية.", "المحاسبة = رموز المدخلات + رموز المخرجات بأسعار لكل نموذج (Opus > Sonnet > Haiku).", "اخفض التكلفة باختيار حجم النموذج المناسب، وتخزين البادئات مؤقتًا، وتقليص المدخلات، والتجميع في حِزَم."]} />

## التالي

- [اختيار نموذج Claude](/docs/api/choosing-a-model)
- [التخزين المؤقت للمطالبات وتحسين التكلفة](/docs/api/prompt-caching)
- [البثّ والمحادثات متعدّدة الجولات](/docs/api/streaming)
